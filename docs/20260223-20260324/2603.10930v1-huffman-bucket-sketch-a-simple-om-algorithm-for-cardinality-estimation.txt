Title: Huffman-Bucket Sketch: A Simple $O(m)$ Algorithm for Cardinality Estimation

URL Source: https://arxiv.org/pdf/2603.10930v1

Published Time: Thu, 12 Mar 2026 01:14:35 GMT

Number of Pages: 25

Markdown Content:
# Huffman-Bucket Sketch 

A Simple 𝑂 (𝑚 ) Algorithm for Cardinality Estimation 

## Matti Karppa 

University of Gothenburg, Sweden Chalmers University of Technology, Sweden 

Abstract 

We introduce the Huffman-Bucket Sketch (HBS) , a simple, mergeable data structure that losslessly com-presses a HyperLogLog (HLL) sketch with 𝑚 registers to optimal space 𝑂 (𝑚 +log 𝑛 ) bits, with amortized constant-time updates, acting as a drop-in replacement for HLL that retains mergeability and substan-tially reduces memory requirements. We partition registers into small buckets and encode their values with a global Huffman codebook derived from the strongly concentrated HLL rank distribution, us-ing the current cardinality estimate for determining the mode of the distribution. We prove that the Huffman tree needs rebuilding only 𝑂 (log 𝑛 ) times over a stream, roughly when cardinality doubles. The framework can be extended to other sketches with similar strongly concentrated distributions. We provide preliminary numerical evidence that suggests that HBS is practical and can potentially be competitive with state-of-the-art in practice. 

# 1 Introduction 

Estimating the number of distinct elements , or cardinality in a massive data stream is a standard primitive in databases [Tru19, CDP +19, FN19, WCLL21, PCT23], networking [CCD14, BEF +18, DJJ +22, CSGH23], and metagenomics [BL19, MSPK19], among others. As the universe of the elements can be essentially infinite, such as the set of all variable-length strings, storing a representative for each element takes Ω(𝑛 log 𝑛 )

bits, by the pigeonhole principle, assuming the true cardinality of the stream is 𝑛 . Even if we can initialize binary counters for all elements in the universe, an exact count needs Θ(𝑛 ) bits [AMS99], which can be infeasibly large. However, the cardinality can be estimated with fewer bits, and it is known that Θ(𝑚 )

bits are necessary and sufficient to achieve a relative standard error of 

q

Var  ˆ𝑛 𝑛 

 = 𝑂 (1/√𝑚 ) [KNW10]. A practical standard solution to the distinct elements problem is the HyperLogLog (HLL) sketch [FFGM07], which has size 𝑂 (𝑚 log log 𝑛 ) bits for a relative error of 𝑂 (1/√𝑚 ). It is attractive because it is almost trivial to implement, can be implemented incredibly efficiently, has constant-time updates, and is mergeable, meaning that a large datastream can easily be processed in a distributed fashion by maintaining sketches of substreams that can later be merged into one sketch, as if the entire stream had been processed by a single sketch. A lot of the work on improving the space usage over HLL has had to compromise some of these properties, such as mergeability [PWY21] or truly constant-time updates [KP22]. In this paper, we present a simple algorithm and an accompanying data structure, the Huffman-Bucket Sketch (HBS) , that can losslessly compress the HLL sketch to 𝑂 (𝑚 ) bits using Huffman codes, while preserving mergeability and sustaining efficient updates. While not truly constant time, the updates are amortized 𝑂 (1), and under mild assumptions on the relationship between 𝑛 and 𝑚 , most updates can be constant time, with only a small fraction of updates requiring more time to update the codewords. The main idea behind the algorithm is to leverage the fact that the distribution of register values (or 

ranks ) in the HLL sketch is highly concentrated around ⌈log 2(𝑛 /𝑚 )⌉ , with very fast decaying tails. In fact, as it is well known, the entropy of the register values is asymptotically constant per register [Dur04], and this manifests in the fact that if we split the registers into 𝑂 (log 𝑛 )-sized buckets, the distribution of the Huffman codeword lengths in each bucket is also highly concentrated, requiring only 𝑂 (log 𝑛 ) bits per bucket with high probability. We show that, as we process a stream of 𝑛 distinct elements, the Huffman tree needs to be recon-structed only 𝑂 (log 𝑛 ) times, essentially only when the cardinality is doubled, which we can detect by maintaining a local cardinality estimate. That is, even though the true cardinality 𝑛 is unknown to us, we can pull ourselves up from the swamp by our hair, much like Baron von Münchhausen, by using the 1

> arXiv:2603.10930v1 [cs.DS] 11 Mar 2026

current global estimate to approximate the register value distribution, which is unambiguously deter-mined by the cardinality for a fixed number of registers, and subsequently unambiguously determines the Huffman tree. The resulting data structure supports the regular operations of estmating a cardinality, inserting a new element in the sketch, querying a register value, and merging two sketches. The asymptotic size of the sketch is 𝑂 (𝑚 + log 𝑛 ) bits, which is optimal [KNW10], and the amortized time for insertions is 𝑂 (1),while merges can be carried out in ˜𝑂 (𝑚 ) time. 1

The idea of bucketing originates from practical considerations: if the size of the bucket is sufficiently small, such as a single machine word or a cache line, then any operations performed on it are essentially constant time. We complement the theoretical, asymptotic analysis by considering the practical implica-tions and implementation concerns of the algorithm, and show numerical evidence that suggests that the algorithm is competetive with the state-of-the-art, especially accounting the fact that it is, in fact, a drop-in replacement for HLL that can be decompressed back to a HLL sketch at any point, and thus maintains the same mergeability properties. We do not analyze the estimation procedure in this paper, as any HLL-compatible estimator can be used, such as the original HLL estimator, Ertl’s MLE estimator [Ert17], or Pettie and Wang’s state-of-the-art estimator [PW21] with relative variance of 1.075 /𝑚 .The rest of the paper is organized as follows. In Section 2, we review the notation, the mathematical preliminaries, the Poisson model, and recap HLL. Section 3 is the main result section, describing the HBS algorithm and the data structure. Section 4 provides asymptotic analysis of the algorithm in the Poissonized balls-and-bins model. In Section 5, we briefly outline extensions of the framework. Section 6 discusses implementation considerations and empirical implications, including the Memory-Variance Product (MVP) of the sketch. 

## 1.1 Related work 

The line of work leading to HLL and its variants was initiated by Flajolet and Martin with their prob-abilistic counting algorithm [FM85], the foundation of which was a sparse indicator matrix, the FM85 matrix , whose columns correspond to geometrically distributed ranks of hash values, and rows to uni-formly distributed substreams. This was refined by Durand and Flajolet’s LogLog algorithm [DF03], whose key innovation was compressing the FM85 matrix by only retaining the maximum rank per row. HLL [FFGM07] was obtained from this by replacing the arithmetic mean in the estimator with the har-monic mean, improving the accuracy and robustness of the estimator, and has since become the standard solution for cardinality estimation in practice. HLL has seen a large number of refinements and variants, both practical and theoretical, including HyperLogLog++[HNH13], HLL-Tailcut [XCZL20], and HyperLogLogLog [KP22]. Another, related and simple cardinality estimation method is linear counting [WZT90], which can be seen as a Bloom filter with one hash function, and is optimal for small cardinalities, assuming 𝑚 = Θ (𝑛 ), and is used as small-cardinality correction in HLL. Kane, Nelson, and Woodruff [KNW10], later improved by Błasiok [Bla20], showed the existence of an optimal 𝑂 (𝑚 + log 𝑛 )-bit sketch with constant time updates, but this sketch is widely considered impractical. Pettie and Wang have established sharp information-theoretic limits [PW21] for cardinality estimation, and observed that the HLL sketch loses some information of the FM85 matrix that would be beneficial for further compression, confirming the observation by Lang [Lan17] that the FM85 matrix can be compressed beyond the entropy of the HLL sketch. This has led to various other work that extends the compression using additional information from the FM85 matrix, including UltraLogLog [Ert24], ExaLogLog [Ert25], and SpikeSketch [DHS +23]. Another line of work has focused on forfeiting merge-ability by using the martingale transform [PWY21]. Finally, while we only address compression of the sketch, a lot of the other work has also focused on improving the estimators [Ert17, Ert24, WP23]. 

# 2 Preliminaries 

## 2.1 Mathematical notation and preliminaries 

We write [𝑛 ] = {1, 2, . . . , 𝑛 } for the set of the first 𝑛 positive integers, and the Iverson bracket notation 

⟦𝑃 ⟧ to denote the indicator of a predicate 𝑃 , which is 1 if 𝑃 is true and 0 otherwise.   

> 1˜𝑂 (·) suppresses polylogarithmic factors.

22.2 Poissonized balls-and-bins model 

The analysis of the algorithm is carried out in the Poissonized balls-and-bins model, which is a standard model for analyzing such algorithms [Ert17, Ert24, Ert25, PW21, PWY21, WP23], and is known to be almost equivalent to the exact model. In the balls-and-the-bins model, we throw 𝑛 balls independently and uniformly at random into 𝑚 

bins, and analyze the distribution. The downside of this model is that the bins are dependent. To address this, we Poissonize the model by letting balls arrive independently at random in the bins, with rate 𝜆 = 𝑛 𝑚 , meaning that the number of balls in each bin is independently and identically distributed as Pois (𝜆 ). This makes 𝑛 a random variable, but assuming the number of bins is sufficiently large, the distribution is well known to be almost equal. 2 In the actual analysis, we have two conjoined processes where the row of the FM85 matrix is selected uniformly at random but the column is selected according to the geometric distribution. Nevertheless, the same Poissonization technique applies. 

## 2.3 HyperLogLog (HLL) 

Let 𝑤 be the word size. We assume that all hash functions ℎ : 𝒰 → [ 2𝑤 ] are fully random and yield 𝑤 -bit hashes. As we are going to hash all elements, by the pigeonhole principle, we require that 𝑤 = Ω (log 𝑛 )

to be able to tell all elements apart. The HLL sketch [FFGM07] consists of an array 𝑀 of 𝑚 registers, each of which records the maximum 

rank of hashes of substreams that are mapped to the register. All registers are initialized to zero. Upon receiving a new element 𝑦 , the element is hashed into a 𝑤 -bit value 𝑥 = ℎ(𝑦 ). The hash value is split in two parts: log 2 𝑚 bits of 𝑥 are used to select the register index 𝑗 ∈ [ 𝑚 ], and the remaining bits are used to compute the rank 𝑟 = 𝜌 (𝑥 ), which is the 1-based position of the leftmost 1-bit in the remaining part of the hash value. It is immediate that 𝑟 ∼ Geom (1/2). The update rule is thus 𝑀 [𝑗 ] ← max {𝑀 [𝑗 ], 𝑟 }.The sketch is clearly idempotent and can be easily merged by taking the elementwise maximum of the registers. The size of the HLL sketch is 𝑂 (𝑚 log log 𝑛 ) bits, as each register records an index of the 𝑤 -bit hash value, which can be stored in 𝑂 (log 𝑤 ) = 𝑂 (log log 𝑛 ) bits. The update time is 𝑂 (1), assuming the hash value can be computed in constant time. In addition to the sketch, 𝑤 = 𝑂 (log 𝑛 ) auxiliary bits are needed to store the hash value 𝑥 , yielding a total size of 𝑂 (𝑚 log log 𝑛 + log 𝑛 ) bits for the sketch. We skip the estimation routine, as it is not relevant for this paper. 

## 2.4 Huffman coding 

The Huffman code [Huf52] over 𝑛 symbols with probabilities 𝑝 𝑟 is constructed by assigning each symbol 𝑟 

as a singleton tree with weight 𝑝 𝑟 , and then recursively merging the two trees with the least weights, by joining them with a node that records the sum of the total weights of the trees, until only tree remains. If 𝑝 1 ≤ 𝑝 2 ≤ · · · ≤ 𝑝 𝑛 , that is, the probabilities are sorted, this can be done in 𝑂 (𝑛 ) time [vL76]. The resulting tree is a full binary tree with 𝑛 − 1 internal nodes and 𝑛 leaves; the codeword is obtained by traversing the tree from the root to a leaf corresponding to the symbol, assigning 0 and 1 to the left and right edges, and concatenating the bits. Let 𝐿 be a random variable for the length of the codeword. It is well known that the expected codeword length is at most the entropy of the distribution plus one bit, that is, E[𝐿 ] ≤ 𝐻 (𝑅 ) + 1 = 1 − Í𝑛 𝑟 =1 𝑝 𝑟 log 2 𝑝 𝑟 .

# 3 Algorithm and data structure 

In this section, we describe the algorithm and the associated data structure. At some points, we will make forward-references to propositions that are proved in Section 4, but the description of the algorithm and data structure is self-contained, and does not require any knowledge of the analysis. The description here is aimed at the asymptotic analysis, and a practical implementation would likely deviate on some points for improved leading constants, which will be addressed in Section 6. 

> 2For a standard textbook reference, see [MU05].

33.1 Huffman-Bucket Sketch data structure 

The Huffman-Bucket Sketch data structure consists of 

• An array of 𝑚 𝐵 buckets, each of which holds 𝐵 registers, 

• A global Huffman tree or codebook mapping between ranks and variable-length codewords, 

• A global cardinality estimate ˆ𝑛 , and 

• The cardinality estimate ˆ𝑛 old at the time of the last Huffman tree construction. Each bucket, in turn, consists of 

• An array of 𝐵 registers, encoded as variable-width Huffman codewords, 

• An array containing a unary encoding of register codeword lengths, 

• The bucketwise minimum rank 𝑟 min of the registers in the bucket, 

• The count of minimum-rank registers 𝑐 min , and 

• The bucketwise cardinality estimate ˆ𝑛 𝑏 .The data structure is simply initialized with estimates ˆ𝑛 𝑏 = ˆ𝑛 = 0 for all buckets 𝑏 , with a trivial Huffman tree, 3 encoding all register values to the codeword corresponding to rank zero, and setting 𝑟 min = 0 and 

𝑐 min = 𝐵 for all buckets. Let 𝐵 = 𝑂 (log 𝑛 ). Suppose that the codeword lengths are ℓ1 , ℓ 2 , . . . , ℓ 𝐵 , and let 𝐿 = Í𝐵 𝑗 =1 ℓ𝑗 be the total length of the codewords in the bucket. We will later show in Proposition 6 that the codeword array has at most 𝐿 = 𝑂 (log 𝑛 ) bits, with high probability. The unary encoding of ℓ𝑗 is 1ℓ𝑗 0, so therefore the unary array takes Í𝐵 𝑗 =1(ℓ𝑗 + 1) = 𝑂 (log 𝑛 ) bits. The minimum rank and the count of minimum rank registers require 𝑂 (log log 𝑛 ) bits, and the cardinality estimate requires 𝑂 (log 𝑛 ) bits. Thus, the total size of a bucket is 𝑂 (log 𝑛 ) bits. This, in turn, means that the total size of the bucket array is 𝑚 𝐵 · 𝑂 (log 𝑛 ) = 𝑂 (𝑚 )

bits. By Proposition 3, the registers of the buckets have the same distribution as the overall sketch, so we only need to maintain one global Huffman tree for the entire sketch, which is constructed using the global cardinality estimate ˆ𝑛 . Furthermore, by Proposition 5, the global cardinality estimate is just a better estimate of the latent cardinality parameter that unambiguously determines the distribution of the register values in the buckets for a fixed 𝑚 , and thus the Huffman tree. The estimate ˆ𝑛 represents our best knowledge of 𝑛 . In the case of very small cardinalities for a bucket, the minimum rank and the count of minimum rank registers allow us to simply fall back to linear counting as a small-cardinality correction, like in [FFGM07]. The Huffman tree is a full binary tree of 𝑂 (log 𝑛 ) nodes, and as such can be encoded in 𝑂 (log 𝑛 ) bits that store the structure of the tree, mapping each node to whether it is a leaf or an internal node. The size of the global cardinality estimates is 𝑂 (log 𝑛 ) bits. We can thus state the following theorem. 

Theorem 1. The total size of the Huffman-Bucket Sketch data structure is 𝑂 (𝑚 + log 𝑛 ) bits, which is opti-mal [AMS99, IW03, KNW10, Woo04]. Proof. This follows directly from the definition of the data structure and Proposition 6. □

We can improve the running times of operations on the sketch if we assume 𝑚 = Ω (log 2 𝑛 ). This allows us to replace the Huffman tree with two lookup tables: an array that maps ranks to codewords, taking 𝑂 (log 2 𝑛 ) bits, and a hash table that maps codewords to ranks, taking as well 𝑂 (log 2 𝑛 ) bits. The total size of the sketch becomes then 𝑂 (𝑚 ) bits. 

## 3.2 Operations on the Huffman-Bucket Sketch 

The sketch supports four operations: peek, poke, insert, and merge. The peek operation takes a buck-etwise index pair (𝑏, 𝑗 ) and returns the value of the 𝑗 th register in the bucket 𝑏 . The poke operation takes a rank value 𝑟 , a bucketwise index pair (𝑏, 𝑗 ), and sets the 𝑗 th register in the bucket 𝑏 to 𝑟 . The insert operation takes an element 𝑦 ∈ 𝒰 and updates the sketch with 𝑦 . The merge operation takes two sketches and merges them into one sketch. The operations are described in pseudocode in Appendix G.          

> 3When 𝑛 =0, the Huffman tree is not well defined because Pr [𝑅 𝑗 >0]=0, but applying Huffman’s algorithm anyway yields a simple comb, corresponding to an unary encoding of 𝑟 .

43.2.1 Peek (lookup register value) 

Registers are indexed by a bucketwise index pair (𝑏, 𝑗 ) ∈ [ 𝑚 /𝐵 ] × [ 𝐵 ] ≃ [ 𝑚 ]. To obtain the value of the register, without making any assumptions on 𝑛 and 𝑚 , we need to locate the codeword in the array, and decode it using the Huffman tree. Locating the start and the length of the codeword using the unary array takes 𝑂 (𝐵 ) = 𝑂 (log 𝑛 ) time, and decoding the codeword takes 𝑂 (log 𝑛 ) time, as we may need to traverse the entire Huffman tree in the worst case. Thus the peek operation can take 𝑂 (log 𝑛 ) time in the worst case. If we assume 𝑚 = Ω (log 2 𝑛 ), the decoding becomes a constant-time lookup. Furthermore, if we assume that the word length of the machine is Ω(log 𝑛 ), we can locate the codeword and its length in constant time, therefore making the peek operation run in 𝑂 (1) time. 

3.2.2 Poke (set register value) 

Given a new rank value 𝑟 to be set in the 𝑗 th register of the bucket 𝑏 , we need to encode 𝑟 into a codeword and replace the old codeword with the new codeword. This may require us to shift the right-hand side of the bucket to accommodate the new codeword if it differs in length. Without any assumptions on 𝑛 

and 𝑚 , the encoding and shifting take 𝑂 (log 𝑛 ) time. However, if we assume 𝑚 = Ω (log 2 𝑛 ) and that we can modify Ω(log 𝑛 ) bits in constant time, then encoding, and shifting operations become 𝑂 (1).

3.2.3 Insert (update sketch with a new element) 

Given an element 𝑦 ∈ 𝒰 , use three distinct hash functions ℎ𝑏 : 𝒰 → [ 𝑚 /𝐵 ], ℎ𝑗 : 𝒰 → [ 𝐵 ], and 

ℎ𝑟 : 𝒰 → [ 2𝑤 ] (or one hash function and select an appropriate number of independent bits) to compute the bucket index 𝑏 = ℎ𝑏 (𝑦 ) ∈ [ 𝑚 /𝐵 ], the register index 𝑗 = ℎ𝑗 (𝑦 ) ∈ [ 𝐵 ], and the rank 𝑟 = 𝜌 (ℎ𝑟 (𝑦 )) ∈ [ 𝑤 ].If 𝑟 ≤ 𝑟 min for the bucket 𝑏 , the update becomes a nop. Otherwise, we need to peek the old register value 𝑟 old . If 𝑟 old ≥ 𝑟 , this again becomes a nop. An actual update is only triggered if 𝑟 old < 𝑟 . The update is performed by poking the register (𝑏, 𝑗 ) with the new rank 𝑟 . After the poke, if 𝑟 old = 𝑟 min , we need to set 𝑐 min ← 𝑐 min − 1, and if 𝑐 min becomes zero, we need to recompute 𝑟 min and 𝑐 min by peeking all registers in the bucket. We also need to update the local cardinality estimate ˆ𝑛 𝑏 and the global cardinality estimate ˆ𝑛 . If the global estimate changes too much from ˆ𝑛 old , we need to reconstruct the Huffman tree and re-encode all buckets. Without any assumptions about 𝑛 and 𝑚 , the peek and poke operations take 𝑂 (log 𝑛 ) time, updating 

𝑟 min takes 𝑂 (log 2 𝑛 ) and updating the estimate(s) takes 𝑂 (log 𝑛 ) time. By unimodality of the distribution (see Section 4.2), it is easy to determine the unique mode in 𝑂 (1) time, sort the probabilities in 𝑂 (log 𝑛 )

time, and construct the Huffman tree in 𝑂 (log 𝑛 ) time. Re-encoding all buckets takes 𝑂 (𝑚 log 𝑛 ) time. Worst-case running time is thus 𝑂 (𝑚 log 𝑛 + log 2 𝑛 ). We will later show in Theorem 8 that this leads to an amortized running time of 𝑂 (1) per insertion over the entire stream, assuming 𝑚 = 𝑂 (𝑛 /log 3 𝑛 ). The intuitive reason for the bound on 𝑚 is that if 𝑚 is too large, the sketch becomes too sparse, as almost all registers are zero; in this regime, it would make more sense to use linear counting anyway [WZT90]. If we assume 𝑚 = Ω (log 2 𝑛 ) and that we can modify Ω(log 𝑛 ) bits in constant time, then peek, poke, and estimate-update 4 become 𝑂 (1), and updating 𝑟 min becomes 𝑂 (log 𝑛 ). Reconstructing the Huffman tree remains 𝑂 (log 𝑛 ), but re-encoding the codewords becomes only 𝑂 (𝑚 ). The best case is thus 𝑂 (1)

and worst case is 𝑂 (𝑚 ), with the Huffman tree construction subsumed the re-encoding of all buckets. In this regime, as shown in Theorem 9 we also have an amortized running time of 𝑂 (1) per insertion over the entire stream, but we can easily lighten the assumption on 𝑚 to assuming 𝑚 = 𝑂 (𝑛 /log 2 𝑛 ). So, the algorithm works best in the regime where 𝑚 is sufficiently larger than the hash values, but substantially smaller than 𝑛 to avoid sparsity, which corresponds to the real-world regime. 

3.2.4 Merge (combine two sketches into one sketch) 

Mergeability in itself is inherited directly from HLL, as the sketches are lossless compressions of HLL sketches. For a merge operation, we need to necessarily decode all register values, but we can potentially reuse the Huffman tree, and we may not need to re-encode all values if the new estimate is not too large. The best case scenario is when either one of the sketches has a considerably larger global estimate than the other, in which case the smaller sketch hardly contributes anything, we reuse the larger sketch’s Huffman tree, and mostly just copy codewords as-is. The worst case scenario is when the two sketches have similar 

> 4At least if we use the vanilla HLL estimator, which is just a harmonic mean times a constant.

5estimates but represent disjoint sets of elements, in which case the new estimate is approximately twice as big as the maximum of the old ones, necessitating Huffman tree reconstruction, and re-encoding of all registers. The merge operation proceeds in two passes. First, we need to decode all register values to compute the new bucketwise and global estimates, taking 𝑂 (𝑚 log 𝑛 ) time without assumptions on 𝑛 and 𝑚 . We then construct the new Huffman tree if necessary, taking 𝑂 (log 𝑛 ) time. Finally, we need to re-encode all registers with the new Huffman tree, which takes 𝑂 (𝑚 log 𝑛 ) time. If we can assume 𝑚 = Ω (log 2 𝑛 ) and that we can access Ω(log 𝑛 ) bits in constant time, the decoding becomes a constant-time lookup, and the re-estimation and re-encoding become 𝑂 (𝑚 ) time. Therefore, under these assumptions, the merge operation takes 𝑂 (𝑚 ) time, as the Huffman tree construction is subsumed by the 𝑂 (𝑚 ) time of re-encoding all registers. 

Theorem 2. The merge operation takes 𝑂 (𝑚 log 𝑛 ) time in the worst case. However, assuming 𝑚 = Ω (log 2 𝑛 )

and that Ω(log 𝑛 ) bits can be accessed in constant time, the merge operation can be implemented in 𝑂 (𝑚 ) time. Proof. The result follows directly from the description of the merge operation above. □

# 4 Analysis in the poissonized balls-and-bins model 

We now analyze the algorithm in a standard balls-and-bins model. We will be assuming a Poisson approximation of the distribution throughout this section, treating the number of balls 𝑛 as a random variable and all of the 𝑚 registers as independent and identically distributed, receiving 𝜆 = 𝑛 𝑚 balls on average. We shall refer to 𝜆 as the load factor .

## 4.1 Rank distribution 

Assuming 𝑚 registers, we shall consider the rank distribution of the fixed register 𝑗 ∈ [ 𝑚 ] after 𝑛 

insertions. Consider a matrix of bins with 𝑚 rows and infinitely many columns. We independently throw balls into the bins, such that each ball is thrown into a random row 𝑗 uniformly at random, and to column 𝑟 ≥ 1 with probability 2−𝑟 . In the Poisson model, this means that the number of balls 𝑁 𝑗,𝑟 

in the bin (𝑗, 𝑟 ) is 𝑁 𝑗,𝑟 ∼ Pois (𝜆 2−𝑟 ), independently for all 𝑗 ∈ [ 𝑚 ] and 𝑟 ≥ 1. If we consider the matrix of indicator variables 𝐼 𝑗,𝑟 = ⟦𝑁 𝑗,𝑟 > 0⟧, this corresponds to the FM85 matrix [FM85] that underlies the entire LogLog family of algorithms. The register value 𝑅 𝑗 = max 𝑟 ≥1{𝑟 | 𝑁 𝑗,𝑟 > 0} is thus the maximum 𝑟 such that 𝑁 𝑗,𝑟 > 0, or zero if there is no such 𝑟 . By the properties of the Poisson distribution, we have the following distribution for the register value: 

• Pr [𝑅 𝑗 ≤ 𝑟 ] = Î∞ 

> 𝑘 =𝑟 +1

Pr [𝑁 𝑗,𝑘 = 0] = 𝑒 −𝜆 2−𝑟 

for 𝑟 ≥ 1,

• Pr [𝑅 𝑗 = 0] = Pr [𝑅 𝑗 ≤ 0] = 𝑒 −𝜆 ,

• Pr [𝑅 𝑗 = 𝑟 ] = Pr [𝑅 𝑗 ≤ 𝑟 ] − Pr [𝑅 𝑗 ≤ 𝑟 − 1] = 𝑒 −𝜆 2−𝑟 

− 𝑒 −𝜆 2−( 𝑟 −1)

= 𝑒 −𝜆 2−𝑟 

− 𝑒 −2𝜆 2−𝑟 

= 𝑒 −𝜆 2−𝑟 

(1 − 𝑒 −𝜆 2−𝑟 

) for 

𝑟 ≥ 1,

• Pr [𝑅 𝑗 > 𝑟 ] = 1 − Pr [𝑅 𝑗 ≤ 𝑟 ] = 1 − 𝑒 −𝜆 2−𝑟 

for 𝑟 ≥ 0, and 

• Pr [𝑅 𝑗 ≥ 𝑟 ] = Pr [𝑅 𝑗 > 𝑟 − 1] = 1 − 𝑒 −𝜆 2−( 𝑟 −1)

= 1 − 𝑒 −2𝜆 2−𝑟 

for 𝑟 ≥ 1.As we make heavy use of the definition for Pr [𝑅 𝑗 = 𝑟 ], we denote this probability simply by 𝑝 𝑟 when 

𝜆 is clear from context and 𝑗 is irrelevant. It is also sometimes convenient to express the probabilities as exponential functions, so we denote 𝑥 𝑟 = 𝑥 𝑟 (𝜆 ) = 𝜆 2−𝑟 , and 𝑔 (𝑥 ) = 𝑒 −𝑥 (1 − 𝑒 −𝑥 ), so that 𝑝 𝑟 = 𝑔 (𝑥 𝑟 ) =

𝑒 −𝑥 𝑟 (1 − 𝑒 −𝑥 𝑟 ).Suppose we divide the registers into 𝑚 /𝐵 buckets of 𝐵 registers each. Then, the register values still have the same distribution. 

Proposition 3. Consider the bucket of 𝐵 registers that receives 𝑛 /( 𝑚 /𝐵 ) balls in expectation. Then, the distribution of the register values in the bucket is the same as the full sketch with 𝑛 balls and 𝑚 registers. Proof. Consider a sketch with 𝐵 registers and 𝑛 /( 𝑚 /𝐵 ) balls in expectation. By direct computation we get 𝜆 = 𝑛 /( 𝑚 /𝐵 ) 

> 𝐵

= 𝑛 𝑚 , which matches the full sketch. □

60 5 10 15 20 25 

> 𝑟
> 0.00.10.20.30.40.50.60.70.8Pr [𝑅 𝑗  ] = 𝑟

Rank probability distribution at various values of 𝜆 

> 𝜆
> 0.25 1.00 16 384 32768

Figure 1: Register value distribution for various 𝜆 , with the marker representing 𝑟 ∗ = ⌈log 2 𝜆 ⌉.

## 4.2 Unimodality and tails 

The distribution is very strongly concentrated around 𝑟 ∗ = ⌈log 2 𝜆 ⌉. Intiuitively, this makes sense: each register essentially estimates the logarithm of the number of elements in their substream, which is log 2 𝜆 

in expectation. The rank probability has sharply decaying tails, but is not symmetric. The right tail decays exponentially, whereas the left tail decays doubly exponentially, which is shown in Appendix A. In fact, this is the reason why the harmonic mean of HLL improves estimation accuracy over the arithmetic mean of LogLog: the harmonic mean penalizes the outliers in the right tail. The rank distribution is illustrated in Figure 1 for various values of 𝜆 , together with the marker representing 𝑟 ∗. The shape indicates that the distribution is unimodal. 

Proposition 4. The rank distribution is unimodal and has a mode at either 𝑟 ∗ −1, 𝑟 ∗, or 𝑟 ∗ +1, where 𝑟 ∗ = ⌈log 2 𝜆 ⌉.Proof. See Appendix B. □

## 4.3 Combined estimates 

It was already established in Proposition 3 that the buckets have the same distribution as the full sketch. This implies that the global cardinality estimate ˆ𝑛 = Í𝐵 𝑏 =1 ˆ𝑛 𝑏 is simply a more accurate estimate of the latent cardinality 𝑛 .

Proposition 5. Let ˆ𝑛 = Í𝑚 /𝐵 𝑏 =1 ˆ𝑛 𝑏 be the sum of the bucketwise estimates, each with a relative standard error of 

𝑂 (1/√𝐵 ). Then, ˆ𝑛 has relative standard error of 𝑂 (1/√𝑚 ).Proof. See Appendix C. □

## 4.4 Bucket size 

For 𝑗 ∈ [ 𝐵 ], let 𝐿 𝑗 be the length of the 𝑗 th codeword in the bucket. We shall now prove that the total size of the codeword array 𝐿 = Í𝐵 𝑗 =1 𝐿 𝑗 in each bucket is 𝑂 (𝐵 ) = 𝑂 (log 𝑛 ) bits with high probability. The idea is to make the observation that (i) the entropy of the register value distribution is 𝑂 (1) bits, (ii) by optimality of the Huffman code, this implies that the expected length of a codeword is 𝑂 (1) bits, and then (iii) use a known upper bound on the worst-case length of a Huffman codeword together with the Chernoff bound to show that 𝐿 = 𝑂 (log 𝑛 ) bits with high probability. 7Proposition 6. Let 𝐵 = 𝛼 log 𝑛 for some constant 𝛼 > 0. Then there exist constants Γ, 𝛾 > 0, independent of 𝑛 

and 𝑚 , such that for all 𝑡 ≥ 0, Pr [𝐿 ≥ Γ log 𝑛 + 𝑡 ] ≤ 𝑒 −𝛾 𝑡 .Proof. See Appendix D. □

## 4.5 Number of Huffman tree reconstructions 

We will now show that the number of Huffman tree reconstructions over 𝑛 insertions is 𝑂 (log 𝑛 ).The intuition behind the result is that is unimodal per Proposition 4, and the tails are very steep per Appendix A, so the distribution is very rigid in the tails and there can be no changes to the Huffman tree in the parts corresponding to the tails, as the tree collapses into a chain or a “comb” structure with a predefined merge order. Moreover, since the left tail decays much faster than the right tail, we can partition the distribution into three parts: the left tail, a constant-width band around the mode, and the right tail. The left tail must collapse into a comb before the right tail has any effect on the tree; and the right tail must collapse into a comb before the band has any effect on the tree, so all nontrivial changes to the tree must be in the band. As the band is determined by the mode, and the mode approximates the base-2 logarithm of 𝑛 , this indicates that there are nontrivial changes roughly when 𝜆 crosses a power of two, shifting the distribution to the right. This can only change 𝑂 (log 𝑛 ) times, so the number of changes to the Huffman tree is 𝑂 (log 𝑛 ).

Proposition 7. Let 𝜆 𝑛 = 𝑛 𝑚 for some fixed 𝑚 , and 𝑁 an upper bound on 𝑛 . The total number of tree changes as 𝜆 

varies over 𝜆 1 , 𝜆 2 , . . . , 𝜆 𝑁 is 𝑂 (log 𝑁 ).Proof. See Appendix E. □

## 4.6 Amortized updates 

We will now analyze the amortized update costs. There are three kinds of updates: (i) ordinary updates, (ii) minimum-rank updates, and (iii) tree updates. For a total of 𝑁 updates, the number of tree updates was bounded to be at most 𝑂 (log 𝑁 ) in Section 4.5, so it remains to bound the number of ordinary updates and minimum-rank updates. The intuition is that there are not too many such updates if 𝜆 is sufficiently large. This is because once the sketch becomes “saturated” enough, almost no new element can trigger an update, as the rank distribution is geometrically decreasing. Conversely, if 𝑚 is close to 𝑛 , the sketch is very sparse and most registers are zero, meaning we are better off using linear counting [WZT90] anyway. 

Theorem 8. Assuming an ordinary update takes 𝑂 (log 𝑛 ) time, a minimum-rank update takes 𝑂 (log 2 𝑛 ) time, and a Huffman tree update followed by re-encoding of all codewords takes 𝑂 (𝑚 log 𝑛 ) time, and furthermore assuming that 𝑚 = 𝑂 (𝑛 /log 3 𝑛 ), the amortized cost of updates is O(1) over 𝑛 updates. Proof. See Appendix F. □

Theorem 9. Assuming that we can access Ω(log 𝑛 ) bits in constant time and 𝑚 = Ω (log 2 𝑛 ), so we can maintain a Huffman codebook, so we can assume an ordinary update takes 𝑂 (1) time, a minimum-rank update takes 𝑂 (log 𝑛 )

time, and a Huffman tree update followed by re-encoding of all codewords takes 𝑂 (𝑚 ) time, the amortized cost of updates is O(1) over 𝑛 updates if we further assume 𝑚 = 𝑂 (𝑛 /log 2 𝑛 ).Proof. See Appendix F. □

# 5 Generalizations 

So far we have focused the analysis of the HBS sketch specifically on the lossless compression of HLL registers. However, the framework admits various generalizations, which we briefly outline here, without analyzing them in detail. Perhaps the most immediate generalization is to change the basis of the rank or 𝜌 function. Instead of looking at the location of the first 1-bit in the binary representation of the hash value, we could look at the location of the first nonzero digit in an arbitrary base-𝑏 representation of the hash value. This is not unlike the directions described in [Ert24, PW21]. Combining such an alternative rank function works directly with the HBS framework, as the compression is agnostic to the actual symbols being compressed, 80 200 400 600 800 1000         

> Registers in bucket 𝐵
> 0500 1000 1500 2000 2500 3000
> Total codeword length  𝐿  (bits)
> Bucket bit size as function of number of registers in bucket 2−12429214 219 224 229
> 𝜆
> 0200 400 600 800 1000
> Size of the bucket array (bits)
> Bucket array size in bits for fixed number of registers as function of 𝜆
> 𝐵
> 10 144 313

(a) (b) Figure 2: (a) The bit size of the bucket array or total codeword size 𝐿 as function of the number of registers in the bucket 𝐵 . The solid line represents the mean of one million repetitions, and the dashed lines the minimum and maximum sizes. (b) The size of a bucket with a fixed number of registers 𝐵 as a function of the load factor 𝜆 over one million random sketches. The marker represents the mean, the error bars represent the minimum and maximum sizes. and it should be possible to obtain similar guarantees on the bucket behavior. The rank function could even be something completely different, as long as it has a similar tail behavior. Secondly, the current analysis works on the Huffman code, but there is no theoretical reason to stick to it, apart from its simplicity, ease of implementation, and ease of analysis. We could consider other schemes such as arithmetic coding, provided they can be updated as efficiently, and it can be shown that the codewords need not be updated too frequently. Thirdly, we need not restrict ourselves to the HLL sketch and could consider applying the same framework to other sketches, using the extra information contained in the FM85 matrix [PW21], like, for example, in the ULL [Ert24] or ExaLogLog [Ert25]. Lastly, it should be possible to apply the framework to other problems as well, such as compressing the Count-Min sketch [CM05], which estimates the frequencies of individual elements in the data stream, maintaining an array of integer counters using hash functions. The key complication for the Count-Min sketch would be that there would have to be some kind of model for the distribution of counter values, as we cannot abstract the data-dependence away like in the case of HLL. However, it might be possible to treat, for example, the rows of the CMS as buckets to be compressed with mild assumptions on the data, or alternatively learn the distribution together with the data. However, we leave this as an open question for now. 

# 6 Practical implications 

## 6.1 Implementation concerns 

The HBS sketch was designed with practical implementation in mind. The idea of bucketing arose from the idea that the buckets should be small enough to be compressed either into single machine words or a single cache line. The analysis of Section 4 is of limited value for a practical implementation, as the world is not asymptotic and constants matter. This leads to, for example, that we cannot really use the idea of a unary index into the array. However, log 𝑛 is a constant for all intents and purposes for all realistic values of 𝑛 , as, in practice, we use 64-bit hash functions. This makes accesses constant time in that they do not really depend on the number of registers or the cardinality. If we wanted to improve the access time, we could provide a within-bucket index of the bit locations of ⌈log 2 𝐵 ⌉ − 1 equally-spaced registers, reducing the need from decoding Θ(𝐵 ) registers to Θ(𝐵 /log 𝐵 ) registers, which is a significant improvement in practice, as 𝐵 is typically small. At the same time, it is not hard to see that the size of the sketch is dominated by the codewords of the buckets, so, for example, a codebook for the Huffman code is minuscule in comparison. Figure 2a shows the size of the total codeword length 𝐿 of a bucket as a function the number of 9Table 1: Memory usage and Memory-Variance Product (MVP) of the sketch at 𝑚 = 215 for different bit budgets. Bit budget 𝐵 Total small sketch size Total big sketch size MVP (small) MVP (big) 64 bits 10 242679 bits 310800 bits 7.961 10.196 512 bits 144 119657 bits 143111 bits 3.926 4.695 1024 bits 313 108311 bits 125784 bits 3.553 4.127 registers in the bucket 𝐵 . The values were computed by drawing one million random sketches for each 𝐵 , with 𝑛 = 230 , 𝑚 = 215 , meaning 𝜆 = 215 , and encoding the buckets with the Huffman code. The solid line shows the mean and the dotted lines the minimum and maximum sizes. As the analysis of Section 4 suggests, the size grows linearly in 𝐵 , as do the minimum and maximum, meaning the size of the codeword array is determined by the number of registers in a bucket. The analysis also indicates that, due to the shape of the distribution as in Figure 1, as the code is determined by a constant-width band around the peak around 𝑟 ∗ = ⌈log 2 𝜆 ⌉, the bit size of the bucket is independent of 𝜆 , assuming 𝜆 is sufficiently large that the left tail can emerge. If the left tail does not emerge, then the size of the bucket is only smaller. Thus, as 𝜆 → ∞ , the size of the bucket is asymptotically constant, depending only on 

𝐵 . Figure 2b shows this behavior with various values of 𝜆 for three different values of 𝐵 . The marker represents the mean, and the error bars represent the minimum and maximum sizes. Thus, what one would do in practice, is choose a bit budget 𝑏 𝐵 for the codeword array, and then choose 𝐵 such that there is sufficient slack that it is improbable for any bucket to overflow. We provide a rudimentary analysis here for the arbitrary case of 𝑛 = 230 and 𝑚 = 215 with values drawn from the random sketches of Figure 2a, without attempting to rigorously optimize the parameters, but just to get a sense of the practical implications. We consider three such budgets: 64 bits, 512 bits, and 1024 bits, corresponding to one machine word, one cache line, and two cache lines, respectively. If we choose suitable values of 𝐵 based on the data from Figure 2a, we get values 10, 144, and 313, respectively. In addition to this array for each bucket, we need to store the minimum rank 𝑟 min and the count of minimum-rank registers 𝑐 min for each bucket, which take 6 and ⌈log 2 𝐵 ⌉ bits, respectively. We also need to store the global estimates ˆ𝑛 and ˆ𝑛 old , and the Huffman tree. Unlike the theoretical description, we do not want to store local estimates because the 64 bits required for that would be too much, but this is fine if we use the vanilla estimator [FFGM07] for the global ˆ𝑛 , as it is easily updated in constant time because it is simply a harmonic mean. The Huffman tree takes only 127 bits if we just store the indicator bits for the leaves and the internal nodes. Since there are  𝑚 𝐵 

 buckets, the total size of the sketch is 

 𝑚 𝐵 

 · ( 𝑏 𝐵 + 6 + ⌈ log 2 𝐵 ⌉) + 127 bits. The total sizes are shown in Table 1, under the small sketch column. It is obvious that the total size decreases as the bit budget increases, representing a tradeoff between the size of the sketch and the update times. For more streamlined updates, we could store the index into a logarithmic number of registers within a bucket, requiring  ⌈log 2 𝐵 ⌉ − 1 · ⌈ log 2 𝑛 𝐵 ⌉ extra bits per bucket, and store the Huffman code as a pair of lookup tables. The encoding table would require (63 + 6) · 64 = 4416 bits, as we need to store the codeword and its length for each possible value 𝑟 , and the decoding table would be a hash table of size at least (63 + 6 + 6) · 64 = 4800 bits, as we need to store the codeword and its length as a key, and also the value 𝑟 as a value. The total sketch sizes are given in Table 1 under the big sketch column. As a final observation, it is possible to further reduce the size of the sketch in practice by observing that we are using a lot of slack here, and there are only very few buckets that actually need the bit budget we are using. Intuitively, the overflows occur when (usually an individual) surprisingly large rank value ends up in a bucket, requiring a very long codeword. It might be possible to use an adaptive scheme where most buckets are stored with fewer bits, and we allocate a handful of big buckets with larger sizes that can accommodate the small number of overflowing buckets. 

## 6.2 Memory-Variance Product 

The behavior of sketches such as HLL is often measured in terms of the Memory-Variance Product (MVP) [PW21], which is the product of the memory usage and the relative variance of the estimator 

Var  ˆ𝑛 𝑛 

. The analysis of Section 4 is asymptotic and ignores constants, so it does not directly give us a meaningful result, apart from showing that the MVP is necessarily 𝑂 (1). However, we can provide some rudimentary evidence that the algorithm is practical in these terms by modeling its behavior numerically. As we are not analyzing the estimators in this paper, we shall assume the relative variance is bounded 10 by 1.075 /𝑚 [WP23]. As we observed in Section 6.1, the size of the bucket is determined solely by the choice of 𝐵 , independent of 𝜆 , and the size of the sketch is dominated by the codeword array, so asymptotically, the sketch size is 𝑂 (𝑚 ) and we can compute the constants numerically by fixing the bit budget for the bucket. This enables us to compute the MVP for different bit budgets, as shown in Table 1. Comparing this to ExaLogLog [Ert25], even the simplistic scheme presented here without fully optimizing parameters and implementation details, we can achieve an MVP that is comparable to the MVP of 3.67 of ExaLogLog without using the extra information of the FM85 matrix and with the possibility of applying our framework on the framework of [Ert25]. It should be noted that this is not a rigorous or complete analysis but a back-of-the-envelope calcula-tion, giving confidence that the sketch is practical in terms of the MVP. A more rigorous analysis of the exact memory usage and the MVP is left for future work. 

# Acknowledgements 

I want to thank Rasmus Pagh and Martin Aumüller for helpful discussions and comments. I also want to thank Tamás Sarlós for good ideas and the wonderful analogy of pulling ourselves up from the swamp by our hair, like Baron von Münchhausen. Lastly, I want to thank my master students Labiba Karar Eshaba and Yasamin Fazelidehkordi, as some of the ideas leading to this paper originated from our discussions on their master thesis project on a related but distinct approach to cardinality estimation. 

# References 

[AMM00] Yaser S. Abu-Mostafa and Robert J. McEliece. Maximal codeword lengths in Huffman codes. 

Computers & Mathematics with Applications , 39(11):129–134, 2000. [AMS99] Noga Alon, Yossi Matias, and Mario Szegedy. The space complexity of approximating the frequency moments. J. Comput. Syst. Sci. , 58(1):137–147, 1999. [BEF +18] Ran Ben-Basat, Gil Einziger, Shir Landau Feibish, Jalil Moraney, and Danny Raz. Network-wide routing-oblivious heavy hitters. In Proceedings of the 2018 Symposium on Architectures for Networking and Communications Systems (ANCS 2018) , pages 66–73, 2018. [BL19] Daniel N. Baker and Ben Langmead. Dashing: fast and accurate genomic distances with hyperloglog. Genome Biology , 20(1):265, 2019. [Bla20] Jaroslaw Blasiok. Optimal streaming and tracking distinct elements with high probability. 

ACM Trans. Algorithms , 16(1):3:1–3:28, 2020. [CCD14] Yousra Chabchoub, Raja Chiky, and Betul Dogan. How can sliding hyperloglog and EWMA detect port scan attacks in IP traffic? EURASIP J. Inf. Secur. , 2014:5, 2014. [CDP +19] Pern Hui Chia, Damien Desfontaines, Irippuge Milinda Perera, Daniel Simmons-Marengo, Chao Li, Wei-Yen Day, Qiushi Wang, and Miguel Guevara. KHyperLogLog: Estimating reidentifiability and joinability of large data at scale. In Proceedings of the 2019 IEEE Symposium on Security and Privacy (SP 2019) , pages 350–364, 2019. [CM05] Graham Cormode and S. Muthukrishnan. An improved data stream summary: the count-min sketch and its applications. J. Algorithms , 55(1):58–75, 2005. [CSGH23] Vera Clemens, Lars-Christian Schulz, Marten Gartner, and David Hausheer. Ddos detection in P4 using HYPERLOGLOG and COUNTMIN sketches. In Proceedings of the IEEE/IFIP Network Operations and Management Symposium (NOMS 2023) , pages 1–6, 2023. [CT05] Thomas M. Cover and Joy A. Thomas. Elements of Information Theory . John Wiley & Sons, Inc., Hoboken, NJ, USA, 2nd edition, 2005. [DF03] Marianne Durand and Philippe Flajolet. Loglog counting of large cardinalities (extended abstract). In Proceedings of the 11th Annual European Symposium onAlgorithms (ESA 2003) ,volume 2832 of Lecture Notes in Computer Science , pages 605–617, 2003. 11 [DHS +23] Yang Du, He Huang, Yu-e Sun, Kejian Li, Boyu Zhang, and Guoju Gao. A better cardinality estimator with fewer bits, constant update time, and mergeability. In Proceedings of the IEEE Conference on Computer Communications (INFOCOM 2023) , pages 1–10, 2023. [DJJ +22] Dinhnguyen Dao, Rhongho Jang, Changhun Jung, David Mohaisen, and DaeHun Nyang. Minimizing noise in HyperLogLog-based spread estimation of multiple flows. In Proceedings of the 52nd Annual IEEE/IFIP International Conference on Dependable Systems and Networks (DSN 2022) , pages 331–342, 2022. [Dur04] Marianne Durand. Combinatoire analytique et algorithmique des ensembles de données. (Multi-variate holonomy, applications in combinatories, and analysis of algorithms) . PhD thesis, École Polytechnique, Palaiseau, France, 2004. [Ert17] Otmar Ertl. New cardinality estimation algorithms for HyperLogLog sketches. CoRR ,abs/1702.01284, 2017. [Ert24] Otmar Ertl. UltraLogLog: A practical and more space-efficient alternative to HyperLogLog for approximate distinct counting. Proc. VLDB Endow. , 17(7):1655–1668, 2024. [Ert25] Otmar Ertl. ExaLogLog: Space-efficient and practical approximate distinct counting up to the exa-scale. In Proceedings of the 28th International Conference on Extending Database Technology (EDBT 2025) , pages 829–841, 2025. [FFGM07] Philippe Flajolet, Éric Fusy, Olivier Gandouet, and Frédéric Meunier. Hyperloglog: the analysis of a near-optimal cardinality estimation algorithm. In Proc. AofA 2007 , 2007. [FM85] Philippe Flajolet and G. Nigel Martin. Probabilistic counting algorithms for data base appli-cations. J. Comput. Syst. Sci. , 31(2):182–209, 1985. [FN19] Michael J. Freitag and Thomas Neumann. Every row counts: Combining sketches and sampling for accurate group-by result estimates. In Proceedings of the 9th Biennial Conference on Innovative Data Systems Research (CIDR 2019) , 2019. [HNH13] Stefan Heule, Marc Nunkesser, and Alexander Hall. Hyperloglog in practice: algorithmic engineering of a state of the art cardinality estimation algorithm. In Proceedings of the Joint 2013 EDBT/ICDT Conferences (EDBT 2013) , pages 683–692, 2013. [Huf52] David A. Huffman. A method for the construction of minimum-redundancy codes. Proceed-ings of the IRE , 40(9):1098–1101, 1952. [IW03] Piotr Indyk and David P. Woodruff. Tight lower bounds for the distinct elements problem. In Proceedings of the 44th Symposium on Foundations of Computer Science (FOCS 2003) , pages 283–288, 2003. [KNW10] Daniel M. Kane, Jelani Nelson, and David P. Woodruff. An optimal algorithm for the dis-tinct elements problem. In Proceedings of the Twenty-Ninth ACM SIGMOD-SIGACT-SIGART Symposium on Principles of Database Systems (PODS 2010) , pages 41–52, 2010. [KP22] Matti Karppa and Rasmus Pagh. HyperLogLogLog: Cardinality estimation with one log more. In Proceedings of The 28th ACM SIGKDD Conference on Knowledge Discovery and Data Mining (KDD 2022) , pages 753–761, 2022. [Lan17] Kevin J. Lang. Back to the future: an even more nearly optimal cardinality estimation algorithm. CoRR , abs/1708.06839, 2017. [MSPK19] Guillaume Marçais, Brad Solomon, Rob Patro, and Carl Kingsford. Sketching and sublinear data structures in genomics. Annual Review of Biomedical Data Science , 2:93–118, 2019. [MU05] Michael Mitzenmacher and Eli Upfal. Probability and Computing: Randomized Algorithms and Probabilistic Analysis . Cambridge University Press, New York, NY, USA, 2005. [PCT23] Christina Pavlopoulou, Michael J. Carey, and Vassilis J. Tsotras. Revisiting runtime dynamic optimization for join queries in big data management systems. SIGMOD Rec. , 52(1):104–113, 2023. 12 [PW21] Seth Pettie and Dingyu Wang. Information theoretic limits of cardinality estimation: Fisher meets shannon. In Proceedings of the 53rd Annual ACM SIGACT Symposium on Theory of Computing (STOC 2021) , pages 556–569, 2021. [PWY21] Seth Pettie, Dingyu Wang, and Longhui Yin. Non-mergeable sketching for cardinality estima-tion. In Proceedings of the 48th International Colloquium on Automata, Languages, and Programming (ICALP 2021) , pages 104:1–104:20, 2021. [Tru19] Immanuel Trummer. Exact cardinality query optimization with bounded execution cost. In 

Proceedings of the 2019 International Conference on Management of Data (SIGMOD 2019) , pages 2–17, 2019. [vL76] Jan van Leeuwen. On the construction of huffman trees. In Proceedings of the Third International Colloquium on Automata, Languages and Programming (ICALP 1976) , pages 382–410, 1976. [WCLL21] Jiayi Wang, Chengliang Chai, Jiabin Liu, and Guoliang Li. FACE: A normalizing flow based cardinality estimator. Proc. VLDB Endow. , 15(1):72–84, 2021. [Woo04] David P. Woodruff. Optimal space lower bounds for all frequency moments. In Proceedings of the Fifteenth Annual ACM-SIAM Symposium on Discrete Algorithms (SODA 2004) , pages 167–175, 2004. [WP23] Dingyu Wang and Seth Pettie. Better cardinality estimators for hyperloglog, pcsa, and beyond. In Proceedings of the 42nd ACM SIGMOD-SIGACT-SIGAI Symposium on Principles of Database Systems (PODS 2023) , pages 317–327, 2023. [WZT90] Kyu-Young Whang, Brad T. Vander Zanden, and Howard M. Taylor. A linear-time probabilis-tic counting algorithm for database applications. ACM Trans. Database Syst. , 15(2):208–229, 1990. [XCZL20] Qingjun Xiao, Shigang Chen, You Zhou, and Junzhou Luo. Estimating cardinality for arbitrar-ily large data stream with improved memory efficiency. IEEE/ACM Trans. Netw. , 28(2):433–446, 2020. 13 Appendix 

# A Tail bounds for the distribution 

We shall prove the following bounds on the tails of the distribution, without claiming originality for the results. 

Lemma 10. Let 𝑟 ∗ = ⌈log 2 𝜆 ⌉. For any integer 𝑘 ≥ 0, we have Pr [𝑅 𝑗 > 𝑟 ∗ + 𝑘 ] ≤ 2−𝑘 .Proof. Let 𝜃 = 𝜆 2−𝑟 ∗

≤ 1. Thus, Pr [𝑅 𝑗 > 𝑟 ∗ + 𝑘 ] = 1 − 𝑒 −𝜃 2−𝑘 

≤ 𝜃 2−𝑘 ≤ 2−𝑘 . □

Lemma 11. Assume 𝜆 > 1, and let 𝑟 ∗ = ⌈log 2 𝜆 ⌉. For any integer 𝑘 ≥ 0, we have Pr [𝑅 𝑗 ≤ 𝑟 ∗ − 𝑘 ] ≤ 𝑒 −2𝑘 −1

.Proof. Let 𝜃 = 𝜆 2−𝑟 ∗

. We have 𝜃 2𝑘 ≥ 2𝑘 −1 because 𝜆 > 1 and 𝑟 ∗ ≥ 1. Thus, Pr [𝑅 𝑗 ≤ 𝑟 ∗ − 𝑘 ] = 𝑒 −𝜃 2𝑘 

≤

𝑒 −2𝑘 −1

. □

# B Proof of Proposition 4 

Let us start by considering the continuous analog of the distribution by replacing 𝑟 with a continuous variable 𝑥 . Define 𝑔 : R → R such that 𝑔 (𝑥 ) = 𝑒 −𝑥 (1 − 𝑒 −𝑥 ).

Lemma 12. The function 𝑔 is unimodal, with a unique maximum at 𝑥 = ln 2 .Proof. We have 𝑔 ′(𝑥 ) = 𝑒 −2𝑥 (2 − 𝑒 𝑥 ). The only critical point is at 𝑥 = ln 2 , which is a maximum, since 

𝑔 ′(𝑥 ) > 0 for 𝑥 < ln 2 and 𝑔 ′(𝑥 ) < 0 for 𝑥 > ln 2 . □

Let us consider the special case where the mode is at 𝑟 = 0 and the entire sequence is strictly decreasing. 

Lemma 13. If 𝑥 1 = 𝜆  

> 2

≤ ln 2 , then the mode of the distribution is at 𝑟 = 0, and the entire sequence 𝑝 𝑟 is strictly decreasing. Proof. Consider the case 𝑝 0 ≥ 𝑝 1 > 𝑝 2, that is, the mode is at 𝑟 = 0, or jointly at 𝑟 = 0 and 𝑟 = 1. This can happen only if 𝑥 1 = 𝜆  

> 2

≤ ln 2 , that is, 𝜆 ≤ 2 ln 2 . As 𝜆 ≤ 2 ln 2 , we have 𝑝 0 = 𝑒 −𝜆 ≥ 𝑒 −2 ln 2 = 14 , and 

𝑝 1 = 𝑒 −𝜆 /2(1 − 𝑒 −𝜆 /2) ≤ 14 , so this condition guarantees that the mode is at 𝑟 = 0, and the sequence is strictly decreasing after 𝑝 1. □

We can therefore now assume that 𝑝 1 > 𝑝 0 and the mode is at 𝑟 ≥ 1. Let us recall the statement of Proposition 4 and prove it. 

Proposition 4. The rank distribution is unimodal and has a mode at either 𝑟 ∗ −1, 𝑟 ∗, or 𝑟 ∗ +1, where 𝑟 ∗ = ⌈log 2 𝜆 ⌉.Proof. Let 𝑟 ′ be the least integer, such that 𝑥 𝑟 ′ ≤ ln 2 , the maximum of 𝑔 . This is the “crossing index” where the distribution transitions from increasing to decreasing. By the properties of 𝑔 , we have that 𝑝 𝑟 

is strictly increasing when 𝑟 < 𝑟 ′, and strictly decreasing when 𝑟 > 𝑟 ′. There is a potential special case if 

𝑥 𝑟 = ln 2 , as the sequence is then decreasing until 𝑥 𝑟 , but regardless of this, the sequence is nonincreasing for 𝑟 ≥ 𝑟 ′. Therefore, the mode is either at 𝑟 ′ or at 𝑟 ′ − 1, whichever leads 𝑥 𝑟 to be closer to ln 2 .By the definition of 𝑟 ∗ = ⌈log 2 𝜆 ⌉, we have 𝑥 𝑟 ∗ = 𝜆 2−𝑟 ∗

≤ 1. Furthermore, 𝑥 𝑟 ∗−1 = 𝜆 2−( 𝑟 ∗−1) = 2 𝜆  

> 2𝑟 ∗

,and 𝑥 𝑟 ∗+1 = 𝜆 2−( 𝑟 ∗+1) = 12 

> 𝜆
> 2𝑟 ∗

. Therefore, 12 < 𝑥 𝑟 ∗ ≤ 1, 1 < 𝑥 𝑟 ∗−1 ≤ 2, and 14 < 𝑥 𝑟 ∗+1 ≤ 12 . Furthermore,  

> 12

< ln 2 < 1, so the crossing index must satisfy 𝑟 ∗ ≤ 𝑟 ′ ≤ 𝑟 ∗ + 1. The mode must satisfy that it is either at 

𝑟 ′ or 𝑟 ′ − 1. Putting this together, we have that the mode is either at 𝑟 ∗ − 1, 𝑟 ∗, or 𝑟 ∗ + 1. The result is thus proven with the special case of mode at zero handled by Lemma 13. □

14 C Proof of Proposition 5 

Let us recall the statement of Proposition 5 and prove it. 

Proposition 5. Let ˆ𝑛 = Í𝑚 /𝐵 𝑏 =1 ˆ𝑛 𝑏 be the sum of the bucketwise estimates, each with a relative standard error of 

𝑂 (1/√𝐵 ). Then, ˆ𝑛 has relative standard error of 𝑂 (1/√𝑚 ).Proof. We already established in Proposition 3 that the distribution of the register values in a bucket is the same as the distribution of the register values in the full sketch. This means that the global cardinality estimate ˆ𝑛 = Í𝐵 𝑏 =1 ˆ𝑛 𝑏 is simply a more accurate estimate of the latent cardinality 𝑛 . By the properties of the vanilla HLL estimator [FFGM07], each ˆ𝑛 𝑏 has a relative standard error bounded by 𝑂 (1/√𝐵 ). Since we have an expected 𝑛 /( 𝑚 /𝐵 ) elements in the bucket, this implies that 

pVar [ ˆ 𝑛 𝑏 ]

> 𝑛
> 𝑚 𝐵

=

pVar [ ˆ 𝑛 𝑏 ]

> 𝑛𝐵 𝑚

=

pVar [ ˆ 𝑛 𝑏 ]

𝑛 · 𝑚 𝐵 ≤ 𝐾 

√𝐵 

for some constant 𝐾 (1.04 according to [FFGM07]). Solving for Var [ ˆ 𝑛 𝑏 ] gives Var [ ˆ 𝑛 𝑏 ] ≤ 𝐾 2 𝑛 2 𝐵 𝑚 2 . Assuming independence of the bucketwise estimates, we have Var [ ˆ 𝑛 ] = Í𝑚 /𝐵 𝑏 =1 Var [ ˆ 𝑛 𝑏 ] ≤ 𝐾 2 𝑛 2 𝐵 𝑚 2 · 𝑚 𝐵 = 𝐾 2 𝑛 2 

> 𝑚

. Thus, the relative standard error of ˆ𝑛 is 

√Var [ ˆ 𝑛 ] 

> 𝑛

≤ 𝐾 √𝑚 . □

# D Proof of Proposition 6 

The proof proceeds in two parts. First, we show that the entropy of the register values is 𝑂 (1). This implies that the expected codeword length of the Huffman code is also 𝑂 (1), from which we can bound the total size of the bucket array by 𝑂 (log 𝑛 ), as claimed. 

## D.1 Entropy 

We start by proving that the entropy of the register values is 𝑂 (1). The proof is presented here for completeness, and we do not claim originality for the result, as it is well known [Dur04, PW21]. The following bound on 𝑥 𝑟 is useful for bounding log 𝑝 𝑟 .

Lemma 14. For 𝑟 ≥ 𝑟 ∗ = ⌈log 2 𝜆 ⌉, Pr [𝑅 𝑗 = 𝑟 ] = 𝑝 𝑟 satisfies 𝑝 𝑟 ≥ 𝑥 𝑟  

> 2𝑒

= 𝜆 2−𝑟  

> 2𝑒

.Proof. For 𝑟 ≥ 𝑟 ∗, we have 𝑒 −𝑥 𝑟 ≥ 1/𝑒 . Also, 1 − 𝑒 −𝑥 𝑟 ≥ 𝑥 𝑟  

> 2

. Putting these together gives 𝑝 𝑟 ≥ 𝑥 𝑟  

> 2𝑒

. □

We can now prove the entropy bound. 

Lemma 15. The entropy of the register value 𝑅 𝑗 is 𝐻 (𝑅 𝑗 ) = − Í∞ 

> 𝑟 =0

𝑝 𝑟 log 2 𝑝 𝑟 = 𝑂 (1).Proof. Consider 

𝐻 (𝑅 𝑗 ) =

> ∞

Õ

> 𝑟 =0

−𝑝 𝑟 log 2 𝑝 𝑟 = (log 2 𝑒 )

> 𝑟 ∗−1

Õ

> 𝑟 =0

−𝑝 𝑟 ln 𝑝 𝑟 +

> ∞

Õ

> 𝑟 =𝑟 ∗

−𝑝 𝑟 ln 𝑝 𝑟 

!

. (1) We need to bound the two sums of Equation (1) separately. For the right-hand tail, since 1 − 𝑒 −𝑥 𝑟 ≤ 𝑥 𝑟 ,we have 𝑝 𝑟 ≤ 𝑥 𝑟 𝑒 −𝑥 𝑟 ≤ 𝑥 𝑟 . By Lemma 14, 𝑝 𝑟 ≥ 𝑥 𝑟  

> 2𝑒

. Thus, for 𝑟 ≥ 𝑟 ∗, we have 

𝑥 𝑟 

2𝑒 ≤ 𝑝 𝑟 ≤ 𝑥 𝑟 . (2) Putting Equation (2) into the right-hand sum of Equation (1), setting 𝜃 = 𝜆 2−𝑟 ∗

≤ 1, so for 𝑟 = 𝑟 ∗ + 𝑘 

15 we have 𝑥 𝑟 = 𝜃 2−𝑘 , we get 

−

> ∞

Õ

> 𝑟 =𝑟 ∗

𝑝 𝑟 ln 𝑝 𝑟 ≤ − 

> ∞

Õ

> 𝑘 =0

𝑥 𝑟 ln 𝑥 𝑟 

2𝑒 = −

> ∞

Õ

> 𝑘 =0

𝑥 𝑟 (ln 𝑥 𝑟 − ln 2 − 1)

= −

> ∞

Õ

> 𝑘 =0

𝑥 𝑟 ln 𝑥 𝑟 +

> ∞

Õ

> 𝑘 =0

(ln 2 + 1)

= −

> ∞

Õ

> 𝑘 =0

𝜃 2−𝑘 ln 



𝜃 2−𝑘 



+

> ∞

Õ

> 𝑘 =0

𝜃 2−𝑘 (ln 2 + 1)

= −𝜃 

> ∞

Õ

> 𝑘 =0

2−𝑘 (ln 𝜃 − 𝑘 ln 2 ) + 𝜃 (ln 2 + 1)

> ∞

Õ

> 𝑘 =0

2−𝑘 

= −2𝜃 ln 𝜃 + 2𝜃 ln 2 + 2𝜃 ln 2 + 2𝜃 = −2𝜃 ln 𝜃 + 4𝜃 ln 2 + 2𝜃 .

(3) Since −𝜃 ln 𝜃 ≤ 1/𝑒 and 0 ≤ 𝜃 ≤ 1, Equation (3) is bounded by 2 

> 𝑒

+ 4 ln 2 + 2 < 6.For the left-hand tail and the left sum of Equation (1), we have 𝑝 𝑟 ≤ 𝑒 −2𝑘 −1

≤ 1/𝑒 for 𝑟 ≤ 𝑟 ∗, hence 

−𝑝 𝑟 ln 𝑝 𝑟 ≤ 2𝑘 −1 𝑒 −2𝑘 −1

. Thus, 

−

> 𝑟 ∗−1

Õ

> 𝑟 =0

𝑝 𝑟 ln 𝑝 𝑟 ≤

> ∞

Õ

> 𝑘 =1

2𝑘 −1 𝑒 −2𝑘 −1

< 1 .

Putting this together, we have 𝐻 (𝑅 𝑗 ) < (log 2 𝑒 )( 6 + 1) = 𝑂 (1). □

Remark 16. Numerically, 𝐻 (𝑅 𝑗 ) goes pretty fast to ≈ 2.83196 for fixed 𝑚 as 𝑛 → ∞ .

## D.2 Bucket size 

We shall now prove that the size of the codeword array in each bucket is 𝑂 (𝐵 ) = 𝑂 (log 𝑛 ) bits with high probability. Let 𝐿 𝑗 be the random variable for the length of the codeword for the 𝑗 th register in the bucket, and let 𝐿 = Í𝐵 𝑗 =1 𝐿 𝑗 be the total length of the codewords in the bucket. We need the following lemma on the bound of the worst-case length of 𝐿 𝑗 . Correspondingly, let 𝐿 (𝑟 ) be the length of the codeword encoding the rank value 𝑟 , that is, 𝐿 𝑗 = 𝐿 (𝑅 𝑗 ).

Lemma 17 ([AMM00], paraphrased) . Suppose 𝐿 𝑗 is the length of a Huffman codeword encoding a symbol with probability 𝑝 𝑟 . Then, 𝐿 𝑗 ≤ − 1.44041 log 2 𝑝 𝑟 in the worst case. 

We need the following lemma. 

Lemma 18. For 𝑟 ≥ 𝑟 ∗, we have 𝑝 𝑟 ∗+𝑘 ≥ 2−𝑘  

> 4𝑒

.Proof. By the choice of 𝑟 ∗, we have 12 < 𝜆  

> 2𝑟 ∗

≤ 1, so for 𝑘 ≥ 0, we have 𝑥 𝑟 ∗+𝑘 = 𝜆  

> 2𝑟 ∗+𝑘

≥ 12 2−𝑘 = 2−( 𝑘 +1).Combined with Lemma 14, we have 𝑝 𝑟 ≥ 𝑥 𝑟 ∗+𝑘  

> 2𝑒

= 𝜆 2−𝑟 ∗+𝑘  

> 2𝑒

≥ 2−( 𝑘 +1) 

> 2𝑒

= 2−𝑘  

> 4𝑒

. □

Together, Lemmata 17 and 18 imply for 𝑘 ≥ 0,

𝐿 (𝑟 ∗ + 𝑘 ) ≤ − 2 log 2 𝑝 𝑟 ∗+𝑘 ≤ − 2 log 2

2−𝑘 

4𝑒 = 2𝑘 + 2 log 2(4𝑒 ) . (4) It is well known (see, for example, [CT05, Chapter 5.7]) that Huffman codes are optimal in expectation, and E[𝐿 𝑗 ] ≤ 𝐻 (𝑅 𝑗 ) + 1. By Lemma 15 and linearity of expectation, we thus have have E[𝐿 ] = 𝑂 (log 𝑛 ).However, we also need to show a stronger statement that 𝐿 is 𝑂 (log 𝑛 ) with high probability. We will do this by applying a standard Chernoff argument. The Chernoff bound states the following: Let 

𝑋 1 , 𝑋 2 , . . . , 𝑋 𝑛 be independent and identically distributed random variables, with a shared moment-generating function 𝑀 (𝑡 ) = E[𝑒 𝑡𝑋 𝑖 ] that is finite for some 𝑡 > 0, and let 𝑋 = Í𝑛 𝑖 =1 𝑋 𝑖 . Then for any such 

𝑡 > 0, we have 

Pr [𝑋 ≥ 𝑎 ] ≤ 𝑒 −𝑡𝑎 𝑛 Ö

> 𝑖 =1

𝑀 (𝑡 ) = 𝑒 −𝑡𝑎  E[𝑒 𝑡𝑋 𝑖 ]𝑛 . (5) To apply Equation (5), we need to establish that the moment-generating function of 𝐿 𝑗 is finite. 16 Lemma 19. There exist constants 𝜏 > 0 and 𝑀 < ∞, independent of 𝑛 and 𝑚 , such that E[𝑒 𝜏 𝐿 𝑗 ] ≤ 𝑀 .Proof. Fix 0 < 𝜏 < ln 2 2 . We have 

E[𝑒 𝜏 𝐿 𝑗 ] =

> ∞

Õ

> 𝑟 =0

𝑒 𝜏 𝐿 (𝑟 ) 𝑝 𝑟 =

> 𝑟 ∗−1

Õ

> 𝑟 =0

𝑒 𝜏 𝐿 (𝑟 ) 𝑝 𝑟 +

> ∞

Õ

> 𝑟 =𝑟 ∗

𝑒 𝜏 𝐿 (𝑟 ) 𝑝 𝑟 . (6) The left-hand sum of Equation (6) is finite, so it suffices to show that the right-hand sum is finite. By Equation (4) and Lemma 18, we have 

> ∞

Õ

> 𝑟 =𝑟 ∗

𝑒 𝜏 𝐿 (𝑟 ) 𝑝 𝑟 =

> ∞

Õ

> 𝑘 =0

𝑒 𝜏 𝐿 (𝑟 ∗+𝑘 ) 𝑝 𝑟 ∗+𝑘 ≤

> ∞

Õ

> 𝑘 =0

𝑒 𝜏 (2𝑘 +2 log 2(4𝑒 )) · 2−𝑘 

4𝑒 = 𝑒 2𝜏 log 2(4𝑒 )

4𝑒 

> ∞

Õ

> 𝑘 =0

 𝑒 2𝜏 

2

 𝑘 

< ∞ ,

since by our choice of 𝜏 , we have 𝑒 2𝜏  

> 2

< 1. □

Let us recall the statement of Proposition 6 and prove it. 

Proposition 6. Let 𝐵 = 𝛼 log 𝑛 for some constant 𝛼 > 0. Then there exist constants Γ, 𝛾 > 0, independent of 𝑛 

and 𝑚 , such that for all 𝑡 ≥ 0, Pr [𝐿 ≥ Γ log 𝑛 + 𝑡 ] ≤ 𝑒 −𝛾 𝑡 .Proof. Fix 𝜏 > 0 and 𝑀 such that E[𝑒 𝜏 𝐿 𝑗 ] ≤ 𝑀 , as guaranteed by Lemma 19. By the Chernoff bound of Equation (5), we have 

Pr [𝐿 ≥ 𝑠 ] ≤ 𝑒 −𝜏 𝑠  E[𝑒 𝜏 𝐿 𝑗 ]𝐵 

Let 𝑠 = Γ log 𝑛 + 𝑡 > 0 and 𝐵 = 𝛼 log 𝑛 , so we have 

Pr [𝐿 ≥ Γ log 𝑛 + 𝑡 ] ≤ 𝑒 −𝜏 (Γ log 𝑛 +𝑡 ) 𝑀 𝛼 log 𝑛 .

Choosing Γ ≥ 𝛼 ln 𝑀 +1 

> 𝜏

yields by direct computation that 

Pr [𝐿 ≥ Γ log 𝑛 + 𝑡 ] ≤ 𝑒 −𝜏 𝑡 ,

proving the claim with 𝛾 = 𝜏 . □

# E Proof of Proposition 7 

This is a long and technical proof, so it has been broken into several subsections. The main idea is that we establish that the tails of the Huffman tree are very rigid and become comb-like very fast, with a simple predefined merge order. This implies that the only place where interesting tree changes can occur is within a narrow constant-width band around the mode. We first analyze the right tail in isolation to establish the rigidity criterion. Then, we define the constant-width band of large weights and the pool 

of small weights. The pool consists of the deep left and deep right tails. We bound the mass of the left and right tails and show that the entire left tail is dominated by every element of the right tail; similarly, the entire mass of the right tail is dominated by every element of the band. Thus, the left tail has to collapse into a single node before it can interfere with the right tail, and, likewise, the right tail has to collapse into a single node before it can interfere with the band. As a consequence of Rolle’s theorem, this implies that there can only be a constant number of tree changes, assuming 𝜆 is constrained to a constant-width interval, as the probability masses corresponding to subsets of nodes are finite-term sums of exponentials. Once we have established the constant number of possible crossover points for a constant interval of 𝜆 , the result follows by observing that there are only 𝑂 (log 𝑛 ) such intervals. 

## E.1 Basics 

Throughout this section, we assume that there exists an upper bound on the cardinality 𝑁 . For fixed 𝑚 , it is obvious that the distribution defining the Huffman tree is unambiguously determined by 𝑛 . Therefore, define 𝜆 𝑛 = 𝑛 𝑚 , 𝑥 𝑟 (𝜆 ) = 𝜆  

> 2𝑟

, and recall that 𝑝 0(𝜆 ) = 𝑒 −𝜆 and 𝑝 𝑟 (𝜆 ) = 𝑒 −𝑥 𝑟 (𝜆 )(1 − 𝑒 −𝑥 𝑟 (𝜆 )) for 𝑟 > 0. Let 𝑇 (𝜆 )

denote the Huffman tree induced by the distribution 𝑝 𝑟 (𝜆 ). We shall analyze the set of change points 

𝒞 𝑁 = {𝑖 ∈ [ 𝑁 − 1] | 𝑇 (𝜆 𝑖 ) ≠ 𝑇 (𝜆 𝑖 +1)} , that is, the set of indices where the Huffman tree changes. We aim to prove that |𝒞 𝑁 | = 𝑂 (log 𝑁 ).For 𝜆 > 0, let 𝑟 ∗(𝜆 ) = ⌈log 2 𝜆 ⌉. Define the intervals 𝐼 0 = [0, 1], and for 𝑘 ≥ 1, 𝐼 𝑘 = (2𝑘 −1 , 2𝑘 ].17 Lemma 20. If 𝑘 ≥ 1 and 𝜆 ∈ 𝐼 𝑘 , then 𝑟 ∗(𝜆 ) = 𝑘 and 2−𝑘 𝜆 ∈ ( 1/2, 1].Proof. Immediate from 2𝑘 −1 < 𝜆 ≤ 2𝑘 . □

We also define 𝑔 (𝑥 ) = 𝑒 −𝑥 (1 − 𝑒 −𝑥 ) for 𝑥 > 0. Then, 𝑝 𝑟 (𝜆 ) = 𝑔 (𝑥 𝑟 (𝜆 )) for 𝑟 ≥ 1 and 𝑝 0(𝜆 ) = 𝑒 −𝜆 .

## E.2 Right tail in isolation 

We will start by analyzing the right tail in isolation , that is, we just ignore the left tail. We show that the part of the Huffman tree corresponding to the right-hand tail is very rigid and collapses into a chain or a “comb” structure with a simple predefined merge order. The following lemma sets the criterion for the comb structure to occur. 

Lemma 21. Let 𝑤 0 ≥ 𝑤 1 ≥ · · · ≥ 𝑤 𝑀 > 0 be a sequence of weights . If for all 𝑖 ≤ 𝑀 − 2, we have 

𝑤 𝑖 ≥ 𝑤 𝑖 +1 + 𝑤 𝑖 +2 , (7) 

then the Huffman tree for the distribution defined by 𝑤 𝑖 has a comb structure with a predefined merge order. Proof. The Huffman algorithm merges the two least probable symbols (or inner nodes) at each step. By the criterion in (7), the newly formed sum is forced to be less than or equal to the next weight, so the result follows by induction. □

Definition 22. For 𝑥 > 0, define the ratio function ℎ(𝑥 ) as 

ℎ(𝑥 ) = 𝑔 (𝑥 /2)

𝑔 (𝑥 ) = 𝑒 −𝑥 /2(1 − 𝑒 −𝑥 /2)

𝑒 −𝑥 (1 − 𝑒 −𝑥 ) = 𝑒 𝑥 /2

1 + 𝑒 𝑥 /2 .

Lemma 23. For 𝑥 > 0, ℎ(𝑥 ) is strictly increasing. Proof. We have ℎ′(𝑥 ) = 𝑒 𝑥 (2+𝑒 𝑥 /2)

> 2

(1+𝑒 𝑥 /2)2 > 0. □

Let 𝑞 ∗ = 

> √5−12

≈ 0.618034 . Observe that 

𝑞 ∗ + 𝑞 2 

> ∗

= 1 . (8) Choose 𝜂 > 0 such that 

𝜂 ≤ 2 ln 

√5 − 1 +

q

6√5 − 2



− 4 ln 2 ≈ 0.285985 .

Importantly, this guarantees that ℎ(𝜂 ) ≤ 𝑞 ∗. Fix integer 𝐾 ′ 

> 𝑅

≥ 1 such that 2−𝐾 ′ 

> 𝑅

≤ 𝜂 . We can now show that the right-hand tail of the distribution is dominated by the last element before the tail, beyond the cut-off point 𝑟 ∗(𝜆 ) + 𝐾 ′

> 𝑅

.

Lemma 24. Let 𝑘 ≥ 1, 𝜆 ∈ 𝐼 𝑘 , and 𝑟 ≥ 𝑘 + 𝐾 ′

> 𝑅

. Then, 𝑝 𝑟 (𝜆 ) ≥ 𝑝 𝑟 +1(𝜆 ) + 𝑝 𝑟 +2(𝜆 ).Proof. By Lemma 20, we 𝜆 2−𝑘 ≤ 1, so for 𝑟 ≥ 𝑘 + 𝐾 ′

> 𝑅

, we have by the choice of 𝐾 ′ 

> 𝑅

that 

𝑥 𝑟 (𝜆 ) = 𝜆 2−𝑟 ≤ 2−𝐾 ′ 

> 𝑅

· 2−𝑘 · 𝜆 ≤ 2−𝐾 ′ 

> 𝑅

≤ 𝜂 .

Then, 𝑝 𝑟 +1 

> 𝑝 𝑟

= 𝑔 (𝑥 𝑟 /2)  

> 𝑔 (𝑥 𝑟 )

= ℎ(𝑥 𝑟 ) ≤ ℎ(𝜂 ) = 𝑞 ∗, and 𝑝 𝑟 +2 

> 𝑝 𝑟 +1

= 𝑔 (𝑥 𝑟 +1/2) 

> 𝑔 (𝑥 𝑟 +1)

= ℎ(𝑥 𝑟 +1) ≤ ℎ(𝜂 ) ≤ 𝑞 ∗, since by Lemma 23, 

ℎ is strictly increasing and 𝑥 𝑟 +1 ≤ 𝑥 𝑟 ≤ 𝜂 . Thus, 𝑝 𝑟 +2/𝑝 𝑟 = (𝑝 𝑟 +2/𝑝 𝑟 +1)( 𝑝 𝑟 +1/𝑝 𝑟 ) ≤ 𝑞 2 

> ∗

, so 𝑝 𝑟 +1 + 𝑝 𝑟 +2 ≤(𝑞 ∗ + 𝑞 2 

> ∗

)𝑝 𝑟 = 𝑝 𝑟 by Equation (8). □

Corollary 25. Fix 𝐾 ′ 

> 𝑅

as above. For any 𝑘 ≥ 1 and any 𝜆 ∈ 𝐼 𝑘 , the Huffman subtree induced by the indices 

𝑘 + 𝐾 ′ 

> 𝑅

, 𝑘 + 𝐾 ′ 

> 𝑅

+ 1, . . . is a comb with predefined merge order. Proof. Immediate from Lemmata 21 and 24. □

18 E.3 Band and pool 

It is immediate that the same argument applies to the left-hand tail which decays doubly-exponential-ly by Lemma 11, so the left-hand tail is even more rigid and collapses into a comb structure with a simple predefined merge order, if treated in isolation. However, since it is also possible that some of the minuscule left-tail entries are merged with some entries in the right tail, we need to construct a pool of small entries that contains entries from both tails. We will then show that the tails in the pool are rigid and cannot induce any nontrivial changes to the tree until they have completely collapsed into a single node. To do this, we truncate the infinite right tail, so we can establish a dominance criterion between the left and the right tail: the entire left tail is dominated by every element of the truncated right tail, so the left tail has to collapse into a single node before it can interfere with the right tail. Similarly, the entire truncated right tail is dominated by every element of the band, so the right tail has to collapse into a single node before it can interfere with the band. Let us assume for now that 𝜆 ∈ 𝐼 𝑘 for some 𝑘 ≥ 1, so that the mode of the distribution is at 𝑘 and we can write 𝜆 = 𝜃 2𝑘 for some 𝜃 ∈ ( 1/2, 1]. Let us choose a constant 0 < 𝛿 < ln 2 , independent of 𝜆 , and determine an integer upper bound on the ranks 𝑟 max .

Lemma 26. Fix 0 < 𝛿 < ln 2 , independent of 𝜆 , and let 𝑟 max = ⌈log 2 𝜆 𝛿 ⌉. Then, for every 𝑘 ≥ 1 and 𝜆 ∈ 𝐼 𝑘 , we have Pr [𝑅 𝑗 > 𝑟 max ] ≤ 𝛿 .Proof. We have 

Pr [𝑅 𝑗 > 𝑟 max ] =1 − 𝑒 −𝑥 𝑟 max = 1 − 𝑒 − 𝜆  

> 2𝑟 max

≤ 1 − 𝑒 − 𝜆   

> 2log 2𝜆 𝛿

= 1 − 𝑒 −𝛿 ≤ 𝛿 .

□

Lemma 27. The maximum rank 𝑟 max is at 𝑂 (1) distance from 𝑘 .Proof. We have 𝑟 max = ⌈log 2 𝜆 𝛿 ⌉ = ⌈log 2 2𝑘 𝜃 𝛿 ⌉ = 𝑘 + ⌈ log 2 𝜃 𝛿 ⌉. Therefore, log 2 12𝛿 < log 2 𝜃 𝛿 ≤ log 2 1 

> 𝛿

, so 

𝑘 + log 2 12𝛿 < 𝑟 max ≤ 𝑘 + log 2 1 

> 𝛿

. □

Lemma 26 gives us a high-probability bound on the maximum register value. Lemma 27 says that the maximum register value satisfying the bound is at a constant distance from 𝑘 . In practice, for fixed 𝑚 ,we would choose 𝛿 sufficiently small that we would not expect to see any deviations above 𝑟 max in any register. The main effect 𝛿 has is that it controls the width of the band around the mode, and what we relegate into the pool. The important thing here is that this choice does not depend on 𝜆 or 𝑛 as such, and 𝑟 max is at a constant distance from 𝑘 . Due to the doubly exponential decay, and the fact that the left tail is finite, no such truncation is needed for the left tail. We shall now formally define the band of large weights around the mode, and the pool of small weight in the tails, with respect to boundary constants 𝐾 𝑅 and 𝐾 𝐿 that are fixed later. 

Definition 28. Let ℒ𝑘 = {0, 1, . . . , 𝑘 − 𝐾 𝐿 − 1} be the set of indices in the deep left tail, possibly empty, and 

ℛ𝑘 = {𝑘 + 𝐾 𝑅 + 1, 𝑘 + 𝐾 𝑅 + 2, . . . , 𝑟 max } be the set of indices in the deep right tail, possibly empty. The pool is the set of indices 𝒫 𝑘 = ℒ𝑘 ∪ ℛ 𝑘 . The band consists of the constant-width window ℬ𝑘 = {𝑘 − 𝐾 𝐿 , 𝑘 − 𝐾 𝐿 + 1, . . . , 𝑘 +

𝐾 𝑅 − 1, 𝑘 + 𝐾 𝑅 } around 𝑘 .

Definition 28 says that the band is a constant-width band around the mode, and the pool is the rest of the indices in the deep tails. Furthermore, the band and the pool form a partition of the indices, that is, 𝒫 𝑘 ∪ ℬ 𝑘 = ℒ𝑘 ∪ ℛ 𝑘 ∪ ℬ 𝑘 = {0, 1, . . . , 𝑟 max } and 𝒫 𝑘 ∩ ℬ 𝑘 = ℒ𝑘 ∩ ℛ 𝑘 = ∅. It should be noted that either 

ℒ𝑘 or ℛ𝑘 can be empty, and we have sloppily ignored the boundary cases where the indices go to zero or negative, or beyond the upper bound on the ranks, but this has no effect on the results, as what we care about is establishing that |ℬ 𝑘 | = 𝑂 (1), and that the pool does not affect the tree construction in a meaningful way. We start by estimating the total masses of ℒ𝑘 and ℛ𝑘 .

Lemma 29. The sum of probabilities in the left tail is at most 

Õ 

> ℓ∈ℒ 𝑘

𝑝 ℓ ≤ 2𝑒 −2𝐾 𝐿 .

Proof. If ℓ ∈ ℒ 𝑘 , then, by Definition 28, ℓ ≤ 𝑘 − 𝐾 𝐿 − 1, so 𝑥 ℓ = 𝜃 2𝑘 −ℓ ≥ 𝜃 2𝐾 𝐿 +1 > 2𝐾 𝐿 . Since the sum decreases faster than exponentially, it is dominated by the first term, so we can bound the entire sum 

Íℓ ∈ℒ 𝑘 𝑝 ℓ ≤ 2𝑒 −2𝐾 𝐿 . □

19 Lemma 30. The sum of probabilities in the right tail is at most 

Õ

> 𝑟 ∈ℛ 𝑘

𝑝 𝑟 ≤ 2−𝐾 𝑅 .

Proof. The right tail probabilities decrease geometrically and are dominated by the first term, and are bounded by 2−𝐾 𝑅 by Lemma 10. □

We need to also bound the minimum values of the right tail, and the band. 

Lemma 31. For any 𝑘 ≥ 1 and 𝜆 ∈ 𝐼 𝑘 , we have 

• For any 𝑟 ∈ ℛ 𝑘 , 𝑝 𝑟 ≥ 𝑔 (𝛿 /2), and 

• For any 𝑏 ∈ ℬ 𝑘 , 𝑝 𝑏 ≥ min {𝑔 (2𝐾 𝐿 ), 𝑔 (2−𝐾 𝑅 −1)} .Proof. If 𝑟 ∈ ℛ 𝑘 , then 𝑟 ≤ 𝑟 max , and 𝑔 is increasing on (0, ln 2 ], so 𝑥 𝑟 ≥ 𝑥 𝑟 max > 𝛿 /2, implying 𝑝 𝑟 = 𝑔 (𝑥 𝑟 ) ≥ 

𝑔 (𝛿 /2) for all 𝑟 ∈ ℛ 𝑘 .For the second claim, if 𝑏 ∈ ℬ 𝑘 , then by unimodality, we have that the probability is nonincreasing when we go to the right from 𝑘 , and nonincreasing when we go to the left from 𝑘 , so the minimum is attained at the endpoints of the band. For the left edge, we have 𝑥 𝑘 −𝐾 𝐿 = 𝜃 2𝐾 𝐿 ≤ 2𝐾 𝐿 , so since 𝑔 

is decreasing on [ln 2 , ∞) , we have 𝑝 𝑘 −𝐾 𝐿 = 𝑔 (𝑥 𝑘 −𝐾 𝐿 ) ≥ 𝑔 (2𝐾 𝐿 ). For the right edge, we have 𝑥 𝑘 +𝐾 𝑅 =

𝜃 2−𝐾 𝑅 ≥ 2−𝐾 𝑅 −1, so since 𝑔 is increasing on (0, ln 2 ], we have 𝑝 𝑘 +𝐾 𝑅 = 𝑔 (𝑥 𝑘 +𝐾 𝑅 ) ≥ 𝑔 (2−𝐾 𝑅 −1). Hence, 

𝑝 𝑏 ≥ min {𝑔 (2𝐾 𝐿 ), 𝑔 (2−𝐾 𝑅 −1)} for all 𝑏 ∈ ℬ 𝑘 . □

We can now establish the boundary constants 𝐾 𝑅 and 𝐾 𝐿 .

Lemma 32. There exist constants 𝐾 𝐿 , 𝐾 𝑅 ≥ 1, as in Definition 28, dependent only on 𝛿 and independent of 𝜆 ,such that 

• Íℓ ∈ℒ 𝑘 𝑝 ℓ < 𝑝 𝑟 for every 𝑟 ∈ ℛ 𝑘 , and 

• Í𝑟 ∈ℛ 𝑘 𝑝 𝑟 < 𝑝 𝑏 for every 𝑏 ∈ ℬ 𝑘 .Proof. For the first claim, by Lemma 29, we have Íℓ ∈ℒ 𝑘 𝑝 ℓ ≤ 2𝑒 −2𝐾 𝐿 , and by Lemma 31, we have 𝑝 𝑟 ≥ 𝑔 (𝛿 /2)

for every 𝑟 ∈ ℛ 𝑘 . Thus, it suffices to choose 𝐾 𝐿 sufficiently large such that 2𝑒 −2𝐾 𝐿 < 𝑔 (𝛿 /2). We can do this since 𝑔 (𝛿 /2) is a positive constant independent of 𝐾 𝐿 , and lim 𝐾 𝐿 →∞ 2𝑒 −2𝐾 𝐿 = 0.For the second claim, by Lemma 30, we have Í𝑟 ∈ℛ 𝑘 𝑝 𝑟 ≤ 2−𝐾 𝑅 , and by Lemma 31, we have 𝑝 𝑏 ≥

min {𝑔 (2𝐾 𝐿 ), 𝑔 (2−𝐾 𝑅 −1)} for every 𝑏 ∈ ℬ 𝑘 . Thus, it suffices to choose 𝐾 𝑅 sufficiently large such that 2−𝐾 𝑅 <

min {𝑔 (2𝐾 𝐿 ), 𝑔 (2−𝐾 𝑅 −1)} . We can do this since 𝑔 (2𝐾 𝐿 ) and 𝑔 (2−𝐾 𝑅 −1) are positive constants independent of 

𝐾 𝑅 , and lim 𝐾 𝑅 →∞ 𝑔 (2−𝐾 𝑅 −1) = 0. □

Corollary 33. Lemma 32 implies that the band and the pool are well-defined, and the band has constant width, independent of 𝜆 .

Corollary 34. Lemma 32 implies that, applying the Huffman algorithm on the distribution, the left-hand tail first collapses into a single node, and then the right-hand tail collapses into a single node. Both the left-hand tail and the right-hand tail satisfy the comb criterion in Lemma 21 and collapse into a comb structure with a predefined merge order. Thus, the only nontrivial merges that can occur are those involving the band and only the band. 

Remark 35. We tacitly ignored the fact that the boundary constants in Lemma 32 can be rather large, and the band can extend beyond the lower or upper bounds of the ranks, but this is of no consequence, as we only care about bounding the band to be of constant width asymptotically. The distribution is rather well-behaved in practice, though. 

## E.4 Tree changes on an interval 

Corollary 34 says that the only nontrivial merges that can occur are those involving the constant-width band and only the band, as the tails collapse into combs before interacting with the band. However, this is not enough to establish the number of tree changes on an interval 𝐼 𝑘 . To do so, we establish that all merges involve a subset of leaves, so their weights are finite sums of exponentials. Observing that the merge order can only change if a difference between two weights changes sign, we can bound the 20 number of tree changes by bounding the number of zeros on such sums, which is constant independent of 𝜆 by Rolle’s theorem, thus establishing the claim. We need the following two lemmata, the latter of which presents a bound on the number of zeros of bounded-term sums of exponentials, which is a consequence of Rolle’s theorem. 

Lemma 36. For integers 1 ≤ 𝑎 ≤ 𝑏 ,

> 𝑏

Õ

> 𝑟 =𝑎

𝑝 𝑟 (𝜆 ) = 𝑒 − 𝜆  

> 2𝑏

− 𝑒 − 𝜆  

> 2𝑎

.

Proof. Follows directly from the properties of the distribution as discussed in Section 4.1. □

Corollary 37. The nodes that represent the collapsed left and right tails have weights that are a sum of at most four exponentials. Proof. The claim follows by observing that both the left and right tails consist of consequtive indices and by Lemma 36. □

Lemma 38. Let 0 < 𝑎 1 < 𝑎 2 < · · · < 𝑎 ℓ , and 

𝐹 (𝜆 ) =

> ℓ

Õ

> 𝑖 =1

𝑐 𝑖 𝑒 −𝑎 𝑖 𝜆 ,

such that not all 𝑐 𝑖 are zero. Then, 𝐹 has at most ℓ − 1 distinct zeros on (0, ∞) .Proof. We induct on ℓ . The base case ℓ = 1 is trivial, as 𝐹 has no zeros, by assumption that 𝑐 1 ≠ 0.Assuming the claim holds for ℓ − 1, consider 𝐺 (𝜆 ) = 𝑒 𝑎 1𝜆 𝐹 (𝜆 ). Clearly, 𝐺 and 𝐹 have the same zeros, so it suffices to show that 𝐺 has at most ℓ − 1 distinct zeros. By Rolle’s theorem, between any two distinct zeros of 𝐺 there is a zero of 𝐺 ′. We have 𝐺 ′(𝜆 ) = Íℓ𝑖 =2 𝑐 𝑖 (𝑎 1 − 𝑎 𝑖 )𝑒 −( 𝑎 𝑖 −𝑎 1)𝜆 , which is also a sum of ℓ − 1

exponentials with at most ℓ − 2 distinct exponents by the induction hypothesis. Thus, the claim holds for 𝐺 . □

Remark 39. The result of Lemma 38 could also have been obtained by arguing that 𝐹 is an analytic function on a compact interval. 

We can now argue that there are only a constant number of tree changes as 𝜆 varies over the interval 

𝐼 𝑘 .

Proposition 40. For fixed 𝑘 ≥ 1, as 𝜆 varies over 𝐼 𝑘 , the Huffman tree can change at most 𝑂 (1) times. Proof. Since the tails have a fixed merge order by Corollary 34, the only way the structure of the tree can change is if there is a tie between two candidate merges involving the band that can change the structure, that is, the difference between the weights of the two candidate merges changes sign as 𝜆 varies. Since |ℬ 𝑘 | = 𝑂 (1), there are only 𝑂 (1) candidate merges that can involve the band, and hence only 

𝑂 (1) candidate merges that can change the structure. Furthermore, since the weights of the nodes are finite-term sums of exponentials, as they are the sums of singleton weights for some subset of ℬ𝑘 , and there are only constantly many such subsets, plus the tail node by Corollary 37, the difference between the weights of any two candidate merges is a finite-term sum of exponentials, so by Lemma 38, there can only be 𝑂 (1) values of 𝜆 that can induce a tree change. □

## E.5 Total number of tree changes 

We now bound the total number of tree changes, that is, when when 𝜆 goes up to 𝑁 𝑚 for some upper bound 𝑁 on the cardinality 𝑛 . Let us recall the statement of Proposition 7 and prove it. 

Proposition 7. Let 𝜆 𝑛 = 𝑛 𝑚 for some fixed 𝑚 , and 𝑁 an upper bound on 𝑛 . The total number of tree changes as 𝜆 

varies over 𝜆 1 , 𝜆 2 , . . . , 𝜆 𝑁 is 𝑂 (log 𝑁 ).Proof. If 𝜆 ≤ 1, then [0, 𝜆 ] ∈ 𝐼 0, meaning 𝑟 ∗(𝜆 ) = 1, which implies there is no left tail. Thus, the right tail decays into a comb, yielding at most 𝑂 (1) tree changes. Consequently, if 𝜆 𝑁 ≤ 1, the total number of tree changes is 𝑂 (1), so assume 𝜆 𝑁 > 1.The interval [0, 𝜆 𝑁 ] intersects at most 𝑂 (log 𝑁 ) intervals of the form 𝐼 𝑘 , and by Proposition 40, there are at most 𝑂 (1) tree changes on each such interval, so the total number of tree changes is 𝑂 (log 𝑁 ). □

21 F Proofs of Theorems 8 and 9 

We will now analyze the remaining update types. Throughout this section, we assume that 𝑁 is the total number of updates, an upperbound on the cardinality, and 𝑛 = 1, 2, . . . , 𝑁 is the current cardinality at a certain time step. We fix 𝑚 , and set 𝜆 = 𝑛 𝑚 . After 𝑛 updates, we receive the (𝑛 + 1)th element with a new rank 𝑅 ∼ Geom   12

 that is independent of 𝑅 min or any current register values. We denote as usual 

𝑥 𝑟 = 𝜆  

> 2𝑟

, 𝑝 0 = 𝑒 −𝑥 0 and 𝑝 𝑟 = 𝑒 −𝑥 𝑟 (1 − 𝑒 −𝑥 𝑟 ) for 𝑟 ≥ 1.Let 𝑅 min = min 𝑗 ∈[ 𝐵 ] 𝑅 𝑗 be the minimum rank contained in a bucket. The following lemma ais necessary for analyzing the updates. 

Lemma 41. For any integer 𝑟 ≥ 0, we have 

Pr [𝑅 min ≤ 𝑟 ] = 1 − Pr [𝑅 𝑗 > 𝑟 ]𝐵 = 1 − ( 1 − 𝑒 −𝑥 𝑟 )𝐵 ,

and 

Pr [𝑅 min = 𝑟 ] = Pr [𝑅 min ≤ 𝑟 ] − Pr [𝑅 min ≤ 𝑟 − 1] = (1 − 𝑒 −𝑥 𝑟 −1 )𝐵 − ( 1 − 𝑒 −𝑥 𝑟 )𝐵 .

Proof. By definition: the minimum rank can be at most 𝑟 if and only if it is not the case that all registers are greater than 𝑟 . □

An ordinary update is triggered if and only if 𝑅 ≥ 𝑅 min , otherwise we can just discard the element. The probability of such an update is by independence of 𝑅 and 𝑅 min , Lemma 41, and marginalizing over 

𝑟 ,

Pr [𝑅 ≥ 𝑅 min ] =

> ∞

Õ

> 𝑟 =0
> ∞

Õ

> 𝑟 ′=𝑟

Pr [𝑅 min = 𝑟 ] Pr [𝑅 = 𝑟 ′]

=

> ∞

Õ

> 𝑟 =0

Pr [𝑅 min = 𝑟 ]

> ∞

Õ

> 𝑟 ′=𝑟

Pr [𝑅 = 𝑟 ′]

= Pr [𝑅 min = 0] + 

> ∞

Õ

> 𝑟 =1

Pr [𝑅 min = 𝑟 ] Pr [𝑅 ≥ 𝑟 ]

= Pr [𝑅 min = 0] + 

> ∞

Õ

> 𝑟 =1

Pr [𝑅 min = 𝑟 ]2−𝑟 −1

=  1 − ( 1 − 𝑒 −𝜆 )𝐵  +

> ∞

Õ

> 𝑟 =1

 (1 − 𝑒 −𝑥 𝑟 −1 )𝐵 − ( 1 − 𝑒 −𝑥 𝑟 )𝐵  2−𝑟 −1 .

(9) Let us now provide an upper bound on the number of ordinary updates. 

Proposition 42. Assuming 𝑚 = 𝑂 (𝑁 /log 3 𝑁 ), after 𝑁 insertions, the expected number of ordinary updates is at most 𝑂 (𝑁 /log 𝑁 ).Proof. For simplicity, let us write 𝜆 𝑛 = 𝑛 𝑚 for 𝑛 = 1, 2, . . . , 𝑁 , 𝑓 (𝑥 ) = (1 − 𝑒 −𝑥 )𝐵 , and rewrite Equation (9) as 

𝑆 (𝜆 ) = 𝑓 (𝜆 ) + 

> ∞

Õ

> 𝑟 =1

  𝑓 (𝑥 𝑟 −1) − 𝑓 (𝑥 𝑟 ) 2−𝑟 −1 . (10) We need to evaluate the sum 𝑁 Õ

> 𝑛 =0

𝑆 (𝜆 𝑛 ) . (11) To do this, we shall bound the inner sum of Equation (10). Let us split Equation (10) into three parts: the constant term 𝑓 (𝜆 ), the first ranks 1 ≤ 𝑟 < 𝑟 ∗ = ⌈log 2 𝜆 ⌉, and the remaining terms 𝑟 ≥ 𝑟 ∗:

𝑆 (𝜆 ) = 𝑓 (𝜆 ) + 

> 𝑟 ∗−1

Õ

> 𝑟 =1

  𝑓 (𝑥 𝑟 −1) − 𝑓 (𝑥 𝑟 ) 2−𝑟 −1 +

> ∞

Õ

> 𝑟 =𝑟 ∗

  𝑓 (𝑥 𝑟 −1) − 𝑓 (𝑥 𝑟 ) 2−𝑟 −1 . (12) The first term of Equation (12) bounded by Bernoulli’s inequality as 

1 − ( 1 − 𝑒 −𝜆 )𝐵 ≤ 𝐵𝑒 −𝜆 ≤ 𝐵 

𝜆 (13) 22 For the second term, observe that 𝑓 is increasing and 𝑓 (𝑥 𝑟 −1) ≤ 1, so therefore 

𝑓 (𝑥 𝑟 −1) − 𝑓 (𝑥 𝑟 ) ≤ 1 − 𝑓 (𝑥 𝑟 ) = 1 − ( 1 − 𝑒 −𝑥 𝑟 )𝐵 ≤ 𝐵𝑒 −𝑥 𝑟 ,

hence 𝑟 ∗−1Õ

> 𝑟 =1

  𝑓 (𝑥 𝑟 −1) − 𝑓 (𝑥 𝑟 ) 2−𝑟 −1 ≤ 𝐵 

2

> 𝑟 ∗−1

Õ

> 𝑟 =1

𝑒 −𝑥 𝑟 2−𝑟 = 𝐵 

2

> 𝑟 ∗−1

Õ

> 𝑟 =1

𝑒 − 𝜆  

> 2𝑟

2−𝑟 . (14) Substituting 𝑘 = 𝑟 ∗ − 𝑟 , we have 2−𝑟 𝑒 − 𝜆  

> 2𝑟

= 2−𝑟 ∗

2𝑘 𝑒 − 𝜆  

> 2𝑟

. Furthermore, we observe that 12 < 𝜆  

> 2𝑟 ∗

≤ 1, implying 

𝑒 − 𝜆  

> 2𝑟

≤ 𝑒 −2𝑘 −1

, so we can further bound the sum of Equation (14) as 

> 𝑟 ∗−1

Õ

> 𝑟 =1

  𝑓 (𝑥 𝑟 −1) − 𝑓 (𝑥 𝑟 ) 2−𝑟 −1 ≤ 𝐵 

2 2−𝑟 ∗

> ∞

Õ

> 𝑘 =1

𝑒 −2𝑘 −1

2𝑘 = 𝑂 

 𝐵 

𝜆 



, (15) by the fact that the series converges to a constant and 𝑟 ∗ = ⌈log 2 𝜆 ⌉.Finally, for the third term of Equation (12), we have 𝑥 𝑟 ≤ 1 for 𝑟 ≥ 𝑟 ∗, so therefore 1 − 𝑒 𝑥 𝑟 ≤ 𝑥 𝑟 , and hence 𝑓 (𝑥 𝑟 ) = (1 − 𝑒 −𝑥 𝑟 )𝐵 ≤ 𝑥 𝐵 𝑟 for 𝑟 ≥ 𝑟 ∗, so 𝑓 (𝑥 𝑟 −1) − 𝑓 (𝑥 𝑟 ) ≤ 𝑓 (𝑥 𝑟 −1) ≤ 𝑥 𝐵 𝑟 −1. Therefore, 

> ∞

Õ

> 𝑟 =𝑟 ∗+1

  𝑓 (𝑥 𝑟 −1) − 𝑓 (𝑥 𝑟 ) 2−𝑟 −1 ≤

> ∞

Õ

> 𝑟 =𝑟 ∗+1

 𝜆 

2𝑟 −1

𝐵 

2−𝑟 −1 = 𝑂 

 1

𝜆 



,

because 2𝑟 ∗−1 < 𝜆 ≤ 2𝑟 ∗

, so the series is 𝑂 (2−𝑟 ∗

) = 𝑂   1

> 𝜆

.We need to check the special case of 𝑟 = 𝑟 ∗, where we have 0 ≤   𝑓 (𝑥 𝑟 ∗−1) − 𝑓 (𝑥 𝑟 ∗ ) 2−𝑟 ∗−1 ≤ 2−𝑟 ∗−1 =

𝑂   1

> 𝜆

.Putting this all together, we get that the inner sum of Equation (12) is at most 𝑆 (𝜆 ) = 𝑂   𝐵 

> 𝜆

 = 𝑂   𝐵𝑚 𝑛 

,so we get 𝑁 Õ

> 𝑛 =0

𝑆 (𝜆 𝑛 ) = 𝑂  𝐵𝑚 log 𝑁  = 𝑂 



𝑚 log 2 𝑁 



,

by approximating the harmonic series with the natural logarithm, and considering that 𝐵 = 𝑂 (log 𝑁 ).Finally, if 𝑚 = 𝑂 (𝑁 /log 3 𝑁 ), then the expected number of ordinary updates is at most 𝑂 (𝑁 /log 𝑁 ), thus yielding the claim. □

Corollary 43. Assuming an ordinary update takes 𝑂 (log 𝑛 ) time and that 𝑚 = 𝑂 (𝑛 /log 3 𝑛 ), the amortized cost of an ordinary update is O(1) over 𝑛 updates. 

Corollary 43 makes intuitive sense: the expected number of ordinary updates is only bounded to a reasonable number if the number of elements is sufficiently large compared to the number of registers, as otherwise almost all registers are empty, and we are better off using linear counting [WZT90]. Next, we analyze the number of minimum-rank updates. 

Proposition 44. The expected number of minimum-rank updates after 𝑁 insertions is at most 𝑂 (𝑚 /log 𝑁 ).Proof. There are 𝑂   𝑚 𝐵 

 = 𝑂 

 𝑚  

> log 𝑁



buckets, and there are 𝑂 (log 𝑁 ) possible rank values. Therefore, there are at most 𝑂 (𝑚 ) possible minimum-rank updates. □

Corollary 45. Assuming a minimum-rank update takes 𝑂 (log 2 𝑛 ) time and that 𝑚 = 𝑂 (𝑛 /log 3 𝑛 ), the amortized cost of a minimum-rank update is O(1) over 𝑛 updates. 

Corollary 45 is rather loose, but since this is subsumed by the cost of ordinary updates, it suffices for our purposes. We can now put these results together to obtain the following results. Let us recall the statement of Theorem 8 and prove it. 

Theorem 8. Assuming an ordinary update takes 𝑂 (log 𝑛 ) time, a minimum-rank update takes 𝑂 (log 2 𝑛 ) time, and a Huffman tree update followed by re-encoding of all codewords takes 𝑂 (𝑚 log 𝑛 ) time, and furthermore assuming that 𝑚 = 𝑂 (𝑛 /log 3 𝑛 ), the amortized cost of updates is O(1) over 𝑛 updates. 

23 Proof. Since we want to bound the amortized costs over 𝑛 updates, we can just bound all of these separately and sum the bounds. By Proposition 7, there are at most 𝑂 (log 𝑛 ) tree updates, so the total cost of tree updates is at most 𝑂 (𝑚 log 2 𝑛 ), which is 𝑂 (𝑛 /log 𝑛 ) under the assumption on 𝑚 . The total cost of ordinary updates is at most 𝑂 (𝑛 ) by Proposition 42, and the total cost of minimum-rank updates is at most 𝑂 (𝑚 log 𝑛 ) by Proposition 44, which is 𝑂 (𝑛 /log 2 𝑛 ) under the assumption on 𝑚 . Thus, the total cost of all updates is at most 𝑂 (𝑛 /log 𝑛 + 𝑛 + 𝑛 /log 2 𝑛 ) = 𝑂 (𝑛 ), so the amortized cost is O(1), as claimed. □

Let us recall the statement of Theorem 9 and prove it. 

Theorem 9. Assuming that we can access Ω(log 𝑛 ) bits in constant time and 𝑚 = Ω (log 2 𝑛 ), so we can maintain a Huffman codebook, so we can assume an ordinary update takes 𝑂 (1) time, a minimum-rank update takes 𝑂 (log 𝑛 )

time, and a Huffman tree update followed by re-encoding of all codewords takes 𝑂 (𝑚 ) time, the amortized cost of updates is O(1) over 𝑛 updates if we further assume 𝑚 = 𝑂 (𝑛 /log 2 𝑛 ).Proof. By Proposition 7, there are at most 𝑂 (log 𝑛 ) tree updates, so the total cost of tree updates is at most 𝑂 (𝑚 log 𝑛 ), which is 𝑂 (𝑛 /log 𝑛 ) under the assumption on 𝑚 . The total cost of ordinary updates is at most 𝑂 (𝑛 ) by Proposition 42 if we adjust for 𝑚 , and the total cost of minimum-rank updates is at most 𝑂 (𝑚 ) by Proposition 44, which is 𝑂 (𝑛 /log 2 𝑛 ) under the assumption on 𝑚 . Thus, the total cost of all updates is at most 𝑂 (𝑛 ), so the amortized cost is O(1), as claimed. □

# G Pseudocode for the operations 

Pseudocode for the operations of the HBS sketch is given in Algorithm 1. 24 Algorithm 1 Huffman-Bucket Sketch operations.  

> 1:

function Peek (𝑏, 𝑗 ) 

> 2:

Determine the 𝑗 th codeword start 𝑠 𝑗 and length ℓ𝑗 in bucket 𝑏 with the unary array  

> 3:

Get the codeword 𝑐 𝑗 from the bits 𝑠 𝑗 , 𝑠 𝑗 + 1, . . . , 𝑠 𝑗 + ℓ𝑗 − 1 in the codeword array  

> 4:

Decode 𝑐 𝑗 using the Huffman tree or lookup table to obtain the rank value 𝑟  

> 5:

return 𝑟  

> 6:

end function  

> 7:

function Poke (𝑟, 𝑏, 𝑗 ) 

> 8:

Encode 𝑟 into a codeword 𝑐 using the Huffman tree or lookup table  

> 9:

Replace 𝑐 old with 𝑐 , shifting the bucket if necessary  

> 10:

end function  

> 11:

function Insert (𝑦 ) 

> 12:

Compute 𝑏 ← ℎ𝑏 (𝑦 ), 𝑗 ← ℎ𝑗 (𝑦 ), and 𝑟 ← 𝜌 (ℎ𝑟 (𝑦 )) . 

> 13:

if 𝑟 > 𝑟 min of the bucket 𝑏 then  

> 14:

𝑟 old ← Peek (𝑏, 𝑗 ) 

> 15:

if 𝑟 > 𝑟 old then  

> 16:

Poke (𝑟, 𝑏, 𝑗 ) 

> 17:

if 𝑟 old = 𝑟 min then  

> 18:

𝑐 min ← 𝑐 min − 1 

> 19:

if 𝑐 min = 0 then  

> 20:

Recompute 𝑟 min and 𝑐 min by peeking all registers in the bucket  

> 21:

end if  

> 22:

end if  

> 23:

Update ˆ𝑛 𝑏 and ˆ𝑛 . 

> 24:

if ˆ𝑛 − ˆ 𝑛 old is too large then  

> 25:

Reconstruct the Huffman tree and re-encode all buckets  

> 26:

ˆ𝑛 old ← ˆ 𝑛  

> 27:

end if  

> 28:

end if  

> 29:

end if  

> 30:

end function  

> 31:

function Merge (𝑆 1 , 𝑆 2) 

> 32:

for all 𝑏 ∈ [ 𝑚 /𝐵 ] do  

> 33:

Construct ˆ𝑛 𝑏 by iterating over 𝑗 ∈ [ 𝐵 ] and taking max of the 𝑟 𝑗 from the sketches  

> 34:

end for  

> 35:

ˆ𝑛 ← Í ˆ𝑛 𝑏  

> 36:

if ˆ𝑛 − max { ˆ 𝑛 old ,1 , ˆ𝑛 old ,2} is too large then  

> 37:

Construct the Huffman tree using ˆ𝑛  

> 38:

else  

> 39:

Reuse the Huffman tree from the sketch with the larger ˆ𝑛 old  

> 40:

end if  

> 41:

for all (𝑏, 𝑗 ) ∈ [ 𝑚 /𝐵 ] × [ 𝐵 ] do  

> 42:

𝑟 ← max {Peek 𝑆 1 (𝑏, 𝑗 ), Peek 𝑆 2 (𝑏, 𝑗 )}  

> 43:

Poke (𝑟, 𝑏, 𝑗 ) 

> 44:

end for  

> 45:

end function 

25