---
title: "Bridging the High-Frequency Data Gap: A Millisecond-Resolution Network Dataset for Advancing Time Series Foundation Models"
title_zh: 弥补高频数据空白：一个用于推进时间序列基础模型的毫秒级分辨率网络数据集
authors: "Subina Khanal, Seshu Tirupathi, Merim Dzaferagic, Marco Ruffini, Torben Bach Pedersen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16497v1"
tags: ["query:netmes"]
score: 8.0
evidence: 高频网络流量与无线状况数据集
tldr: 本研究针对时间序列基础模型（TSFM）在处理高频数据方面的短板，推出了一个具有毫秒级分辨率的5G无线网络数据集。该数据集填补了现有大规模数据集在秒级以下分辨率的空白，并引入了无线网络这一新领域。通过在短时预测任务上对传统模型和TSFM进行基准测试，发现现有TSFM在零样本和微调情况下均表现欠佳。此工作强调了将高频数据纳入预训练对于提升模型泛化性与鲁棒性的必要性。
selection_source: fresh_fetch
motivation: 现有时间序列基础模型的数据集多集中在秒级到年级的低频率，难以捕捉毫秒级的高频动态变化。
method: 引入了一个来自真实5G无线部署的毫秒级分辨率数据集，并提供了跨度为100毫秒至9.6秒的短时预测用例。
result: 基准测试表明，大多数现有的TSFM在处理这种高频新数据分布时，无论是零样本还是微调模式下表现都较差。
conclusion: 研究强调了在预训练阶段整合高频数据集对于优化TSFM架构、提升其在现实应用中泛化能力和鲁棒性的重要意义。
---

## 摘要
时间序列基础模型 (TSFMs) 需要多样化的真实世界数据集，以适应不同的领域和时间频率。然而，目前的大规模数据集主要集中在采样间隔（即时间分辨率）为秒级到年级的低频时间序列上，这阻碍了它们捕捉高频时间序列数据细微差别的能力。为了解决这一局限性，我们引入了一个新颖的数据集，该数据集捕获了来自实际运行的 5G 无线部署的毫秒级分辨率无线和流量状况，扩展了 TSFMs 的范围，使其能够将高频数据纳入预训练。此外，该数据集引入了一个新领域——无线网络，从而补充了能源和金融等现有的更通用领域。该数据集还提供了短期预测的使用案例，预测范围从 100 毫秒（1 步）到 9.6 秒（96 步）。通过在该数据集的预测任务上对传统机器学习模型和 TSFMs 进行基准测试，我们证明了大多数 TSFM 模型配置在零样本和微调设置下，在此新数据分布上的表现均较差。我们的工作强调了在预训练和预测过程中纳入高频数据集的重要性，以增强 TSFMs 在实际应用中的架构、微调策略、泛化能力和鲁棒性。

## Abstract
Time series foundation models (TSFMs) require diverse, real-world datasets to adapt across varying domains and temporal frequencies. However, current large-scale datasets predominantly focus on low-frequency time series with sampling intervals, i.e., time resolution, in the range of seconds to years, hindering their ability to capture the nuances of high-frequency time series data. To address this limitation, we introduce a novel dataset that captures millisecond-resolution wireless and traffic conditions from an operational 5G wireless deployment, expanding the scope of TSFMs to incorporate high-frequency data for pre-training. Further, the dataset introduces a new domain, wireless networks, thus complementing existing more general domains like energy and finance. The dataset also provides use cases for short-term forecasting, with prediction horizons spanning from 100 milliseconds (1 step) to 9.6 seconds (96 steps). By benchmarking traditional machine learning models and TSFMs on predictive tasks using this dataset, we demonstrate that most TSFM model configurations perform poorly on this new data distribution in both zero-shot and fine-tuned settings. Our work underscores the importance of incorporating high-frequency datasets during pre-training and forecasting to enhance architectures, fine-tuning strategies, generalization, and robustness of TSFMs in real-world applications.

---

## 论文详细总结（自动生成）

以下是对论文《Bridging the High-Frequency Data Gap: A Millisecond-Resolution Network Dataset for Advancing Time Series Foundation Models》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前时间序列基础模型（TSFMs）虽然在通用任务上表现出色，但其预训练数据大多集中在低频领域（如秒级、小时级、年级），缺乏毫秒级的高频数据。
*   **核心问题**：现有的 TSFMs 是否能够处理具有高度非平稳性、突发噪声和毫秒级分辨率的真实无线网络数据？
*   **整体含义**：本文通过引入一个全新的 5G 无线网络毫秒级数据集，填补了高频数据的空白，并揭示了现有基础模型在处理此类极端动态数据时的局限性。

### 2. 方法论
*   **数据采集架构**：基于 OpenIreland 测试床，利用软件定义无线电（USRP）模拟 5G O-RAN 环境。涵盖了 CU（中央单元）、DU（分布单元）和 RU（射频单元）的解耦架构。
*   **核心思想**：通过模拟多种移动模式（静态、行人、车载、高速铁路）和流量类型（合法流量如视频流、VoIP，以及恶意流量如 DDoS 攻击），构建一个多维度的无线网络性能指标（PM）序列。
*   **关键技术细节**：
    *   **特征工程**：提取物理层（PHY）和介质访问控制层（MAC）指标，包括信道质量指示（CQI）、调制编码方案（MCS）、信噪比（SINR）、信号强度（RSSI）及下行比特率。
    *   **预测任务**：设定为短期预测，预测窗口从 100 毫秒（1 步）到 9.6 秒（96 步）。
    *   **数据处理**：将原始毫秒数据聚合为 100 毫秒间隔，以平衡计算开销与实时控制需求。

### 3. 实验设计
*   **数据集/场景**：
    *   **目标数据集**：自研 5G 网络数据集（Network）。
    *   **对比数据集**：ETTh1、Electricity、Weather、Traffic（用于统计特性对比，证明网络数据的独特性）。
*   **基准模型（Benchmark）**：
    *   **浅层/传统模型**：随机森林（RF）、XGBoost（XGB）、自适应随机森林（ARF，擅长处理概念漂移）、在线线性回归（OLR）、Naive 预测。
    *   **基础模型（TSFMs）**：TinyTimeMixer (TTM)、Chronos (Chronos-bolt-small)、Lag-Llama。
*   **对比设置**：分为单变量预测和多变量预测；评估 TSFMs 的零样本（Zero-shot）性能与微调（Fine-tuning）性能。

### 4. 资源与算力
*   **硬件规格**：实验在一台本地机器上完成，配置为：Intel Core i7-1165G7 CPU (4 核 8 线程)，32 GB RAM。
*   **算力说明**：论文**未明确提及使用 GPU**，考虑到所选的 TSFMs 变体（如 Chronos-small）较为轻量，且实验包含大量传统 CPU 密集型模型（如 ARF），推测主要计算在 CPU 上完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单/多变量对比实验。
    *   针对 TTM 进行了多种微调策略（Head-only, Adapter）的消融研究。
    *   测试了不同时间分辨率（100ms 到 3000ms）对模型性能的影响。
    *   涵盖了 5 种移动模式和多种流量标签的泛化性测试。
*   **充分性评价**：实验设计较为充分且客观。通过使用三个随机种子确保了结果的可重复性，并详细对比了不同数据集的 STL 分解和自相关特性，证明了实验结果的差异源于数据分布的本质不同。

### 6. 主要结论与发现
*   **TSFMs 表现不佳**：在毫秒级网络数据上，大多数 TSFMs 的表现甚至不如简单的 Naive 预测或传统的 ARF 模型。
*   **ARF 的优越性**：自适应随机森林（ARF）在多变量设置下表现最佳，因为它能动态更新树结构以应对网络数据中频繁出现的“概念漂移”（Concept Drift）。
*   **分布失配**：TSFMs 在低频数据上训练形成的先验知识（平滑趋势、强季节性）无法直接迁移到具有突发尖峰、弱季节性和高噪声的高频无线网络数据中。
*   **微调局限性**：即使经过微调，TSFMs 的提升也有限，说明仅靠少量领域数据难以修正预训练阶段缺失的高频特征捕捉能力。

### 7. 优点
*   **领域填补**：首次将无线网络这一垂直领域引入 TSFM 基准测试，并提供了极高分辨率的数据。
*   **深度分析**：不仅对比了精度，还通过统计学手段（如 SNR 分析、Q-Q 图）深入剖析了为什么现有模型会失效。
*   **实用性强**：数据集包含恶意攻击标签，为未来的网络安全预测和异常检测提供了基础。

### 8. 不足与局限
*   **模型规模限制**：实验主要使用了 TSFMs 的小型变体（如 Chronos-small），虽然文中提到 Chronos-base 提升有限，但未在大规模参数模型上做全面验证。
*   **特征工程简化**：对于 TSFMs，未进行复杂的特征对齐或专门针对高频数据的预处理优化。
*   **算力环境受限**：实验在移动端 CPU 环境下运行，可能限制了对更复杂微调算法（如全参数微调）的探索。

（完）
