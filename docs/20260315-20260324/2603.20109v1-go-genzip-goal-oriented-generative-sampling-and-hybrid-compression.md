---
title: "GO-GenZip: Goal-Oriented Generative Sampling and Hybrid Compression"
title_zh: GO-GenZip：目标导向的生成式采样与混合压缩
authors: "Pietro Talli, Qi Liao, Alessandro Lieto, Parijat Bhattacharjee, Federico Chiariotti, Andrea Zanella"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20109v1"
tags: ["query:netmes"]
score: 9.0
evidence: 生成式AI驱动的网络遥测采样与混合压缩
tldr: "针对网络遥测数据量巨大导致存储和传输不可持续的问题，本文提出GO-GenZip框架。该框架采用面向目标的生成式AI技术，将自适应采样与混合压缩相结合。通过联合优化观测策略和编码方式，在保留关键特征的同时利用生成模型重建数据。实验证明，该方法在保持高重建精度和分析保真度的前提下，将采样和传输成本降低了50%以上。"
selection_source: fresh_fetch
motivation: 传统的网络遥测数据处理方式在面对海量细粒度KPI流时，其存储、传输和实时分析成本已变得不可持续。
method: 提出一种面向目标的生成式采样与混合压缩框架，通过自适应掩码采样和生成式模型，结合传统无损编码与GenAI驱动的有损压缩。
result: "在真实网络数据集上的实验表明，该框架在维持下游任务分析精度的同时，减少了超过50%的采样和数据传输成本。"
conclusion: GO-GenZip通过联合优化采样与压缩，为高效、可持续的大规模网络遥测数据管理提供了一种创新的生成式AI解决方案。
---

## 摘要
目前的网络数据遥测流水线由来自多个分布式源、流向中央聚合器的大规模细粒度关键性能指标（KPI）流组成，这使得数据存储、传输和实时分析变得日益不可持续。本研究提出了一种由生成式人工智能（GenAI）驱动的采样与混合压缩框架，从目标导向的角度重新设计了网络遥测。与被动压缩完全观测数据的传统方法不同，我们的方法在信息与下游任务相关性的指导下，联合优化了观测内容及其编码方式。该框架将采用自适应掩码技术的自适应采样策略与生成式建模相结合，以识别模式并在时空维度上保留关键特征。选择性获取的数据通过混合压缩方案进一步处理，该方案结合了传统的无损编码与 GenAI 驱动的有损压缩。在真实网络数据集上的实验结果表明，采样和数据传输成本降低了 50% 以上，同时在下游任务中保持了相当的重构精度和目标导向的分析保真度。

## Abstract
Current network data telemetry pipelines consist of massive streams of fine-grained Key Performance Indicators (KPIs) from multiple distributed sources towards central aggregators, making data storage, transmission, and real-time analysis increasingly unsustainable. This work presents a generative AI (GenAI)-driven sampling and hybrid compression framework that redesigns network telemetry from a goal-oriented perspective. Unlike conventional approaches that passively compress fully observed data, our approach jointly optimizes what to observe and how to encode it, guided by the relevance of information to downstream tasks. The framework integrates adaptive sampling policies, using adaptive masking techniques, with generative modeling to identify patterns and preserve critical features across temporal and spatial dimensions. The selectively acquired data are further processed through a hybrid compression scheme that combines traditional lossless coding with GenAI-driven, lossy compression. Experimental results on real network datasets demonstrate over 50$\%$ reductions in sampling and data transfer costs, while maintaining comparable reconstruction accuracy and goal-oriented analytical fidelity in downstream tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GO-GenZip** 的框架，旨在通过生成式人工智能（GenAI）优化网络遥测数据的采样与压缩。以下是对该论文的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代网络（如5G/6G）产生海量的细粒度关键性能指标（KPI）数据。传统的遥测流水线通常被动地收集并压缩所有观测数据，导致存储、带宽和计算资源承受巨大压力，且往往忽略了数据对下游特定任务（如故障预测、负载均衡）的实际价值。
*   **研究动机**：
    *   **不可持续性**：分布式源向中央聚合器发送的原始数据流规模呈指数级增长。
    *   **缺乏目标导向**：传统压缩不区分数据的重要性，导致传输了大量对最终决策无用的冗余信息。
    *   **采样与压缩脱节**：现有的掩码自编码器（MaskAE）多采用随机掩码，未考虑数据的结构特征和任务需求。

### 2. 方法论：GO-GenZip 框架
*   **核心思想**：将“观察什么”（采样）与“如何编码”（压缩）进行联合优化，由下游任务的目标（Goal）驱动。
*   **关键技术细节**：
    *   **自适应采样策略 ($\pi_s$)**：利用上下文信息（基站类别、时间、任务索引），通过一个全连接神经网络生成采样掩码。使用 **Gumbel-Softmax** 技巧使离散的采样过程可微，从而实现端到端的梯度训练。
    *   **混合压缩策略 ($\pi_c$)**：将采样数据分为两部分：一部分通过 **GenAI 编码器**（有损、高压缩比）处理，另一部分通过 **LZMA 算法**（无损、保留细节）处理。这种混合模式平衡了重建保真度与压缩效率。
    *   **双重优化与约束匹配**：引入拉格朗日乘数（$\beta_s, \beta_c$）构建损失函数，在满足采样预算（Memory）和传输速率预算（Bandwidth）的约束下，最大化互信息或任务准确度。
    *   **目标导向（GO）训练**：不仅优化数据重建误差，还将下游预测任务（如时延预测、吞吐量预测）纳入训练循环，实现端到端优化。

### 3. 实验设计
*   **数据集**：使用来自 **1162个真实4G基站** 的遥测数据，包含34个KPI指标，采样间隔为1小时，持续10天。
*   **实验场景**：
    *   **数据重建**：评估模型还原原始KPI张量的能力。
    *   **多任务预测**：包含下行资源块使用率、连接数、时延、负载、上行速率和切换尝试等6个预测任务。
*   **对比方法（Benchmark）**：
    *   **S-G (Sampling-Generative)**：仅使用生成式有损压缩。
    *   **S-H (Sampling-Hybrid)**：本文提出的混合压缩方案。
    *   **固定策略**：不随上下文变化的静态采样掩码。
    *   **无损压缩**：纯传统的无损压缩基准。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **模型结构**：提到策略网络为两层 MLP（使用 ELU 激活），预测模块为三层结构（128, 64, 32 神经元，使用 GELU 激活）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同的采样率（SR）和压缩比（CR）进行了多组对比实验。
    *   进行了消融实验，对比了“混合 vs 纯生成”以及“自适应 vs 固定策略”。
    *   展示了针对不同基站类别和任务生成的采样掩码可视化图。
*   **充分性评价**：实验设计较为充分，涵盖了从底层重建到高层任务性能的多个维度。使用真实世界的大规模基站数据增加了结果的说服力。

### 6. 主要结论与发现
*   **大幅减产**：在保持相当的重建精度和任务性能的前提下，采样和数据传输成本降低了 **50% 以上**。
*   **混合方案的优越性**：混合压缩（S-H）在所有配置下均优于纯生成式压缩（S-G），尤其在处理稀疏数据时表现更稳健。
*   **自适应性的价值**：自适应策略显著优于固定掩码策略，能够根据基站类型和时间动态调整关注的 KPI。
*   **GO 训练的提升**：端到端的目标导向训练相比仅基于重建的训练，在下游任务中实现了更低的平均绝对误差（MAE）和更高的稳定性。

### 7. 优点与亮点
*   **联合优化**：首次将自适应采样掩码与混合压缩（有损+无损）在目标导向框架下结合。
*   **灵活性**：框架具有通用性，可扩展至信道图构建（Channel Charting）或通感一体化（ISAC）等其他网络场景。
*   **实用性**：直接针对电信运营商面临的“数据爆炸”痛点，且在真实数据集上验证了有效性。

### 8. 不足与局限
*   **计算开销**：虽然降低了传输成本，但在基站端运行神经网络策略和编码器会增加额外的计算开销，论文未详细分析这种“计算换带宽”的能效比。
*   **实时性挑战**：双重优化和 Gumbel-Softmax 训练过程较为复杂，可能影响在线更新策略的速度。
*   **应用限制**：目前主要针对时间序列 KPI 数据，对于非结构化或极高维度的原始物理层数据（如 IQ 样本）的适用性尚待验证。

（完）
