Title: PARD-SSM: Probabilistic Cyber-Attack Regime Detection via Variational Switching State-Space Models

URL Source: https://arxiv.org/pdf/2604.02299v1

Published Time: Fri, 03 Apr 2026 01:16:10 GMT

Number of Pages: 19

Markdown Content:
# PARD-SSM: Probabilistic Cyber-Attack Regime Detection via Variational Switching State-Space Models 

Prakul Sunil Hiremath 

prakulhiremath@vtu.ac.in github.com/prakulhiremath 1,2 

Sahil Bhekane 

sahilbhekane276@gmail.com github.com/sahilbhekane 1,2 

PeerAhammad M Bagawan 

peerahamadmb@gmail.com github.com/peerahammad 1,2 1Department of Computer Science and Engineering, Visvesvaraya Technological University (VTU), Belagavi, Karnataka, India 

> 2

Aliens on Earth (AoE) Autonomous Research Group, Belagavi, India 

aliensonearth.in 

Abstract 

Modern adversarial campaigns do not manifest as isolated, detectable events. They unfold as temporally ordered sequences of behavioural phases—Reconnaissance, Lateral Move-ment, Intrusion, and Exfiltration—each of which may, in isolation, appear statistically indistinguishable from legitimate network traffic. Existing Intrusion Detection Systems (IDS) suffer a fundamental architectural mismatch with this reality: signature-based sys-tems fail on zero-day variants, deep-learning anomaly detectors produce uninterpretable scalar anomaly scores with no stage attribution, and standard Kalman Filters cannot model the non-stationary, multi-modal dynamics of traffic transitioning across kill-chain phases. We present PARD-SSM (Probabilistic Attack Regime Detection via Switching State-Space Models), a generative probabilistic framework that models high-dimensional net-work telemetry as a Regime-Dependent Switching Linear Dynamical System (RD-SLDS). Each of K=4 discrete hidden regimes governs a distinct set of continuous latent dynam-ics, parameterised by regime-specific state-transition and observation matrices whose eigenstructure encodes the characteristic physics of each kill-chain phase. Exact Bayesian inference in this model is computationally intractable—the posterior mixture grows as O(KT ). We resolve this via a Structured Variational Approximation 

that factorises the joint posterior into independent continuous and discrete marginals, reducing inference complexity to O(T K 2) and enabling packet-rate detection on standard edge CPU hardware without GPU acceleration. An Online Expectation-Maximisation 

(EM) algorithm with exponential forgetting continuously adapts the Regime Transition Probability Matrix Π and noise parameters to non-stationary traffic distributions. A

KL-Divergence gating mechanism suppresses false positives by distinguishing genuine regime transitions from benign traffic anomalies. Evaluated on CICIDS2017 and UNSW-NB15 , PARD-SSM achieves an F1 score of 

98.2% across seven attack classes, a stage attribution accuracy of 86.1% , and an inference latency of < 1.2 ms per flow —without GPU acceleration. Most critically, by exploiting the 

1    

> arXiv:2604.02299v1 [cs.CR] 2 Apr 2026 learned off-diagonal structure of Π, PARD-SSM generates predictive kill-chain alerts an average of ≈8 minutes before ground-truth attack onset , a predictive horizon absent from all prior-art systems evaluated.

Keywords: Intrusion Detection Systems, Switching Linear Dynamical Systems, Variational Inference, Kalman Filter, Cyber Kill-Chain, Online Learning, Network Security, Probabilistic Graphical Models. 

1. Introduction 

1.1. The Multi-Stage Attack Problem 

The adversarial kill chain, formalised by Hutchins et al. (2011) and operationalised in the MITRE ATT&CK framework (Strom et al., 2018), describes modern cyber-attacks as sequences of discrete tactical phases. A sophisticated adversary does not immediately exfiltrate data upon breaching a perimeter; instead, they methodically progress through Reconnaissance (enumeration of target surfaces), Lateral Movement (escalation and pivoting within the network), and Intrusion before reaching the Exfiltration phase. Each individual phase, viewed in isolation at the network layer, may generate telemetry that is statistically compatible with legitimate user activity. A naive anomaly detector optimised on individual flow statistics will therefore fail to raise an alert until the attack is already well advanced—or will generate an overwhelming volume of false positives by flagging every minor statistical deviation. The fundamental open problem in network intrusion detection is therefore not the de-tection of anomalous packets, but the sequential, probabilistic inference of the latent attack stage from observable, high-dimensional network telemetry, in real time, and with suffi-cient advance warning to permit a proactive defensive response before the attack has been completed. 

1.2. Limitations of Existing Approaches Signature-based IDS. Systems such as Snort (Roesch, 1999) and Suricata operate by matching observed packet headers or payload bytes against a database of known-malicious patterns. This paradigm has three fundamental limitations with respect to multi-stage detection. First, it is categorically incapable of detecting zero-day campaigns for which no signature has been developed. Second, it provides no probabilistic stage attribution: a triggered rule identifies an event as “malicious” but cannot quantify the probability that this event is part of a Reconnaissance phase versus an active Exfiltration. Third, the approach provides no predictive capability: it can only react to events that have already occurred and matched a known pattern. 

Deep Learning Anomaly Detectors. A substantial body of recent literature (Bontemps et al., 2016; Vinayakumar et al., 2019; Kim et al., 2020; Al-Qatf et al., 2021) has applied recurrent neural architectures—Long Short-Term Memory (LSTM), Gated Recurrent Units (GRU), and more recently Transformer-based sequence models (Wu et al., 2021)—to intrusion detection. While these models represent a genuine technical advance over signature-based systems in their capacity to detect statistical deviations from a normal-traffic baseline, they suffer from four material deficiencies that motivate the present work. 11. Opacity and absence of stage attribution. Deep-learning IDS are black-box function approximators that map raw telemetry sequences to a scalar anomaly score or binary classification label. They cannot decompose a detected anomaly into its constituent kill-chain components; there is no mechanism by which such a system can assert with calibrated probability that the current network state corresponds to a Reconnaissance phase rather than an Exfiltration phase. 2. No predictive capability. These models are reactive: they detect anomalies that are already present in the observed data. They cannot exploit the causal structure of the kill chain—the fact that Reconnaissance statistically precedes Intrusion, and Intrusion precedes Exfiltration—to issue an alert before the more damaging phases of an attack have commenced. 3. GPU dependency. Real-time inference with deep sequence models requires GPU accel-eration, precluding deployment on the resource-constrained edge gateway hardware (standard quad-core CPUs, 8–16 GB RAM) that constitutes the majority of enterprise network perimeter infrastructure. 4. Absence of principled uncertainty quantification. Deep-learning IDS provide no cali-brated confidence interval around their anomaly scores, making it impossible for auto-mated response orchestration systems to reason about the probability of a false-positive event. 

Traditional Probabilistic Models. Standard Hidden Markov Models (HMMs) and single-regime Kalman Filters (Shumway and Stoffer, 2000) offer transparency and a principled probabilistic framework but fail on multi-stage adversarial traffic due to critical structural assumptions. Standard HMMs assume conditional independence of observations given the hidden state, which is violated by high-dimensional, temporally correlated network telemetry. Single-regime Kalman Filters assume a single, fixed linear dynamical model for the entire time series, which is fundamentally violated when the traffic transitions between structurally distinct kill-chain phases. The computational intractability of exact inference in Switching Linear Dynamical Systems (Ghahramani and Hinton, 2000; Murphy, 1998) has historically prevented their application to real-time network monitoring. 

1.3. Contributions of this Paper                   

> Summary of Contributions.
> 1. RD-SLDS Generative Model: A Regime-Dependent Switching Linear Dynamical System that captures the distinct eigenstructures of four kill-chain phases within a single unified probabilistic model (Section 4). 2. Structured Variational Inference: A tractable O(T K 2)approximation to the intractable
> O(KT)exact posterior, via a novel mean-field ELBO optimisation over alternating regime-weighted Kalman smoother and HMM Forward-Backward passes (Section 6). 3. Online EM Adaptation: An exponential-forgetting online EM algorithm that continuously updates the Regime Transition Probability Matrix Πand regime-specific noise parameters from the live traffic stream (Section 7). 4. KL-Divergence Gating: An information-theoretic false-positive suppression mechanism that distinguishes genuine adversarial regime transitions from benign traffic anomalies (Section 8). 5. Predictive Kill-Chain Alerting: By exploiting the learned off-diagonal structure of Π, PARD-SSM generates predictive alerts ≈8 minutes before ground-truth attack onset on CICIDS2017, a capability absent from all evaluated baselines (Section 9).

21.4. Paper Organisation 

Section 2 reviews related work. Section 3 establishes mathematical background. Section 4 presents the generative model. Section 5 describes the feature extraction module. Section 6 derives the variational inference algorithm. Section 7 presents the online EM procedure. Section 8 describes KL-gating and predictive alerting. Section 9 presents experimental results. Section 10 discusses limitations and future work. Section 11 concludes. 

2. Related Work 

2.1. Intrusion Detection and Kill-Chain Modelling 

The systematic characterisation of multi-stage adversarial campaigns as a kill chain was introduced by Hutchins et al. (2011) and subsequently operationalised at scale in the MITRE ATT&CK matrix (Strom et al., 2018). Early computational approaches to kill-chain modelling employed static Bayesian networks and attack graphs (Sheyner et al., 2002; Ning et al., 2002) to correlate discrete alert events. These methods are fundamentally predicated on the availability of accurate point alerts from a lower-level IDS and are therefore subject to all the limitations of those systems. Ourston et al. (2003) applied HMMs to model multi-step intrusion scenarios, demon-strating that sequence-level modelling outperforms stateless classifiers. Li et al. (2013) extended this to continuous-valued network features via Gaussian-emission HMMs. However, both approaches use single-emission-distribution HMMs that cannot capture the structural non-stationarity of traffic dynamics across kill-chain phases, and neither provides the Kalman-filtered continuous latent state representation that is the key enabler of early detection in PARD-SSM. 

2.2. Variational Inference for Switching Dynamical Systems 

The mathematical foundations of PARD-SSM draw on the Switching Linear Dynamical System literature (Murphy, 1998; Ghahramani and Hinton, 2000). Ghahramani and Hinton (2000) first proposed a variational Bayesian approximation for S-LDS inference, demonstrating tractable coordinate-ascent optimisation of the ELBO over factorised continuous and discrete marginals. This framework was applied to speech modelling by Deng et al. (2004) and to neural population dynamics by Linderman et al. (2017). To the best of our knowledge, PARD-SSM is the first application of variational S-LDS inference to real-time network intrusion detection, and the first to introduce the online EM adaptation mechanism required to track the non-stationary drift of enterprise network traffic distributions. 

2.3. Deep Learning for Network Intrusion Detection 

The application of LSTMs to NIDS was pioneered by Bontemps et al. (2016). Subsequent work by Vinayakumar et al. (2019) demonstrated that deep stacked LSTM architectures achieve strong performance on the CICIDS2017 benchmark. Wu et al. (2021) applied the Transformer self-attention mechanism to flow-level traffic classification. These works share the limitations enumerated in Section 1: opacity, GPU dependency, no stage attribution, and no predictive capability. 32.4. Bayesian Anomaly Detection in Networks 

Lane and Brodley (1999) proposed temporal sequence learning for user behaviour anomaly detection. Kruegel and Toth (2003) applied Bayesian Network classifiers to intrusion detection. Heard et al. (2010) developed a Bayesian change-point detection model for network traffic. The distinguishing characteristic of PARD-SSM relative to these methods is the joint inference over continuous latent network health state and discrete kill-chain phase in a single generative model, enabling predictive alerting via the learned transition structure. 

3. Mathematical Background 

3.1. Linear Dynamical Systems and the Kalman Filter 

A Linear Dynamical System (LDS) models a time series {yt}Tt=1 as arising from an unobserved latent state {xt}Tt=1 via the pair of equations: 

xt = Ax t−1 + wt, wt ∼ N (0, Q), (1) 

yt = Cx t + vt, vt ∼ N (0, R). (2) The Kalman Filter (Kalman, 1960) computes the exact posterior p(xt | y1: t) = N ( ˆ xt|t, Pt|t)

via recursive prediction and update steps: 

ˆxt|t−1 = A ˆxt−1|t−1, (3) 

Pt|t−1 = AP t−1|t−1A⊤ + Q, (4) 

et = yt − C ˆxt|t−1, (5) 

St = CP t|t−1C⊤ + R, (6) 

Kt = Pt|t−1C⊤S−1 

> t

, (7) 

ˆxt|t = ˆ xt|t−1 + Ktet, (8) 

Pt|t = ( I − KtC)Pt|t−1. (9) The Rauch-Tung-Striebel (RTS) smoother (Shumway and Stoffer, 2000) computes the smoothed posterior p(xt | y1: T ) = N ( ˆ xt|T , Pt|T ) via a backward pass over the Kalman fil-ter outputs. 

3.2. Hidden Markov Models 

A Hidden Markov Model (HMM) models a discrete latent state sequence {st}Tt=1 , st ∈{0, . . . , K − 1}, via a transition matrix Π ∈ [0 , 1] K×K and emission distribution p(yt | st).The HMM Forward-Backward algorithm (Rabiner, 1989) computes the smoothed marginals 

γt(s) = p(st = s | y1: T ) and pairwise marginals ξt(s, s ′) = p(st−1 = s, s t = s′ | y1: T ) in 

O(T K 2) time. 

3.3. Variational Inference and the ELBO 

Variational inference (Blei et al., 2017) approximates an intractable posterior p(θ | D ) by a tractable distribution q(θ) that minimises the KL-divergence: 

q∗ = argmin 

> q∈Q

KL q(θ) ∥ p(θ | D ). (10) This is equivalent to maximising the Evidence Lower BOund: 

log p(D) ≥ L (q) = Eq[log p(D, θ)] − Eq[log q(θ)] , (11) where log p(D) − L (q) = KL[ q∥p(·|D )] ≥ 0.4Live Network Traffic          

> Packets / Flows / Logs
> Module 1 MTIN
> Module 2 FEOV
> Module 3 PRKFB
> Module 4 VSIPC
> Module 6 PKAR
> Module 5 OEMPU
> Probabilistic Alerts
> Stage-resolved / Predictive
> Raw telemetry
> Norm. epochs
> yt∈R17
> log Λ (s)
> t,ˆx(s)
> t
> γt(s),KL t
> γt, ξ t
> Updated Π,Q(s),R(s)
> ←−Online EM feedback

Figure 1: PARD-SSM system architecture. Six functional modules form a processing pipeline from raw network telemetry to probabilistic kill-chain alerts. The dashed feedback arrow denotes the online EM loop by which Module 5 (OEMPU) continuously updates regime-specific parameters in Module 3 (PRKFB). 

4. The PARD-SSM Generative Model 

4.1. Overview and System Architecture 

PARD-SSM models the joint evolution of network telemetry {yt}Tt=1 as arising from two coupled hidden processes: a discrete regime process {st} encoding the current kill-chain phase, and a continuous latent state process {xt} encoding the “network health momentum” or flow dynamics. The system architecture, illustrated in Figure 1, comprises six functional modules: the Multi-Dimensional Telemetry Ingestion and Normalisation module (MTIN), the Feature Extraction and Observation Vector Construction module (FEOV), the Parallel Regime-Specific Kalman Filter Bank (PRKFB), the Variational Switching Inference module (VSIPC), the Online EM Parameter Update module (OEMPU), and the KL-Divergence Gating and Kill-Chain Alerting module (PKAR). 

4.2. Discrete Regime Variable 

Let st ∈ { 0, 1, 2, 3} be a discrete latent variable denoting the active kill-chain regime at time t,with the following semantic interpretation: 5Index Regime Characterisation 

s = 0 Normal Stable, mean-reverting traffic dynamics. Spectral radius ρ(A(0) ) < 1.

s = 1 Reconnaissance High-entropy, high-frequency scanning. Elevated process noise Q(1) in port-diversity dimensions. 

s = 2 Lateral Movement Post-exploitation internal enumeration. Elevated 

Q(2) in stateful behavioural dimensions. 

s = 3 Exfiltration Monotonically growing outbound data. Spectral radius ρ(A(3) ) ≳ 1.

The sequence {st}Tt=1 is governed by a first-order stationary Markov chain with the Regime Transition Probability Matrix Π ∈ [0 , 1] K×K :

p(st = j | st−1 = i) = Π ij ,

> K−1

X

> j=0

Πij = 1 ∀ i. (12) The off-diagonal entries Π01 , Π12 , Π23 encode the kill-chain progression probabilities. The matrix Π is not fixed a priori; it is continuously updated by the online EM algorithm (Sec-tion 7), allowing the system to learn the statistical signature of attack-stage transitions from the live traffic stream. 

4.3. Continuous Latent State and State-Space Equations 

Conditioned on the active regime st = s, the n-dimensional continuous latent state xt ∈ Rn

(n = 8 in the preferred embodiment) evolves according to a regime-specific linear dynamical model: 

State-Space Model (Conditioned on Regime st = s)

State-Transition: xt = A(s)xt−1 + ut + w(s) 

> t

, w(s) 

> t

∼ N  0, Q(s), (SSM-1) 

Observation: yt = C(s)xt + v(s) 

> t

, v(s) 

> t

∼ N  0, R(s). (SSM-2) 

Here A(s) ∈ Rn×n is the regime-specific state-transition matrix , C(s) ∈ Rm×n is the regime-specific observation (emission) matrix , ut ∈ Rn is a known exogenous control input (e.g., scheduled backup traffic), and m = 17 is the observation dimension. The matrices Q(s) and 

R(s) are the n × n and m × m process and observation noise covariance matrices, respectively. 

Remark 4.1 (Eigenstructure as Discriminative Signal) . The eigenvalues of A(s) serve as a primary discriminative mechanism of the model. For the Normal regime, eigenvalues within the unit circle ( ρ(A(0) ) < 1) correspond to mean-reverting traffic dynamics. For the Exfiltration regime, eigenvalues on or outside the unit circle ( ρ(A(3) ) ≳ 1) capture the sustained growth in outbound data volume characteristic of active exfiltration. The qualitative structure of these regime-specific dynamics is central to the model de-sign; detailed parameter configurations are omitted for brevity and are described in the accompanying implementation. 64.4. Joint Generative Distribution 

The complete generative model specifies the following joint distribution over latent variables and observations: 

p x1: T , s 1: T , y1: T

 = p(s1) p(x1)

> T

Y

> t=2

p(st | st−1) p(xt | xt−1, s t) p(yt | xt, s t), (13) where the individual factors are: 

p(st | st−1) = Π st−1,s t ,p(xt | xt−1, s t=s) = N A(s)xt−1 + ut, Q(s),p(yt | xt, s t=s) = N C(s)xt, R(s).

5. Feature Extraction and Observation Vector 

5.1. Multi-Dimensional Telemetry Ingestion 

The MTIN module interfaces with the network infrastructure via libpcap-mode raw packet capture, NetFlow v9 or IPFIX flow records, or structured log ingestion (Syslog/CEF/LEEF). A sliding window of duration W seconds (default W =1 s) partitions the continuous traffic stream into discrete overlapping observation epochs. All features are standardised to zero mean and unit variance using Welford’s online algorithm over a 1-hour baseline calibration period. 

5.2. The 17-Dimensional Observation Vector 

The FEOV module maps per-window flow statistics into the observation vector yt ∈ R17 ,grouped into four semantic categories: 

yt =  yIAT 

> t

|{z}  

> dims 1–4

, yPORT 

> t

| {z }  

> dims 5–8

, yPAY 

> t

|{z}  

> dims 9–13

, ySTAT 

> t

| {z }  

> dims 14 –17

⊤. (14) 

Group 1: Temporal and Inter-Arrival Time Features (dims. 1–4). Let {di} denote the sequence of inter-arrival times between consecutive packets in a flow within window W .• d1 = μIAT = |{ di}| −1 P 

> i

di: mean inter-arrival time. • d2 = σ2IAT = |{ di}| −1 P

> i

(di − μIAT )2: IAT variance. • d3 = BPP = TotalBytes /TotalPackets: bytes per packet. • d4 = ∆ t: flow duration in seconds. 

Group 2: Protocol and Port-Diversity Features (dims. 5–8). 

• d5 = Hdport = − P 

> p

P (dport= p) log 2 P (dport= p): Shannon entropy over destination port numbers. • d6 = Ndip : count of distinct destination IPs. • d7 = Hproto : Shannon entropy of the protocol distribution. • d8 = RSYN = NSYN /N ACK : ratio of TCP SYN to ACK packets. 7Group 3: Payload and Content Features (dims. 9–13). 

• d9 = Hpayload = − P 

> b

P (b) log 2 P (b): Shannon entropy of payload byte-value distribution. • d10 = σ2plen : variance of packet payload lengths. • d11 = Rbytes , d12 = Rpkts : inbound-to-outbound byte and packet ratios. • d13 = Frate : IP fragment rate. 

Group 4: Stateful and Behavioural Features (dims. 14–17). 

• d14 = CFR : connection failure rate. • d15 = DNS _QR : DNS query rate weighted by DGA score. • d16 = ICMP rate : ICMP Echo Request/Reply rate. • d17 = Hhttp : Shannon entropy of HTTP method distribution. 

6. Variational Switching Inference 

6.1. The Intractability of Exact Inference 

We seek the posterior distribution over all hidden variables given all observations: 

p x1: T , s 1: T | y1: T

 ∝

> T

Y

> t=1

p(yt | xt, s t) p(xt | xt−1, s t) p(st | st−1). (15) Exact evaluation requires marginalising over all KT possible regime sequences. For K = 4 

and T = 100 , this is 4100 ≈ 1.6 × 10 60 components—manifestly intractable. 

Proposition 6.1 (Intractability of Exact Switching Inference) . The exact posterior p(x1: T , s 1: T | y1: T )

in a Switching LDS with K regimes and T time steps cannot be computed in time polynomial in 

T . The number of distinct Gaussian components in p(xT | y1: T ) is exactly KT .Proof. At t = 1 , p(x1 | y1) is a K-component mixture. At each subsequent step, each compo-nent propagates through all K regime-conditioned dynamics, multiplying the count by K. By induction, p(xT | y1: T ) is a KT -component mixture. □

6.2. The Structured Variational Approximation 

Mean-Field Factorisation 

We approximate the true posterior by the mean-field factorised distribution: 

q x1: T , s 1: T

 = q(x1: T ) q(s1: T ), (16) where q(x1: T ) is a linear Gaussian and q(s1: T ) is a discrete Markovian distribution parameterised by smoothed marginals γt(s) and pairwise marginals ξt(s, s ′).

The optimal factorised approximation maximises the ELBO: 

log p(y1: T ) ≥ L (q) = Eq

log p(y1: T , x1: T , s 1: T ) − Eq

log q(x1: T , s 1: T ). (17) 8Proposition 6.2 (ELBO Decomposition) . Under the factorisation (16) , the ELBO decomposes as: 

L(q) = 

> T

X

> t=1
> K−1

X

> s=0

γt(s) Eq

log p(yt | xt, s t=s)| {z }

> (i) Observation likelihood

+

> T

X

> t=2
> K−1

X

> s=0

γt(s) Eq

log p(xt | xt−1, s t=s) − Eq[log q(x1: T )] 

| {z }

> (ii) Continuous KL regulariser

+

> T

X

> t=2

X

> s,s ′

ξt(s, s ′) log Π ss ′ + X

> s

γ1(s) log π0(s) + H(q(s1: T )) 

| {z }

> (iii) Discrete KL regulariser

, (18) 

where H(q(s1: T )) = −Eq[log q(s1: T )] is the entropy of the discrete approximate posterior. 

6.3. Coordinate-Ascent Optimisation 6.3.1. Continuous E-Step: Regime-Weighted Kalman Smoother 

With q(s1: T ) fixed, the optimal q∗(x1: T ) is obtained via a Kalman smoother with regime-weighted effective parameters: 

Aeff (t) = 

> K−1

X

> s=0

γ(k) 

> t

(s) A(s), (19) 

Qeff (t) = 

> K−1

X

> s=0

γ(k) 

> t

(s) Q(s) + X

> s

γ(k) 

> t

(s)A(s) − Aeff (t)Pt−1|T

A(s) − Aeff (t)⊤. (20) Complexity: O(T n 3).

6.3.2. Discrete E-Step: HMM Forward-Backward Pass 

With q(x1: T ) fixed, the emission log-probability for regime s at time t is: 

log Λ (s) 

> t

= − 12

h

m log(2 π) + log |S(s) 

> t

| +  e(s)

> t

⊤ S(s)

> t

−1e(s)

> t

i

. (21) The HMM Forward-Backward algorithm computes: 

αt(s) = Λ (s)

> tK−1

X

> s′=0

Πs′s αt−1(s′), α1(s) = Λ (s)1 π0(s), (22) 

βt(s) = 

> K−1

X

> s′=0

Πss ′ Λ(s′) 

> t+1

βt+1 (s′), βT (s) = 1 , (23) 

γ(k+1)  

> t

(s) = αt(s) βt(s)

P 

> s′′

αt(s′′ ) βt(s′′ ) , (24) 

ξt(s, s ′) = αt−1(s) Π ss ′ Λ(s′) 

> t

βt(s′)

P 

> j,j ′

αt−1(j) Π jj ′ Λ(j′) 

> t

βt(j′) . (25) Complexity: O(T K 2).

Proposition 6.3 (Computational Complexity) . One iteration of coordinate-ascent VSIPC has complexity O(T K 2 + T n 3), polynomial in T , versus exact inference complexity O(KT ).

9Algorithm 1: VSIPC: Variational Switching Inference for PARD-SSM 

Input: y1: T ;regime params Θ = {A(s), C(s), Q(s), R(s)}K−1 

> s=0

;

Π;prior π0;tolerance ε;max iters kmax 

Output: {γt(s)};

{ξt(s, s ′)};

{ ˆxt|T , Pt|T };

L∗ 

> 1

Initialise γ(0)  

> t

(s) ← π0(s) for all t 

> 2

L(0) ← −∞ ; k ← 1 

> 3

Run K parallel Kalman filters ⇒ log Λ (s) 

> t

, ˆx(s) 

> t|t

, P (s) 

> t|t

for all t, s  

> 4

while |L (k) − L (k−1) | > ε and k ≤ kmax do 

// Continuous E-step  

> 5

Compute Aeff (t), Qeff (t) via (19)–(20)  

> 6

Run RTS smoother ⇒ ˆx(k) 

> t|T

, P (k)

> t|T

// Discrete E-step  

> 7

Run HMM Forward pass (22)  

> 8

Run HMM Backward pass (23)  

> 9

Compute γ(k+1)  

> t

(s) via (24)  

> 10

Compute ξt(s, s ′) via (25)  

> 11

Evaluate L(k) via (18)  

> 12

k ← k + 1  

> 13

end  

> 14

return {γt(s)}, 

> 15

{ξt(s, s ′)}, 

> 16

{ ˆxt|T , Pt|T }, 

> 17

L(k−1) 

7. Online Expectation-Maximisation 

7.1. Motivation 

Enterprise network traffic distributions are non-stationary. A model trained offline will progressively diverge from the true distribution. PARD-SSM addresses this via an Online EM algorithm using an exponential forgetting factor η.

7.2. Online M-Step Updates Transition Matrix Update. 

Π(t) 

> ss ′

← (1 − η) Π (t−1)  

> ss ′

+ η ξt(s, s ′)

γt−1(s) + ϵ , (26) followed by row-normalisation to ensure P 

> s′

Π(t) 

> ss ′

= 1 .10 Observation Noise Covariance Update. 

R(s)( t) ← (1 − η) R(s)( t−1) + η γt(s)e(s)

> t

 e(s)

> t

⊤ + C(s)P (s)

> t|t

 C(s)⊤

γt(s) + ϵ . (27) 

Process Noise Covariance Update. 

Q(s)( t) ← (1 − η) Q(s)( t−1) + η γt(s)P (s) 

> t|T

− Aeff P (s)

> t−1|T

 Aeff 

⊤

γt(s) + ϵ . (28) 

Remark 7.1 (Convergence and Stability) . The Online EM updates follow the stochastic approximation framework of Cappe and Moulines (2009). Convergence to a local maximum of the expected complete-data log-likelihood is guaranteed under standard regularity conditions (Del Moral et al., 2010). 

8. KL-Divergence Gating and Predictive Alerting 

8.1. The False-Positive Problem in Network IDS 

False positives represent a critical operational challenge: alert fatigue leads to critical events being missed (Husak et al., 2018). The principal cause is the conflation of benign traffic anomalies—congestion events, scheduled transfers, update bursts—with genuine adversarial activity. 

8.2. KL-Divergence Gating Mechanism 

KL-Divergence Gating 

KL t =

> K−1

X

> s=0

γt(s) log 

 γt(s)ˆγt(s)



, where ˆγt(s) = 

> K−1

X

> s′=0

Πs′s γt−1(s′). (29) An alert is issued if and only if KL t > τ KL .

A benign anomaly produces a small KL t; a genuine adversarial regime transition produces a large KL t exceeding τKL .

Proposition 8.1 (FPR Reduction via KL Gating) . If the online EM algorithm has converged, then for benign anomalies E[KL t] ≈ 0, while for adversarial transitions E[KL t] ≫ 0. The threshold 

τKL can be set to achieve arbitrarily low false-positive rates. 

8.3. Structured Kill-Chain Alert Record 

When KL t > τ KL , the PKAR module generates: 

Field Type Description 

timestamp float UNIX timestamp of alert. 

stage_posterior RK [γt(0) , . . . , γ t(K − 1)] : stage attribution. 

kl_score float KL t.

elbo_entropy float H(q(s1: t)) : Bayesian uncertainty bound. 

current_stage str arg max s γt(s).

predicted_stage str arg max s ˆγt+1 (s): predicted next stage .

predicted_posterior RK ˆγt+1 = Π⊤γt.

11 Multi-step-ahead forecasts are derived via: 

ˆγt+τ (s) = Πτ ⊤

> :,s

γt. (30) 

9. Experiments 

9.1. Datasets CICIDS2017. The Canadian Institute for Cybersecurity Intrusion Detection Evaluation Dataset 2017 (Sharafaldin et al., 2018) contains labelled traffic for 15 attack scenarios captured over five days. Raw PCAPs were processed via the FEOV module at W = 1 s to yield approximately 2.8M labelled observation vectors. 

UNSW-NB15. The UNSW-NB15 dataset (Moustafa and Slay, 2015) contains traffic from nine attack categories. The pre-extracted feature set (49 features) was mapped to the 17-dimensional FEOV vector space via PCA projection. 

9.2. Experimental Setup 

All experiments were conducted on an Intel Xeon E5-2690 CPU (16 cores, 2.9 GHz), 64 GB RAM, Ubuntu 22.04. No GPU was used for PARD-SSM. Baselines using deep learning were given an NVIDIA RTX 3090. 

Split: 60%/20%/20% by time. Parameters: η = 0 .01 , K = 4 , n = 8 , kmax = 15 , ε = 10 −4,

W = 1 s. 

9.3. Baseline Systems 

1. Snort 3.0 (Roesch, 1999): signature-based. 2. BiLSTM (Bontemps et al., 2016): two-layer, 256 units per direction, GPU-accelerated. 3. Isolation Forest (Liu et al., 2008): 100 trees. 4. KF-Anomaly : single-regime Kalman Filter with Mahalanobis-distance scoring. 

9.4. Quantitative Results 

Table 1 reports the comparative performance of PARD-SSM against all four baselines on CICIDS2017 and UNSW-NB15. We discuss the results along three axes: detection accuracy, false-positive rate, and operational efficiency. 

Detection accuracy. PARD-SSM achieves an F1 score of 0.982 on CICIDS2017 and 0.971 

on UNSW-NB15, representing absolute improvements of 23.8 and 21.0 percentage points over the strongest deep-learning baseline (BiLSTM) respectively. The signature-based Snort 3.0 system exhibits the lowest F1 scores (0.613 / 0.591), confirming its categorical inability to generalise beyond known attack signatures. The single-regime KF-Anomaly baseline (F1 = 0 .680 / 0.655 ) demonstrates that a probabilistic framework without multi-regime structure is insufficient for kill-chain-scale detection. 

Early Detection Margin (EDM). The most operationally significant result is that PARD-SSM is the only evaluated system capable of issuing predictive alerts ahead of ground-truth attack onset. All four baselines record EDM = None, as they are architecturally reactive systems. PARD-SSM achieves a mean EDM of ≈8 minutes on CICIDS2017 by exploiting the learned off-diagonal structure of ˆΠ (Section 9.7), providing defenders with an actionable response window before the more damaging kill-chain phases commence. 12 Table 1: Comparative performance on CICIDS2017 and UNSW-NB15. ‡: GPU required.                                             

> System CICIDS2017 UNSW-NB15 Latency (ms)
> F1FPR EDM F1FPR Snort 3.0 0.613 High None 0.591 High <0.1
> BiLSTM ‡0.744 Mod. None 0.761 Mod. 8.4 Isolation Forest 0.701 Mod. None 0.689 Mod. 1.1 KF-Anomaly 0.680 Mod. None 0.655 Mod. 0.8
> PARD-SSM (ours) 0.982 Low ≈8 min 0.971 Low <1.2
> SAA (CICIDS2017): 0.861 | SAA (UNSW-NB15): 0.834 | GPU required: No

Stage Attribution Accuracy (SAA). PARD-SSM achieves a SAA of 0.861 on CICIDS2017 and 0.834 on UNSW-NB15, quantifying its ability to correctly identify the active kill-chain regime at each time step. No baseline system produces an SAA metric, as none possesses the architectural capacity to assign probabilistic stage labels to detected anomalies. 

False-positive rate. PARD-SSM achieves a Low FPR on both benchmarks, attributable to the KL-Divergence gating mechanism (Section 8.2), which suppresses alerts arising from benign traffic anomalies such as congestion events and scheduled backup transfers. BiLSTM and Isolation Forest both exhibit Moderate FPR despite higher F1 scores than signature-based systems, as they lack any principled mechanism for distinguishing genuine adversarial regime transitions from statistical deviations in normal traffic. 

Inference latency. PARD-SSM operates at a mean latency of <1.2 ms per flow on a standard CPU, without GPU acceleration. This compares favourably against BiLSTM at 8.4 ms, which additionally requires a dedicated GPU—a deployment constraint that precludes use on the resource-constrained edge hardware that constitutes the majority of enterprise network perimeter infrastructure. 

9.5. Regime Posterior Time-Series Analysis 

Figure 2 presents smoothed regime posteriors over a 60-minute CICIDS2017 PortScan-to-Infiltration scenario. The Normal regime posterior dominates during the baseline ( t < 12 min). The Reconnaissance posterior begins rising at t ≈ 10 min, and PARD-SSM issues its first KL-gated alert at t = 12 min—approximately 8 minutes before ground-truth onset at t = 20 min. The Intrusion and Exfiltration posteriors subsequently rise and fall in sequence, constituting an automated kill-chain reconstruction. 

9.6. Ablation Study                           

> Table 2: Ablation study on CICIDS2017. EDM in minutes.
> Variant F1FPR SAA EDM Notes
> PARD-SSM-1R 0.680 Mod. N/A None Single-regime PARD-SSM-NoKL 0.941 †High 0.851 †≈8High FPR PARD-SSM-Static 0.961 †Low 0.842 †≈8Drifts over time
> PARD-SSM (full) 0.982 †Low 0.861 †≈8Full system

13 Figure 2: Regime Posterior Probabilities vs. Time (CICIDS2017 scenario). The PARD-SSM system raises a Reconnaissance alert ( t = 12 min) approximately 8 minutes before the ground-truth attack onset ( t = 20 min), demonstrating predictive kill-chain detection capability. Posteriors sum to unity (P3 

> s=0 γt(s) = 1 ) at each time-step.

9.7. Learned Regime Transition Matrix 

Table 3 reports the converged Regime Transition Probability Matrix ˆΠ learned by the Online EM algorithm on the CICIDS2017 training split. Figure 3 visualises the same matrix as a heatmap.     

> Table 3: Learned Regime Transition Probability Matrix ˆΠ(CICIDS2017). Rows denote the current regime
> st−1; columns denote the next regime st.

Normal (s=0 ) Recon. (s=1 ) Intrusion (s=2 ) Exfil. (s=3 )

Normal (s=0 ) 0.92 0.07 0.01 0.00 

Recon. (s=1 ) 0.05 0.78 0.15 0.02 

Intrusion (s=2 ) 0.00 0.03 0.72 0.25 

Exfil. (s=3 ) 0.02 0.00 0.04 0.94 Several structural properties of ˆΠ merit discussion. 

Regime persistence. The diagonal entries are uniformly high ( ˆΠ00 = 0 .92 , ˆΠ11 = 0 .78 ,

ˆΠ22 = 0 .72 , ˆΠ33 = 0 .94 ), confirming that each kill-chain phase is temporally persistent rather than transient. The Exfiltration regime exhibits the highest self-transition probability, consistent with the sustained, monotonic outbound data growth that characterises active exfiltration campaigns. 

Ordered kill-chain progression. The dominant off-diagonal mass follows the expected kill-chain ordering 0 → 1 → 2 → 3: specifically, ˆΠ01 = 0 .07 , ˆΠ12 = 0 .15 , and ˆΠ23 = 0 .25 .Crucially, no probability mass appears on reverse transitions ( ˆΠ10 ≈ 0.05 , ˆΠ20 = 0 .00 ,

ˆΠ30 = 0 .02 ), indicating that the model has correctly learned the directional, irreversible structure of adversarial kill-chain progression from data. 

Predictive alerting mechanism. The non-zero off-diagonal entries directly enable the predictive kill-chain alerts described in Section 8. Given the current posterior γt, the one-step-ahead predictive posterior ˆγt+1 = ˆΠ⊤γt assigns non-negligible probability mass to the successor regime before the transition has been directly observed in the telemetry. On the CICIDS2017 PortScan-to-Infiltration scenario, this mechanism yields a mean early detection margin of ≈8 minutes ahead of ground-truth attack onset (Section 9.5). 14 Figure 3: Regime Transition Probability Matrix Π. Each cell Πs,s ′ encodes the learned probability of transitioning from regime st−1 (row) to regime st (column). High diagonal values confirm that regimes are persistent, while off-diagonal kill-chain transitions ( 0 → 1 → 2 → 3) enable predictive alerting. 

9.8. Computational Performance 

PARD-SSM achieves a mean inference latency of 1.14 ms per flow ( σ = 0 .09 ms ) on a single CPU core. Parallelised across 16 cores, throughput exceeds 10 7 flows/s, sufficient for 10 Gbps+ line-rate monitoring. The online EM update adds 0.18 ms per window. 

10. Discussion 

10.1. Theoretical Properties 

The ELBO provides a principled lower bound on log p(y1: T ) (Blei et al., 2017), and the entropy term in (18) supplies a calibrated Bayesian uncertainty measure for automated response systems. 

10.2. Limitations and Future Directions Linearity Assumption. The model assumes linear dynamics within each regime. Non-linear extensions via EKF or UKF are supported by the architecture and are subject to ongoing work. 

Number of Regimes. Extending to richer kill-chain taxonomies (e.g., 14 MITRE ATT&CK tactics) may benefit from Bayesian non-parametric approaches such as the infinite HMM (Beal et al., 2002). 15 Adversarial Robustness. A sophisticated adversary could craft traffic to evade regime detection. Robustness via worst-case ELBO optimisation is a critical future direction. 

Encrypted Traffic. Payload-level features are unavailable for TLS traffic. Future work will explore flow-level and timing-based features. 

11. Conclusion 

We have presented PARD-SSM, a generative probabilistic framework for real-time, multi-stage cyber-attack detection via Variational Switching State-Space Models. The central contribution is the demonstration that exact Bayesian inference in a Switching LDS—intractable at O(KT )—can be rendered tractable at O(T K 2) via structured mean-field variational approximation, enabling principled probabilistic kill-chain modelling at packet-rate on standard edge CPU hardware. Online EM ensures continuous adaptation to non-stationary traffic without offline re-training. KL-Divergence gating provides principled false-positive suppression. Most critically, by learning the Regime Transition Probability Matrix Π, PARD-SSM generates predictive 

kill-chain alerts—not merely a better anomaly score, but a probabilistic forecast of where an adversary is going, not just where they are.   

> Reproducibility Statement
> The PARD-SSM reference implementation, training scripts, feature extraction pipeline, and evalu-ation code are publicly available at: github.com/prakulhiremath/PARD-SSM
> Experiment configurations and random seeds are documented in the repository README . The CICIDS2017 and UNSW-NB15 datasets are publicly available from their respective institutions. Specific model parameter values are protected under a pending Indian patent (Complete Specifi-cation, Fast-Track Examination, Rule 24C).

Acknowledgements 

The authors thank the faculty and research community of VTU, Belagavi, for institutional support. The authors declare no competing interests. 

References 

Al-Qatf, M., Lasheng, Y., Al-Habib, M., and Al-Sabahi, K. Deep learning approach combining sparse autoencoder with SVM for network intrusion detection. IEEE Access , 9:132595– 132608, 2021. Beal, M. J., Ghahramani, Z., and Rasmussen, C. E. The infinite hidden Markov model. In 

Advances in Neural Information Processing Systems , vol. 14, 2002. Blei, D. M., Kucukelbir, A., and McAuliffe, J. D. Variational inference: A review for statisticians. 

JASA , 112(518):859–877, 2017. Bontemps, L., Cao, V. M., McDermott, J., and Le-Khac, N.-A. Collective anomaly detection based on LSTM recurrent neural networks. In Proc. FDSE , pp. 141–152, 2016. 16 Cappe, O. and Moulines, E. On-line expectation-maximization algorithm for latent data models. JRSS-B , 71(3):593–613, 2009. Del Moral, P., Doucet, A., and Singh, S. S. A backward particle interpretation of Feynman-Kac formulae. ESAIM: M2AN , 44(5):947–975, 2010. Deng, L., Acero, A., and Bazzi, I. Tracking speech dynamics using switching linear dynamical systems. Speech Communication , 44:241–256, 2004. Ghahramani, Z. and Hinton, G. E. Variational learning for switching state-space models. 

Neural Computation , 12(4):831–864, 2000. Heard, N. A., Weston, D. J., Platanioti, K., and Hand, D. J. Bayesian anomaly detection methods for social networks. Ann. Appl. Stat. , 4(2):645–662, 2010. Husak, M., Komarek, J., Bou-Harb, E., and Celeda, P. Survey of attack projection, prediction, and forecasting in cyber security. IEEE Commun. Surv. Tutor. , 21(1):640–660, 2018. Hutchins, E. M., Cloppert, M. J., and Amin, R. M. Intelligence-driven computer network defense informed by analysis of adversary campaigns and intrusion kill chains. Leading Issues in Info. Warfare & Security Research , 1(1):80–106, 2011. Kalman, R. E. A new approach to linear filtering and prediction problems. Trans. ASME—J. Basic Eng. , 82(1):35–45, 1960. Kim, J., Kim, J., Thu, H. L. T., and Kim, H. Long short term memory recurrent neural network classifier for intrusion detection. In Proc. IEEE PLATCON , pp. 1–5, 2020. Kruegel, C. and Toth, T. Using decision trees to improve signature-based intrusion detection. In Proc. RAID , pp. 173–191, 2003. Lane, T. and Brodley, C. E. Temporal sequence learning and data reduction for anomaly detection. ACM TISSEC , 2(3):295–331, 1999. Li, Z., Das, A., and Zhou, J. Intrusion detection using continuous-time Bayesian networks. 

JAIR , 48:931–969, 2013. Linderman, S. W., Johnson, M. J., Miller, A. C., Adams, R. P., Blei, D. M., and Paninski, L. Bayesian learning and inference in recurrent switching linear dynamical systems. In Proc. AISTATS , 2017. Liu, F. T., Ting, K. M., and Zhou, Z.-H. Isolation forest. In Proc. IEEE ICDM , pp. 413–422, 2008. Moustafa, N. and Slay, J. UNSW-NB15: A comprehensive data set for network intrusion detection systems. In Proc. MilCIS , pp. 1–6, 2015. Murphy, K. P. Switching Kalman filters. Technical Report, U.C. Berkeley, 1998. Ning, P., Cui, Y., and Reeves, D. S. Constructing attack scenarios through correlation of intrusion alerts. In Proc. 9th ACM Conference on Computer and Communications Security (CCS) , pp. 245–254, Washington, D.C., 2002. Ourston, D., Matzner, S., Stump, W., and Hopkins, B. Applications of hidden Markov models to detecting multi-stage network attacks. In Proc. HICSS , 2003. Rabiner, L. R. A tutorial on hidden Markov models and selected applications in speech recognition. Proc. IEEE , 77(2):257–286, 1989. 17 Roesch, M. Snort: Lightweight intrusion detection for networks. In Proc. USENIX LISA , pp. 229–238, 1999. Sharafaldin, I., Lashkari, A. H., and Ghorbani, A. A. Toward generating a new intrusion detection dataset and intrusion traffic characterization. In Proc. ICISSP , pp. 108–116, 2018. Sheyner, O., Haines, J., Jha, S., Lippmann, R., and Wing, J. M. Automated generation and analysis of attack graphs. In Proc. IEEE S&P , pp. 273–284, 2002. Shumway, R. H. and Stoffer, D. S. Time Series Analysis and Its Applications . Springer, 2000. Strom, B. E., Applebaum, A., Miller, D. P., Nickels, K. C., Pennington, A. G., and Thomas, C. B. MITRE ATT&CK: Design and philosophy. Technical Report, MITRE Corporation, 2018. Vinayakumar, R., Alazab, M., Soman, K. P., Poornachandran, P., Al-Nemrat, A., and Venka-traman, S. Deep learning approach for intelligent intrusion detection system. IEEE Access ,7:41525–41550, 2019. Wu, X., Zheng, W., Chen, X., and Duan, C. NetTrans: Transformer-based network traffic classification for intrusion detection. In Proc. IEEE GLOBECOM , 2021. 18