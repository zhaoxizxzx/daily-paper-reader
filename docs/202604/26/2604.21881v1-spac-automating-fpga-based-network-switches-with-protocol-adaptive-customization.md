---
title: "SPAC: Automating FPGA-based Network Switches with Protocol Adaptive Customization"
title_zh: SPAC：通过协议自适应定制实现基于 FPGA 的网络交换机自动化
authors: "Guoyu Li, Yang Cao, Lucas H L Ng, Alexander Charlton, Qianzhou Wang, Will Punter, Philippos Papaphilippou, Ce Guo, Hongxiang Fan, Wayne Luk, Saman Amarasinghe, Ajay Brahmakshatriya"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21881v1"
tags: ["query:netmes"]
score: 8.0
evidence: 针对自定义协议和流量模式自动化生成基于 FPGA 的网络交换机
tldr: SPAC是一个自动化生成FPGA网络交换机的框架，旨在解决不同应用（如低延迟传感器网络或高吞吐数据中心）对定制化协议和架构的需求。它通过领域特定语言（DSL）、模块化HLS组件库和设计空间搜索（DSE）引擎，实现了协议与架构的协同优化。实验表明，SPAC能显著降低资源消耗并优化延迟，为特定工作负载提供帕累托最优的交换机设计。
selection_source: fresh_fetch
motivation: 随着网络需求的多样化，传统固定架构交换机难以同时满足低延迟和高吞吐等特定应用场景下的定制化协议需求。
method: 提出SPAC框架，利用DSL进行协议架构协同设计，结合模块化HLS组件库和迹感知DSE引擎实现自动化定制与仿真。
result: "实验显示SPAC生成的交换机减少了约55%的逻辑资源使用，并在维持丢包率的同时将延迟降低了7.8%至38.4%。"
conclusion: SPAC证明了通过针对特定工作负载定制微架构和协议，可以在FPGA上实现资源效率与网络性能的最优平衡。
---

## 摘要
随着新兴应用对网络需求的分化，延迟敏感型服务要求极低的逻辑延迟，而超大规模训练和集合通信则需要持续的线速吞吐量以进行同步大批量传输。这种分化产生了对针对特定协议和特定应用流量模式定制的网络交换机的迫切需求。本文提出了 SPAC（交换机与协议自适应定制），这是一种自动化生成基于 FPGA 的网络交换机的新方法，该方法针对自定义协议和特定应用流量模式进行了协同优化。SPAC 引入了一个统一的工作流，包括用于协议-架构协同设计的领域特定语言 (DSL)、基于 HLS 的模块化自适应交换组件库，以及一个感知轨迹（trace-aware）的设计空间探索 (DSE) 引擎。通过提供多保真度仿真栈，SPAC 能够在部署前快速识别帕累托最优（Pareto-optimal）设计。我们在从延迟敏感的传感器和高频交易 (HFT) 网络到超大规模数据中心架构的一系列真实场景中，展示了 SPAC 领域特定自适应的有效性。实验结果表明，通过针对特定工作负载定制微架构和协议，SPAC 生成的设计分别减少了 55% 的 LUT 和 53% 的 BRAM 使用量。与固定架构的同类产品相比，SPAC 在各种任务中实现了 7.8% 到 38.4% 的延迟降低，同时保持了合理的资源消耗和丢包率。

## Abstract
With network requirements diverging across emerging applications, latency-critical services demand minimal logic delay, while hyperscale training and collectives require sustained line-rate throughput for synchronized bulk transfers. This divergence creates an urgent need for custom network switches tailored to specialized protocols and application-specific traffic patterns. This paper presents SPAC (Switch and Protocol Adaptive Customization), a novel approach that automates the generation of FPGA-based network switches co-optimized for custom protocols and application-specific traffic patterns. SPAC introduces a unified workflow with a domain-specific language (DSL) for protocol-architecture co-design, a library of modular HLS-based adaptive switch components, and a trace-aware Design Space Exploration (DSE) engine. By providing a multi-fidelity simulation stack, SPAC enables rapid identification of Pareto-optimal designs prior to deployment. We demonstrate the efficacy of the domain-specific adaptation of SPAC across a spectrum of real-world scenarios, spanning from latency-sensitive sensor and HFT networks to hyperscale datacenter fabrics. Experimental results show that by tailoring the micro-architecture and protocol to the specific workload, SPAC-generated designs reduce LUT and BRAM usage by 55% and 53%, respectively. Compared to fixed-architecture counterparts, SPAC delivers latency reductions ranging from 7.8% to 38.4% across various tasks while maintaining adequate resource consumption and packet drop rate.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SPAC (Switch and Protocol Adaptive Customization)**，一个旨在自动化生成定制化 FPGA 网络交换机的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现代网络应用的需求呈现极端分化：高频交易（HFT）追求极低延迟，而超大规模 AI 训练则需要高吞吐量的同步数据传输。传统的固定架构交换机（如基于 PISA 架构的 P4 交换机）虽然具有一定的协议可编程性，但其底层硬件微架构（如缓冲区管理、调度算法）是固定的，无法针对特定流量模式（如突发流量或小包流量）进行优化。
**核心问题：** 如何打破协议与硬件架构之间的耦合，实现针对特定应用协议和流量模式的“协议-架构协同设计”自动化？

### 2. 方法论
SPAC 提出了一个端到端的自动化工作流，包含以下三个核心组件：
*   **SPAC DSL（领域特定语言）：** 允许开发者定义自定义协议格式、进行语义绑定（指定路由键等），并配置架构策略（如缓冲区策略、哈希策略）。它作为“单一真理来源”，同时生成硬件 HLS 代码、软件驱动和仿真模型。
*   **模块化 HLS 交换机模板：** 提供了一系列可组合的 HLS 模块，包括协议感知解析器（Parser）、转发表（支持全查找或多行哈希）、虚拟输出队列（VOQ）缓冲区（支持 N*N 或共享内存）以及多种调度器（RR, iSLIP, EDRRM）。
*   **设计空间探索（DSE）引擎：** 采用**渐进式约束满足策略**。
    1.  **静态剪枝：** 根据线速要求剔除无法满足时序约束的架构。
    2.  **粗粒度分析：** 利用轻量级统计代理模型（Statistical Surrogate Model）快速评估延迟分布。
    3.  **统计调优：** 基于流量轨迹（Trace）确定最优缓冲区大小，并在 ns-3 仿真中验证。

### 3. 实验设计
*   **应用场景/数据集：**
    *   **HFT：** 高频交易，特征是极低延迟和小包突发。
    *   **RL (All-Reduce)：** 分布式强化学习，特征是高吞吐和 Incast（多对一）拥塞。
    *   **DataCenter：** 模拟阿里巴巴集群轨迹的微服务依赖。
    *   **Industry：** 医疗废物焚烧炉的 SCADA 工业控制流量。
    *   **Underwater：** 水下机器人通信，协议极度压缩（有效载荷仅 2 字节）。
*   **Benchmark 与对比方法：**
    *   **RTL 级基准：** GCQ, SMiSLIP, Hipernetch（高性能手动优化 RTL 交换机）。
    *   **P4 级基准：** VitisNetP4, P4-to-FPGA, P4THLS（基于 P4 的 FPGA 映射工具）。
    *   **自身对比：** SPAC Ethernet（通用以太网配置） vs. SPAC Optimized（针对特定场景定制）。

### 4. 资源与算力
*   **硬件平台：** 使用了 **AMD Alveo U45N (xcu26)** 网络加速卡，主机配备 AMD EPYC 9335 32 核处理器。
*   **开发工具：** Vitis HLS 2023.2，目标频率 350MHz。
*   **算力说明：** 论文未提及具体的“训练”时长（因为不是深度学习模型），但强调了其统计代理模型比周期精确仿真快几个数量级，能在秒级处理流量轨迹并完成 DSE 搜索。

### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **资源与性能对比：** 对比了不同端口数（2-16 端口）下的 LUT、FF、BRAM 使用量及频率。
*   **可扩展性分析：** 测试了端口增加对延迟和吞吐量的影响。
*   **领域自适应实验：** 针对上述 5 种真实场景分别进行了定制化优化实验。
*   **准确性验证：** 对比了代理模型预测值与实际 HLS 综合后的资源/延迟误差（MAPE 在 0.4%~7.4% 之间）。
实验设计覆盖了从底层硬件指标到上层应用表现的完整链路，对比对象既包含学术界前沿也包含工业界工具，实验较为充分且客观。

### 6. 主要结论与发现
*   **资源优化：** 通过协议压缩和架构裁剪，定制化设计相比通用以太网设计可减少 **55% 的 LUT** 和 **53% 的 BRAM**。
*   **性能提升：** 在不同任务中，SPAC 生成的交换机将平均延迟降低了 **7.8% 至 38.4%**。
*   **架构敏感性：** 实验发现不同的调度算法对流量模式高度敏感（如 iSLIP 适合均匀流量，EDRRM 适合突发流量），证明了架构定制的必要性。
*   **HLS 竞争力：** 尽管使用 HLS 开发，但 SPAC 在性能和资源效率上达到了与手动优化 RTL 相当甚至更优的水平。

### 7. 优点
*   **端到端自动化：** 实现了从高层 DSL 描述到线速硬件生成的全自动流程，极大降低了 FPGA 网络设备的开发门槛。
*   **协议-架构协同：** 不仅仅是改变协议解析，还能根据流量特征自动选择最匹配的调度器和缓冲区结构。
*   **多保真度仿真：** 结合了快速统计模型和高精度 ns-3 仿真，解决了定制协议缺乏仿真支撑的痛点。

### 8. 不足与局限
*   **HLS 非确定性：** 论文提到 HLS 综合结果存在一定的波动，可能影响 DSE 预测的极端准确性。
*   **硬件平台限制：** 目前主要针对 FPGA 平台，尚未讨论如何扩展到 ASIC 或其他可编程 ASIC 芯片。
*   **内核复杂度：** 虽然支持注入自定义计算内核（In-Network Computing），但对于极其复杂的有状态逻辑，如何自动保证线速处理仍有待深入探讨。
*   **扩展规模：** 实验主要集中在 16 端口以内，对于超大规模交换矩阵的布线拥塞问题讨论较少。

（完）
