Title: Multi-Agent LLM Governance for Safe Two-Timescale Reinforcement Learning in SDN-IoT Defense

URL Source: https://arxiv.org/pdf/2604.01127v1

Published Time: Thu, 02 Apr 2026 01:28:24 GMT

Number of Pages: 20

Markdown Content:
> 1

# Multi-Agent LLM Governance for Safe Two-Timescale Reinforcement Learning in SDN-IoT Defense 

Saeid Jamshidi, Negar Shahabi, Foutse Khomh, Carol Fung, and Mohammad Hamdaqa 

Abstract —Software-Defined Networking (SDN) is increasingly adopted to secure Internet-of-Things (IoT) networks due to its centralized control and programmable forwarding. However, SDN-IoT defense is inherently a closed-loop control problem in which mitigation actions impact controller workload, queue dynamics, rule-installation delay, and future traffic observations. Aggressive mitigation may destabilize the control plane, degrade Quality of Service (QoS), and amplify systemic risk. Existing learning-based approaches prioritize detection accuracy while ne-glecting to model controller coupling and optimize short-horizon Reinforcement Learning (RL) objectives without structured, auditable policy evolution. This paper introduces a self-reflective two-timescale SDN-IoT defense solution that separates fast de-centralized mitigation from slow policy governance. At the fast timescale, per-switch Proximal Policy Optimization (PPO) agents perform controller-aware mitigation under explicit safety con-straints and action masking. At the slow timescale, a multi-agent Large Language Model (LLM) governance engine generates machine-parsable edits to a global policy constitution Π, encoding admissible action masks, controller-aware safety thresholds, and reward priorities. Policy updates are formalized as structured deltas ∆Π , validated through targeted stress campaigns, and deployed only if non-regression and hard-safety conditions are met, ensuring fail-closed, auditable evolution without modifying RL parameters. Evaluation is conducted as a closed-loop systems study under heterogeneous IoT traffic and adversarial stress, explicitly modeling controller service-rate limits and delayed actuation. Results show that the proposed solution improves Macro-F1 by 9.1% over unconstrained PPO and by 15.4% over static threshold-based mitigation. Worst-case agent degradation is reduced by 36.8%, controller backlog peaks decrease by 42.7%, and RTT p95 inflation remains below 5.8% during high-intensity attacks. No oscillatory instability and controller collapse are observed across independent runs. Governance-driven policy evolution converges within five reflection cycles, reducing catastrophic overload events from 11.6% to 2.3% of episodes without retraining RL parameters. 

Index Terms —Software-defined networking, Internet of Things security, reinforcement learning, Proximal Policy Optimization (PPO), multi-agent systems, large language models (LLMs). 

I. I NTRODUCTION 

The rapid proliferation of Internet of Things (IoT) devices has transformed enterprise and critical infrastructures into 

S. Jamshidi and F. Khomh are with the SWAT Laboratory, Poly-technique Montr´ eal, Montr´ eal, QC, Canada (e-mail: {saeid.jamshidi, foutse.khomh }@polymtl.ca). M. A. Hamdaqa is with the SæT Laboratory, Polytechnique Montr´ eal, Montr´ eal, QC, Canada. N. Shahabi and C. Fung are with the Concordia Institute for Information Systems Engineering (CIISE), Concordia University, Montr´ eal, QC, Canada (e-mail: negar.shahabi@concordia.ca, carol.fung@concordia.ca). 

large-scale, heterogeneous, and latency-sensitive environments [1] [2]. Industrial systems, smart grids, transportation net-works, and healthcare platforms now rely on billions of resource-constrained devices that continuously generate dy-namic traffic [3] [4]. This expansion substantially increases the attack surface, exposing operational networks to Denial-of-Service (DoS) attacks, stealthy scans, and adaptive adversarial behaviors [5], [6]. Software-Defined Networking (SDN) is widely adopted in IoT networks due to its centralized control plane and programmable forwarding capabilities [7]. SDN enables rapid deployment of mitigation actions such as flow drops, rate limiting, and quarantine policies [8]. However, this centralization introduces structural coupling: mitigation actions themselves consume controller resources and alter queue dynamics, flow-table states, and rule-install delays. Consequently, defensive decisions modify not only the attack surface but also the distribution of future observations [9] [10]. SDN-IoT defense, therefore, constitutes a closed-loop control problem rather than a pure traffic classification task [11] [12]. Aggressive mitigation actions in SDN networks may increase control-plane overhead, as they often require frequent rule installations and interactions with the controller, which can significantly affect controller workload and network performance [13] [14] [15]. Under heavy traffic and attack conditions, this may lead to controller bottlenecks and degra-dation of Quality of Service (QoS) [16], [17]. Despite these operational constraints, many existing security mechanisms treat mitigation as an isolated response step rather than as part of a coupled control process involving the controller, switches, and network dynamics. Existing learning-based SDN security solutions generally follow two paradigms. Detection-centric approaches primarily focus on improving classification accuracy, treating mitigation as a static reaction layer, and often neglecting the feedback between mitigation decisions and control-plane dynamics [18]–[20]. Reinforcement learn-ing (RL) approaches have recently been explored to enable adaptive mitigation and dynamic network control in SDN net-works [21], [22]. However, many RL-based systems optimize simplified reward functions and short-term objectives, which may lead to unstable behavior under complex traffic dynam-ics and adversarial traffic patterns [23], [24]. Furthermore, most existing systems lack mechanisms for structured and auditable policy evolution. When instability occurs, adaptation typically relies on retraining models and manually adjusting mitigation rules, which can be operationally costly and dif-ficult to audit in large-scale SDN-IoT networks [25]. As a 

> arXiv:2604.01127v1 [cs.CR] 1 Apr 2026 2

result, current approaches struggle to simultaneously main-tain adaptive defense behavior, controller stability, and QoS guarantees under evolving attack conditions. To address these limitations, we introduce a two-timescale control framework for SDN-IoT defense. At the fast timescale, decentralized per-switch Proximal Policy Optimization (PPO) agents perform controller-aware mitigation under explicit safety constraints. At the slow timescale, a governance layer analyzes system trajectories and proposes structured updates to a shared global policy entity Π. Instead of modifying neural network param-eters, this governance mechanism updates an explicit policy constitution that defines admissible action masks, controller-aware thresholds, reward priorities, and actuation limits. Policy changes are represented as machine-readable updates, ∆Π ,and are deployed only after passing non-regression tests and safety validation, ensuring fail-safe operation. By separating fast mitigation from slower policy evolution, the proposed design combines adaptive learning with conservative network change management. This two-timescale structure preserves decentralized responsiveness while limiting high-risk behavior caused by controller coupling. The main contributions of this work are:  

> •

Self-reflective two-timescale SDN-IoT defense archi-tecture. We formulate SDN-IoT defense as a closed-loop control system that separates fast decentralized mitigation from slower reflective governance. Indepen-dent per-switch PPO agents perform real-time mitigation using deployable telemetry signals, while a multi-agent LLM governance layer operates on a slower timescale to analyze trajectory evidence and adapt the global safety structure. This separation preserves rapid local respon-siveness while enabling stable and interpretable long-horizon policy evolution.  

> •

Auditable policy constitution with structured multi-agent LLM updates. We introduce a global policy entity, Π, that serves as an explicit policy constitu-tion encoding safety constraints, admissible action sets, controller-aware thresholds, and reward priorities. Policy evolution is expressed through structured updates ( ∆Π )generated by a constrained multi-agent LLM pipeline (Critic, Compiler, Red-Team, and Judge). Updates are schema-validated, conflict-resolved, and deployed only after deterministic validation and non-regression testing, ensuring interpretable, auditable, and reproducible policy evolution.  

> •

Controller-aware and risk-sensitive defense under re-alistic SDN dynamics. The framework explicitly mod-els SDN control-plane coupling, including controller backlog, delayed rule activation, and flow-table pres-sure, capturing how mitigation actions reshape future network states. Defense policies are optimized using a risk-sensitive, multi-objective formulation that jointly balances security effectiveness, QoS stability, controller load, and operational overhead, thereby encouraging poli-cies that prevent catastrophic controller saturation while maintaining stable network operation. The remainder of this paper is organized as follows. Sec-tion II reviews related work on SDN-IoT security. Section III presents the proposed two-timescale control framework. Section V describes the experimental setup and evaluation protocol. Section VI reports system-level results and compares performance with existing approaches. Sections VIII, IX, and X discuss implications, limitations, and concluding remarks. II. R ELATED WORK 

This section reviews prior work relevant to SDN-IoT se-curity, categorized into four areas: deep-learning-based in-trusion detection, reinforcement-learning-based autonomous defense, multi-agent and distributed defense architectures, and resilience and explainable cyber-defense mechanisms. 

A. Deep-Learning-Based Security in SDN-IoT Networks 

A substantial body of recent work focuses on DL-driven intrusion detection for SDN-enabled IoT networks. These studies exploit the centralized visibility of SDN controllers and the programmability of SDN-IoT architectures to detect attacks across traffic flows, packet-level attributes, and tempo-ral communication patterns. For example, the HCL framework in [26] combines CNN and LSTM models to capture both spatial and temporal traffic characteristics and reports effective detection of DoS, DDoS, port scanning, fuzzing, and OS-level attacks in SDN-IoT settings. The study also highlights persis-tent limitations in existing SDN-IoT IDS research, including scalability concerns, challenges in multiclass classification, and high computational demands. Other works similarly em-phasize that DL improves detection capabilities in SDN-IoT environments but often focus primarily on classification performance. The secure routing framework in [27] integrates deep-belief-network-based attack detection with QoS-aware routing, thereby combining security and routing efficiency in SDN-IoT networks. The DeepSDN architecture in [28] advances controller-centric security orchestration by coupling DL with SDN-based cloud supervision and blockchain-based communication, enabling real-time detection and mitigation of multiple classes of cyber threats in IoT networks. Likewise, the robust CNN-based multiclass IDS in [29] demonstrates that CNN-based models can generalize across multiple SDN-IoT datasets while maintaining strong performance in the presence of class imbalance and multiclass attack settings. 

B. Reinforcement Learning for Autonomous Cyber Defense 

A second line of work shifts from passive detection to adaptive defense through reinforcement learning. These studies treat cyber defense as a sequential decision-making prob-lem, allowing agents to learn policies through interaction with simulated and operational environments. In [30], graph-attention-enhanced DRL is proposed for autonomous AIoT defense, leveraging graph representations to encode relation-ships among hosts and interactions between devices. This work argues that one-dimensional observations are insufficient for complex IoT topologies and shows that graph-aware PPO and DQN variants can improve autonomous defense performance in increasingly complex scenarios. A closely related direction 3

is represented by [31], which enhances autonomous cyber-defense decision-making through graph embedding. That work argues that traditional one-hot state encodings fail to capture topology and inter-node dependence, and therefore proposes Node2vec-enhanced RL over a game-theoretic at-tacker–defender setting. Importantly, [31] also highlights two limitations highly relevant to the present study: many RL-based cyber-defense methods neglect topology-aware percep-tion, and many available training environments remain overly abstract and insufficiently representative of real operational settings. Recent research has also explored autonomous cyber-defense frameworks that combine RL with additional AI components. In [32], a DRL-based autonomous cyber-defense agent is implemented together with knowledge graphs and an augmented LLM, enabling monitoring, service removal, recov-ery, and decoy deployment in a simulated cyber-operations environment. This work is particularly relevant because it notes that earlier DRL-based approaches often behave as black boxes and lack explicit network-topology representation and structured human–machine interaction. In addition, [33] introduces resilience-oriented PPO-based defensive agents and argues that autonomous defenses should be evaluated not only by cumulative reward but also by their ability to absorb attacks, recover promptly, and preserve defender priorities over time. 

C. Multi-Agent and Distributed Defense Architectures 

A third category of work investigates multi-agent and dis-tributed cyber-defense architectures. These studies are moti-vated by the limitations of centralized security systems in large-scale, dynamic, and distributed environments. In [34], a hierarchical multi-agent architecture is proposed for coor-dinated cyber defense using specialized swarms and explicit inter-agent communication. The work emphasizes that effec-tive cyber defense increasingly requires cooperation, commu-nication, and task allocation among distributed agents rather than isolated detection modules. Similarly, [35] presents a rule-based cognitive AI framework in which multiple agents specialize in threat detection, risk assessment, mitigation plan-ning, and adaptive learning, aiming to improve interpretability and response coordination. The multi-agent perspective is also evident in [36], which proposes a cloud-oriented agile defense system for volumetric DDoS mitigation that lever-ages LSTM-based detection, federated learning, blockchain-backed transparency, and multiple specialized agents. This work highlights the operational advantages of agent collab-oration, including parallel execution, reduced latency, local learning, and scalability across large environments. Although its application domain is public-cloud defense rather than SDN-IoT, it demonstrates how distributed agents can support autonomous detection and mitigation while preserving privacy and system responsiveness. More control-theoretic perspec-tives on distributed secure coordination appear in [37], which studies resilient control of cyber-physical multi-agent systems under attacks. While [37] does not specifically target SDN-IoT intrusion detection, it formalizes the use of trust, confidence, attack localization, and resilient distributed control to maintain secure collective behavior under adversarial conditions. From a system design perspective, this reinforces the potential advantages of distributed defensive intelligence over purely centralized. Although previous work has made significant strides in im-proving detection accuracy, adaptive mitigation, and scalable multi-agent control in SDN-IoT networks, these approaches often rely on a single optimization loop in which policies evolve primarily through parameter updates. As a result, the intricate structural coupling between mitigation actions, con-troller workload, delayed actuation, and QoS stability remains largely unexplored. Additionally, existing systems lack mech-anisms for auditable and conservative policy evolution. When instability and unexpected behavior arise, adaptation typically depends on retraining and manual rule tuning, both of which are opaque and operationally costly. These limitations high-light the need to reframe SDN-IoT defense as a two-timescale control challenge, in which fast, decentralized mitigation oper-ates alongside slower, structured governance that enables the evolution of policy constraints without destabilizing learned behavior. The solution proposed in this paper aims to bridge this gap by integrating controller-aware reinforcement learning with interpretable policy-constitution editing validated through stringent safety checks. III. P ROPOSED SELF -R EFLECTIVE MULTI -A GENT 

SDN-I OT C ONTROL SOLUTION 

The proposed solution models SDN-IoT defense as a closed-loop multi-agent control system in which mitigation decisions impact packet forwarding, controller workload, rule installa-tion delay, and the statistical structure of future observations. In SDN networks, forwarding rules are installed through a centralized controller with finite processing capacity; hence, mitigation introduces delayed actuation and shared-resource coupling across switches. The core idea of the solution is therefore to combine decentralized RL for fast local re-sponse with a higher-level reflective reasoning layer that edits global safety structure and long-horizon control priorities in an auditable, policy-centric manner. Figure 1 illustrates the overall architecture and highlights the two-timescale nature of the system. At the lower level, independent per-switch PPO agents provide fast decentralized mitigation using deployable telemetry signals. At the control-plane level, the SDN con-troller aggregates network state and enforces safety through action masking and constraint interfaces. At the upper level, a reflective multi-agent LLM governance engine analyzes summarized trajectory evidence and produces structured policy deltas that update the explicit global policy entity Π, thereby reshaping admissible actions and reward priorities across the network without directly modifying RL parameters. In this work, multi-agent refers to 1) the SDN environment coupling across switches through the shared controller and 2) the multi-agent LLM ensemble that performs reflective policy editing. The RL controllers are trained independently for each switch (no centralized critic, no joint gradient sharing, no Multi-agent RL objective). Thus, the learning layer is not positioned as Multi-agent RL; rather, it is a set of independent RL con-trollers operating under a shared, editable policy structure Π4   

> Fig. 1. Architecture of the proposed self-reflective SDN-IoT control solution. Independent per-switch PPO agents enable fast, decentralized mitigation, while the centralized controller mediates shared-control-plane dynamics and enforces safety constraints. A multi-agent LLM governance engine (Critic, Compiler, Red-Team, Judge, and Memory roles) operates on a slower timescale to generate auditable policy updates that modify the global policy entity Πand guide the safe evolution of the system.

(defined below). This design choice is intentional; independent per-switch PPO preserves scalability and avoids centralized training bottlenecks, while global coordination is induced through 1) shared control-plane coupling and 2) Π-constrained action feasibility and reward shaping, which together provide controller-aware, system-level alignment without a joint gra-dient objective. We represent the latent global network state as: 

st =  qt, f t, dt, zt, (1) where qt ∈ RN 

> +

denotes switch queue occupancies, f t repre-sents flow-table states including timeout counters and eviction risk, dt captures controller backlog, service delay, and pending rule installations, and zt encodes the latent traffic including be-nign load and adversarial intensity. This decomposition makes explicit that SDN-IoT defense is not a static classification task: actions change the physical control loop by modifying queueing, rule availability, and control-plane latency, thereby altering what agents observe in subsequent steps. Formally, the SDN-IoT control loop defines a controlled Markov process with a transition kernel: 

P : ( S × A ) → ∆( S), (2) where ∆( S) denotes the probability simplex over S. This explicitly encodes that mitigation changes the distribution of future network states (queues, controller backlog, flow-table pressure), not only instantaneous traffic snapshots. The system evolves according to: 

st+1 ∼ P (st+1 |st, at), (3) where at = ( at

> 1

, . . . , a tN ) is the joint action vector. Because controller capacity is shared, this transition kernel is non-separable: local decisions alter the transition distribution ex-perienced by all agents. In particular, SDN introduces delayed actuation (controller queueing and rule-apply delay), so P

implicitly includes the stochastic delay between “decide” and “effect”. To make SDN rule-activation delay explicit, we introduce an auxiliary controller memory state ξt capturing pending FlowMod / meter-install jobs: 

st+1 ∼ P  st+1 | st, at, ξ t , ξt+1 = H ξt, at, d t . (4) Here, ξt formalizes the intuition that DROP_FLOW /

QUARANTINE may not be effective immediately when con-troller backlog is high; instead, their effect is mediated through the evolution of ξt and dt. We explicitly incorporate SDN control-plane realism into P (·) by modeling bounded PacketIn buffering, stochastic controller service time, stochastic rule-apply delay, and finite flow-table capacity with timeout/evic-tion dynamics; together, these mechanisms determine when an action becomes effective and how long flow misses persist. This point is fundamental in SDN-IoT because congestion and security are coupled through the control plane; a policy that naively increases rule writes can reduce attacks yet still destabilize the controller, thereby worsening end-to-end QoS. To expose this coupling explicitly, the controller backlog evolves as: 

dt+1 = max 0, d t +

> N

X

> i=1

ρti − μ

!

, (5) where t denotes the discrete control time step and dt represents the controller backlog (e.g., the number of pending PacketIn and rule-installation requests) at time t. The term ρti is the PacketIn arrival rate generated by switch i at time t, and μ is the controller service rate. This dynamic clarifies that even correct mitigation may destabilize the system if it induces excessive PacketIn and rule churn: when P 

> i

ρti approaches and exceeds μ, the controller becomes the bottleneck, rule 5

installation is delayed, flow misses persist longer, and the observation process becomes biased by control-plane satura-tion. A standard sufficient condition for bounded backlog in expectation is that the long-run average PacketIn arrival does not exceed service: 

lim sup  

> T→∞

1

T 

> T−1

X

> t=0

E

" NX

> i=1

ρti

#

< μ. (6) This inequality motivates why controller-aware shaping terms (e.g., gt

> ctrl

) and Π-based caps are essential: policies that reduce attacks but drive P 

> i

ρti upward can violate this condition and destabilize the control plane. Importantly, while a full Lyapunov proof is beyond scope, the closed-loop design induces a negative-feedback stabilization mechanism: when dt

(related overload indicators) increases, Π tightens admissible actions and throttles controller-heavy actuation, which reduces effective PacketIn/job arrivals and rule-churn, pushing the sys-tem back toward the region where E[P 

> i

ρti] < μ . Empirically, this creates a bounded operating envelope in which backlog excursions trigger stronger penalties and masks, preventing sustained overload and suppressing runaway delayed-actuation cascades. Crucially, the solution operates on two coupled timescales. On the fast timescale, RL switch agents map local telemetry to SDN actions, reacting immediately to localized congestion and evidence of attacks. On the slow timescale, a set of LLM agents performs reflective analysis over recent trajectories and produces structured edits to an interpretable policy entity Π (constraints, masks, reward patches). This sep-aration ensures that local actions remain responsive while the global safety structure remains stable, auditable, and adaptable to evolving IoT traffic patterns. Let t denote the fast RL timestep and let reflection occur every K steps. Define the slow index k = ⌊t/K ⌋ and the trace window Tk ≡ T t−K:t.Then policy evolution is: 

Πk+1 = Π k ⊕ ∆Π k,

∆Π k = L(Π k, Ek),

Ek = SUMMARIZE TRACE (Tk).

(7) which explicitly states that Π is updated only on the slow clock and only via structured deltas. To make the slow-timescale component explicit, we define the global policy entity as a structured object: 

Π ≜  SΠ(·), τ , w, χ, (8) where SΠ(o) is a context-dependent safe action set (mask/-constraints) parameterized by Π, τ are threshold parameters used by constraints (e.g., overload and flow-table pressure thresholds), w are reward weights used in Rt = w⊤gt, and 

χ denotes additional policy patches such as action-dependent cost tables, hint-trust calibration, rule-install throttles, and controller-aware actuation caps. The role of the LLM multi-agent layer is to update Π by producing a machine-parsable delta ∆Π based on evidence, rather than directly modifying RL weights, thereby enabling auditability and reproducibility at the policy-structure level. In particular, the LLM agents do not modify the PPO parameters {θi}; they modify the shared safe execution space and the reward-shaping configuration that define the learning landscape and the operational envelope for all switch agents. Let Ek denote an evidence bundle extracted from recent traces (QoS tails, PacketIn drops, FlowMods, churn, FP/FN, etc.). The LLM ensemble implements a struc-tured operator: 

∆Π k = L(Π k, Ek), (9) where ∆Π k is restricted to edits of (SΠ, τ , w, χ) and explic-itly forbidden from editing the RL weights {θi}. Operationally, the LLM layer changes the policy constitution (what is al-lowed, what is penalized, and what thresholds trigger safety rules), not the instantaneous per-step action selection. Equally important, the allowed edit set is intentionally conservative: 

∆Π k may only 1 restrict/relax masks within predefined action families, 2) adjust thresholds within bounded ranges, and 3) reweight shaping terms under regime flags. This bounded edit space prevents the governance layer from introducing arbitrary control logic and supports auditable change control. The reflective layer operates on Ek = SUMMARIZE TRACE (Tk),which is computed deterministically from control-plane and dataplane counters and from evaluation labels (FP/FN) avail-able to the solution. The LLM agents are not given raw packet payloads, and do not require deep packet inspection; instead, they operate on aggregated statistics (e.g., queue tails, flow-mod rates, PacketIn drops, RTT percentiles, churn, and flags) that are already standard SDN/IoT monitoring outputs. This design keeps reflection aligned with deploy-able telemetry and avoids dependence on content inspection. Each role (Critic, Compiler, Red-Team, and Judge) is in-stantiated with a fixed system prompt and a fixed JSON-schema contract, and all roles can use the same lightweight LLM backbone (as configured in the implementation) while remaining functionally distinct through role-specific prompts and outputs. To preserve determinism and auditability, the reflective loop enforces strict JSON parsing, schema checks, rule deduplication, and conflict resolution before any can-didate update is evaluated. All prompts, responses, hashes, latency, parse success, and accepted/rejected deltas are logged to enable end-to-end reproducibility. Because the reflective loop runs on the slow clock, it is not on the dataplane/fast-control critical path; runtime enforcement uses the current compiled Πk, while candidate updates are generated and validated out-of-band and only swapped at policy check-points. In the reference implementation associated with this methodology, all four LLM roles (Critic, Compiler, Red-Team, and Judge) are instantiated with the same backbone model identifier gpt-4o-mini , as specified in the configuration (model_critic , model_compiler , model_redteam ,

model_judge ). The roles remain distinct due to role-specific system prompts and schema-constrained outputs, even when the same backbone is used. To reduce run-to-run variation and stabilize policy edits, the LLM calls in the reflective loop are executed with fixed decoding parameters (e.g., fixed temperature and nucleus sampling configuration, as set in the implementation). When a candidate response fails JSON parsing, it is treated as invalid and cannot modify Π; the system either retries under the same constraints for a bounded number of attempts and falls back to the previous policy 6

entity Πk (no-update). This makes the reflective layer fail-closed : malformed and non-compliant outputs cannot enter the control loop. Moreover, the acceptance gate (hard-safety and non-regression) acts as an anti-oscillation mechanism: because policy edits must improve and preserve key metrics under tar-geted stress campaigns, repeated back-and-forth changes that would induce unstable drift are filtered out, and the deployed 

Π evolves through conservative, monotone refinements driven by observed failure evidence. 

A. Multi-Agent Formulation 

Each switch is associated with an autonomous decision agent, leading to a partially observable stochastic game: 

G = ⟨S , {O i}, {A i}, P, R, γ ⟩. (10) At each time t, agent i receives telemetry: 

oti = {rti , q ti , c ti, f ti , e ti, u ti, ρ ti, φ ti, h ti}, (11) which captures traffic rate, congestion, compute pressure, rule pressure, control-plane stress, actuation intensity, and weak semantic context. The observation vector is intentionally de-signed to be computable from SDN/IoT monitoring primitives (per-port counters, queue occupancy, flow-table PacketIn, and flow-mod counters), enabling deployment without payload inspection. The telemetry is a noisy projection of the latent global state, captured by an observation kernel: 

oti ∼ Oi(· | st), ot ∼

> N

Y

> i=1

Oi(· | st), (12) which emphasizes partial observability: agents act on deploy-able counters rather than on the full latent regime zt. Entropy is computed as: 

eti = − 1log K

> K

X

> k=1

pti,k log pti,k , (13) which measures dispersion of traffic sources and provides scale-invariant detection of distributed attacks. Because IoT traffic may include benign, synchronized bursts, eti is jointly interpreted alongside ρti (control-plane stress), φti (actuation intensity), and uti (port diversity) to reduce false alarms from legitimate events, such as firmware updates. The multi-agent structure is not limited to the RL layer. In addition to the 

N switch-level RL agents, the solution uses a small set of LLM agents as policy-edit agents that operate over aggregated traces. These LLM agents do not execute dataplane actions; instead, they collaborate to propose, compile, adversarially test, and gate updates to Π, thereby shaping and constraining the behavior of all RL agents through SΠ(·) and reward patches. Operationally, the RL agents answer the question “what action should be taken now at this switch given local telemetry?”, whereas the LLM agents answer the question “what global safety structure and shaping should govern all switch policies for the next horizon given observed failures and new patterns?” This separation is also a practical scalability choice: the RL layer remains lightweight and reactive (per-switch inference and local PPO updates), while the governance layer performs slower, policy-centric reasoning on aggre-gated evidence, avoiding centralized high-frequency decision-making. We model each LLM role as a typed mapping over evidence Ek:CRITIC LLM : (Π k, Ek) → I k (diagnosis) ,

(14) COMPILER LLM : Ik → ∆Π k (compile) ,

(15) RED TEAM LLM : Ek → C rt (stress tests) ,

(16) JUDGE LLM : (Π k, Πcand , ∆M) → { 0, 1} (gate) . (17) This makes the multi-agent LLM layer explicitly procedural and constrained: it is a governance pipeline that produces only structured policy edits and accepts them only under conserva-tive non-regression constraints. Although the implementation uses gpt-4o-mini for all roles, each role is operationally distinct because it is constrained by a different typed output contract. Specifically, the Critic returns structured diagnoses and proposed deltas; the Compiler normalizes proposals into enforceable masks/constraints and rewards patch updates; the Red-Team generates targeted adversarial campaigns; and the Judge gates deployment via approve/reject decisions with required fixes. This separation is therefore not a “different model per role” requirement; it is a “different function per role” requirement enforced through schema and deterministic validation. At minimum, the Critic output contains typed fields that identify failure modes (e.g., controller saturation, rule churn bursts, false disruptions under benign synchronized events) and corresponding proposed remedies, expressed as candidate policy edits. The Compiler output contains a typed delta ∆Π k with fields that map directly to Π (e.g., con-straint/mask rules for SΠ, threshold updates for τ , reward weight/patch updates for w and χ, and explicit caps/throttles). The Judge output contains an explicit binary decision and a machine-readable rationale block that can be logged and traced back to ∆M, ensuring the acceptance decision is reproducible and not implicit. In this sense, the governance layer functions as a constrained policy-update operator rather than a learned controller: it can only move within a predefined, schema-checked edit space and must pass stress-tested feasi-bility checks before affecting the closed loop. The reflective layer can be viewed as solving a conservative policy-structure update: 

∆Π k ∈ arg max  

> ∆Π ∈U

E[J (Π k ⊕ ∆Π) | E k]

s.t. HARD SAFETY (Π k ⊕ ∆Π) = 1 . (18) where U denotes the set of allowed edit types (masks/thresh-olds/reward patches/caps). This explains, mathematically, why the LLM is not a free-form controller: it is restricted to a bounded set of updates and must satisfy safety constraints before deployment. 

B. Coupled Action Effects in SDN 

Each agent selects: 

ati ∈ { ALLOW , ALERT , MIRROR ,

RATE_LIMIT , DROP_FLOW , QUARANTINE }. (19) 7

These actions affect both the data-plane traffic and the control-plane dynamics, thereby altering the distribution of future observations. This is essential in SDN-IoT: DROP_FLOW and 

QUARANTINE are implemented via controller-mediated rule updates and can increase flow-mod load, whereas MIRROR 

can increase processing and bandwidth overhead. At each time step, the switch-level RL agent outputs a discrete SDN prim-itive, which is either realized locally (e.g., mirroring) and via controller-installed OpenFlow rules (e.g., rate-limiting, drop-ping, quarantining), thereby closing the loop among telemetry, decision-making, and data-plane enforcement. Consequently, the same selected action can yield different outcomes depend-ing on controller saturation and the time required for the rule to take effect. Queue evolution can be written as: 

qt+1  

> i

=

h

qti + λti(ati, zt) − μi(ati)

i

> +

, (20) where λti(ati, zt) denotes effective offered load after mitigation (e.g., RATE_LIMIT reduces load), μi(ati) is effective service under action overhead (e.g., MIRROR may reduce effective service due to processing), and [·]+ ensures non-negativity. To model delayed actuation, we incorporate a controller-induced penalty term that captures the fact that some actions only take effect after controller processing: 

qt+1  

> i

=

h

qti + λti − μi(ati) + δti (dt, a ti)

i

> +

, (21) where δti (dt, a ti) increases when the controller backlog dt is large and when the chosen action requires heavy control-plane intervention (e.g., rule installation). A simple controller-aware model assumes δti is monotone in backlog: 

δti (dt, a ti) = κ(ati) · σ(dt), σ(d) = log(1 + d), (22) where κ(a) is larger for controller-heavy actions (e.g., 

DROP_FLOW , QUARANTINE ) and smaller for local actions (e.g., MIRROR ). This explicitly formalizes why the same action can have different effects under saturation. Thus, miti-gation induces a coupled nonlinear control process: 

(qt+1 , dt+1 ) = F (qt, dt, at, zt), (23) which makes clear why purely centralized and purely heuristic policies fail in large SDN-IoT networks: the same action can produce different outcomes depending on controller saturation and flow-table pressure. This is precisely why the solution separates fast local-control RL from slower global policy editing (LLM agents): RL handles immediate local variations, while the LLM layer updates the shared structure that governs safe actions and long-horizon priorities across all switches. 

C. Risk-Sensitive Multi-Objective Reward 

We define the performance vector: 

gt = ( gt

> sec

, g t

> lat

, g t

> ctrl

, g t

> cost

), (24) representing security effectiveness, latency impact, controller load, and operational cost. The scalar reward is: 

Rt = w⊤gt. (25) 

Algorithm 1 Self-Reflective SDN-IoT Control                                                                               

> 1: Init switches/controller queues/flow capacities; init agents {πθi, V i}and global policy Π
> 2: Set reflection interval Kand rollout size M
> 3: for episode = 1 . . . E do
> 4: Reset dataplane queues/flow tables and controller backlog 5: for timestep t= 0 . . . T do
> 6: Update controller: serve PacketIn jobs; apply pending rules 7: for each switch ido
> 8: Observe oti; sample ati∼πθi(· | oti)
> 9: at∗
> i←SAFETY FILTER (ati, o ti,Π) ;execute; update
> (qt+1
> i, f t+1
> i)
> 10: Update (ρti, φ ti)and backlog dt+1 ; compute gt, R t
> 11: Store (oti, a ti, a t∗
> i, R t, o t+1
> i)
> 12: end for
> 13: if buffer ≥Mthen
> 14: PPO + critic update; clear buffer 15: end if
> 16: if tmod K= 0 then
> 17: Π←REFLECT AND VALIDATE (Π ,Tt−K:t)
> 18: end if
> 19: end for
> 20: end for

To tie these terms to SDN-IoT realities, gt 

> sec

is linked to mit-igation correctness over a window (reducing missed attacks), 

gt 

> lat

is a bounded proxy of congestion (e.g., normalized queue and RTT), gt 

> ctrl

penalizes controller overload indicators such as PacketIn drops, backlog, and flow-mod saturation, and gt

> cost

encodes operational overhead such as mirroring bandwidth and quarantine disruption. Instead of optimizing expected return, we adopt a risk-sensitive objective: 

J(π) = max  

> π

CVaR α

> ∞

X

> t=0

γtRt

!

, (26) which emphasizes worst-case trajectories and discourages catastrophic network states, such as persistent queue ex-plosions and control-plane collapses. In SDN-IoT, this risk sensitivity is critical because failure cases are not average-case events; a small number of overload episodes can dominate operator experience and cause large-scale service disruption. Operationally, optimizing a tail-risk functional aligns with SDN change control; rare overload episodes (controller col-lapse, sustained backlog, rule-delay cascades) are dispropor-tionately costly, so policy evolution is driven by preventing the lower tail rather than marginally improving mean reward. The governance layer leverages this by increasing controller-stress penalties and tightening the feasibility of high-risk actions pre-cisely when tail-risk evidence appears in Ek, thereby biasing learning and execution away from catastrophic outcomes. Let 

G ≜ P∞ 

> t=0

γtRt denote the discounted return. Conditional Value at Risk (CVaR) can be expressed via the Rockafellar-Uryasev [38] form: CVaR α(G) = min 

> η∈R



η + 11 − α E(G − η)+



. (27) This makes clear that the objective penalizes the tail of bad trajectories (e.g., controller collapse) as well as the mean. The reflective LLM layer can directly tune w and other shaping parameters inside Π (e.g., increasing the penalty on gt 

> ctrl

after observing controller saturation), which provides a principled mechanism for shifting long-horizon priorities without destabilizing the RL optimization loop. 8

D. Decentralized RL Control 

Each switch learns policy πθi (ai|oi) using PPO.Advantage estimation is computed as: 

At =

> ∞

X

> k=0

(γλ )k(rt+k + γV (ot+k+1 ) − V (ot+k)) , (28) while the clipped surrogate objective is: 

LPPO = E [min( ρtAt, clip (ρt, 1 − ϵ, 1 + ϵ)At)] , (29) with the likelihood ratio: 

ρt = πθi (ati|oti)

πold θi (ati|oti) . (30) Independent learning enables scalability to large-scale IoT networks, whereas coupling via shared reward terms and con-troller dynamics induces implicit coordination among agents. In particular, because gt 

> ctrl

penalizes controller stress, each local agent learns that excessive PacketIn and flow-mod churn harms the global system, thereby aligning decentralized mitigation with overall SDN stability. The LLM policy agents do not replace PPO and intervene at every step; instead, they modify the constraints and shaping parameters that define the learning landscape and the safe execution space. This makes the RL layer stable and fast, while still enabling the overall system to adapt structurally to new IoT behaviors and emerging attack patterns. From a systems standpoint, this two-timescale separation also bounds runtime overhead: PPO inference and safety filtering remain local and lightweight, while reflective governance runs infrequently (every K steps) and affects only the compiled policy Π at checkpoints. Algorithm 1 formalizes the fast-timescale interaction between 1) SDN controller pro-cessing and delayed rule activation, 2) per-switch RL action sampling, and 3) Π-enforced safe execution. The reflective update stage is intentionally deferred to the slow loop in Section III-F and Algorithm 2. 

E. Safety-Constrained Execution 

To ensure operational stability, actions are filtered by: 

at∗ 

> i

=

(

ati ati ∈ S (oti)

fallback (oti) otherwise , (31) where S(oti) encodes admissible actions derived from con-troller capacity constraints and network safety rules. Impor-tantly, S(oti) is not static: it is parameterized by the policy entity Π and can be edited by the reflective layer, enabling the system to evolve from experience while preserving hard safety boundaries. For example, when ρti and dt are high, Π

may restrict heavy actions that increase actuation load; when 

f ti indicates flow-table pressure, Π may forbid policies that increase rule churn. If the reflective layer produces an invalid delta (non-parsable JSON, schema violation, conflicting con-straints that cannot be resolved, and candidate policy failing hard-safety checks), then Π is not updated, and the system continues with Πk. Likewise, the safety filter always executes a valid fallback action, ensuring that LLM failures do not cause undefined behavior in the dataplane and controller. A key point in SDN-IoT is that the safest action is context-dependent; under severe overload, the safest strategy may be to avoid controller-intensive actions and choose mitigations that reduce future PacketIn, whereas under low load, the system may allow heavier containment. Encoding this logic in Π

makes it explicit, auditable, and editable by the LLM agent ensemble. We interpret SΠ(o) as a constraint-induced mask: 

SΠ(o) = {a ∈ A : c(o, a ; τ , χ) ≤ 0}, (32) where c encodes controller-aware constraints (e.g., backlog caps, FlowMod throttles, flow-table pressure limits). This makes LLM edits interpretable: updates to (τ , χ) directly reshape the admissible action set. In discrete action spaces, the safety filter can be viewed as selecting the closest feasible action under a task-specific divergence D:

at∗ 

> i

= arg min  

> a∈S Π(oti)

D(a, a ti). (33) This equation provides a mathematical interpretation of the SAFETY FILTER : It increases uti under overload conditions, increases P 

> i

ρti under overload conditions, and mimics pat-terns that reduce the need to intervene at execution time. 

Algorithm 2 Reflect And Validate: Multi-LLM Agents Editing the Global Policy Entity Π                                                                                               

> Require: Current policy entity Πk, recent trace window Tk
> Ensure: Updated policy entity Πk+1
> // 0) Evidence bundle (deterministic)
> 1: Ek←SUMMARIZE TRACE (Tk)▷QoS tails, PacketIn drops, backlog, FlowMods, churn, FP/FN, regimes
> // 1) Critic: diagnose + propose edits
> 2: Ik←CRITIC LLM (Π k,Ek)
> // 2) Compile proposals into typed delta (schema)
> 3: ∆Π raw k← I k.proposals ;∆Π k←COMPILER LLM (∆Π raw k)▷
> masks, constraints, thresholds, reward patch, caps
> // 2.1) Normalize/dedup/schema-check (non-LLM)
> 4: ∆Π k←SCHEMA CHECK (DEDUPLICATE (NORMALIZE (∆Π k)))
> // 2.2) Merge with precedence + conflict resolution
> 5: Πcand ←Πk⊕∆Π k▷hard constraints →masks →thresholds →
> reward patch
> // 3) Red-team: targeted stress campaigns
> 6: Crt ←RED TEAM LLM (Ek)
> // 4) Evaluate current vs candidate on identical campaigns
> 7: Mk←EVALUATE POLICY (Π k,Crt );Mcand ←
> EVALUATE POLICY (Π cand ,Crt )
> 8: ∆M ← M cand − M k
> // 5) Hard safety + conservative non-regression gating
> 9: safe ←CHECK HARD SAFETY (Π cand );ok ←
> NON REGRESSION (∆ M)
> // 6) Judge: final conservative decision + explanation
> 10: approve ←JUDGE LLM (Π k,Πcand ,∆M,safe ,ok )
> 11: if approve ∧safe ∧ok then
> 12: Πk+1 ←Πcand
> 13: else
> 14: Πk+1 ←Πk
> 15: end if // 7) Paper-grade logging (reproducibility)
> 16: L OG ARTIFACTS (Π k,∆Π k,Πcand ,Crt ,Mk,Mcand ,Πk+1 )
> 17: return Πk+1

F. LLM-Based Reflective Policy Evolution 

We maintain a structured policy entity, Π, that contains interpretable safety rules and reward modifications. Given trajectory trace Tk, the reflective operator produces: 

Πk+1 = R(Π k, Tk), (34) 9

and deployment follows: 

Π∗ =

(

Πk+1 if validated 

Πk otherwise . (35) This reflective mechanism is explicitly multi-agent: it com-prises cooperating LLMs that play distinct roles within the RL layer, converting raw traces into auditable policy edits. Con-cretely, the Critic LLM agent analyzes Tk to identify failure modes in SDN-IoT terms (e.g., controller overload episodes, churn bursts, false disruptions during benign synchronized IoT events) and proposes structured fixes. The Compiler LLM agent converts proposals into enforceable policy deltas (e.g., action masks, constraints, threshold rules, reward patch co-efficients) expressed in a machine-parsable representation for 

Π. The Red-Team LLM agent generates targeted adversarial test scenarios that exploit discovered weaknesses (e.g., high-diversity scans that increase uti, overload conditions that in-crease sum i rho ti, and mimicry patterns that reduce telemetry separability). In addition, the Judge LLM agent validates whether the candidate policy entity improves stability and safety without degrading security, ensuring that global policy edits remain conservative and reproducible. In this sense, RL governs fast actuation while the LLM ensemble governs the evolution of the system’s interpretable “constitution” Π

that constrains and shapes behavior across all switches. To formalize the fact that LLM agents propose structured edits, we view the reflection output as a delta: 

∆Π k ≜  ∆SΠ, ∆τ , ∆w, ∆χ, (36) and the merge operation as Πcand = Π k ⊕ ∆Π k, where ⊕

applies precedence rules, resolves conflicts, and deduplicates overlapping constraints. This makes policy editing explicit in the mathematics: LLM agents do not “guess actions”; they produce deltas that modify safety masks, thresholds, and reward shaping in a controlled and auditable way. The merge ⊕

is deliberately conservative: hard constraints take precedence over soft patches, conflicts are resolved by selecting the more restrictive feasible constraint under overload, and edits are range-clipped to prevent abrupt policy drift. These determinis-tic rules ensure that successive applications of ∆Π behave as controlled refinements rather than unconstrained rewrites. Let 

M(Π , C) return a metric vector under campaign set C, e.g., 

M = ( F 1, RTT p95 , D ctrl , FlowMods , DropRate ). (37) We accept Πcand only if it satisfies hard safety and non-regression constraints: HARD SAFETY (Π cand ) = 1 , (38) NON REGRESSION (∆ M) = I

h

∆F 1 ≥ − ϵF 1 (39) 

∧ ∆RTT p95 ≤ ϵrtt (40) 

∧ ∆Dctrl ≤ ϵctrl 

i

. (41) where ∆M = M(Π cand , Crt ) − M(Π k, Crt ) and 

(ϵF 1, ϵ rtt , ϵ ctrl ) are conservative tolerances. Define the acceptance set: 

A ≜

n

Π : 

HARD SAFETY (Π) = 1 ,

∆F 1 ≥ − ϵF 1,

∆RTT p95 ≤ ϵrtt ,

∆Dctrl ≤ ϵctrl 

o

. (42) Then the deployment rule is simply Πk+1 = Πcand iff 

Πcand ∈ A; otherwise Πk+1 = Π k. This expresses the gate as a clean feasibility test, reinforcing that reflective updates are conservative and fail-closed. Consequently, even if an LLM proposes an aggressive edit, it cannot be deployed unless it is 1) syntactically valid, 2) semantically consistent under deter-ministic merge rules, and 3) empirically non-regressive under targeted stress campaigns, which together provide practical safety assurances in lieu of full formal verification. IV. T HREAT MODEL 

We consider adversarial SDN-IoT networks in which ma-licious traffic attempts to degrade network stability, evade detection, and overload the control plane. The defender is the proposed multi-agent control solution, composed of decentral-ized RL switch agents, a centralized SDN controller, and a reflective governance layer that evolves the global policy entity 

Π. We model a traffic-only adversary: the attacker interacts with the system exclusively through packet/flow generation and timing patterns, and does not compromise SDN control software, switch firmware, telemetry counters, and the training pipeline. This threat model targets the dominant operational risk in SDN-IoT, namely, control-plane and queueing collapse induced by adversarial traffic, while explicitly separating it from platform-compromise attacks. Let st ∈ S denote the la-tent network state at time t, and let zt denote the traffic, which includes both benign IoT activity and adversarial behavior. The attacker does not directly manipulate the controller and the learning parameters; instead, it impacts the system through traffic-generation patterns. Consequently, the system evolves according to the adversarially-impactd transition kernel: 

st+1 ∼ P (· | st, at, zt), (43) where at is the joint mitigation action chosen by the switch agents. This formulation captures that attacks alter the sta-tistical structure of future observations by affecting queue lengths, controller workload, and rule-installation delays. We assume the attacker can adapt zt online as a function of ob-served network responses (e.g., RTT changes, packet loss, and reachability), yielding a non-stationary, temporally correlated adversarial. However, the attacker is assumed to be unable to directly observe internal controller backlog variables, the internal reward decomposition, and the current Π parameters, except indirectly through measurable network behavior. The adversary seeks to increase cumulative network degradation while avoiding detection. This objective can be expressed as: 

max  

> z0: T
> T−1

X

> t=0



λ1D(st, at) − λ2I[detected ]



, (44) 10 

where D(·) represents disruption measures such as queue growth, RTT inflation, and controller saturation. This for-mulation reflects realistic adversarial behavior in IoT net-works, where the attacker aims to remain persistent rather than immediately visible. To more precisely re-flect SDN-IoT attacker incentives, detection can be de-fined as any defender-triggered containment action (e.g., sus-tained DROP_FLOW /QUARANTINE decisions and persistent 

RATE_LIMIT ) that measurably reduces attacker throughput and increases the attacker’s cost. Under this interpretation, 

I[detected ] captures not only classifier-style alarms but also the operational visibility of mitigation outcomes. The attacker can generate distributed, bursty, and temporally correlated traffic and may vary source diversity, packet timing, and synchro-nization patterns. These controls indirectly impact key system variables, including switch queue occupancies qti , PacketIn rates ρti, flow-table pressure f ti , and controller backlog dt. In particular, the attacker may mimic benign IoT synchronization events to reduce separability in the observation space and increase the difficulty of detection. We explicitly include SDN-specific stress patterns in the attacker action space: 1) control-plane saturation attacks that maximize PacketIn/FlowMod pressure by inducing flow-misses and churn; 2) low-and-slow persistent attacks that remain below static thresholds but accumulate tail-risk; 3) burst-and-idle campaigns that exploit delayed actuation and queue dynamics; 4) mimicry attacks 

that imitate legitimate synchronized IoT bursts (firmware updates, telemetry reporting waves) to increase false posi-tives and induce conservative policies; and 5) multi-switch correlated campaigns that trigger coupled congestion across multiple switches to amplify shared-controller bottlenecks. Switch agents do not observe the full network state but only local telemetry: 

oti ∼ Oi(· | st). (45) Thus, the defender operates under partial observability of the adversary. The attacker, however, does not observe in-ternal policy parameters, reward shaping, and governance updates, thereby creating an information asymmetry between the attacker and the defender. This asymmetry is realistic in many SDN-IoT networks, where attackers can probe the data plane but cannot access the controller’s internals. Nev-ertheless, the attacker may still perform black-box inference over time by correlating probe traffic with observed end-to-end effects; therefore, our model allows adaptive zt while keeping Π and reward internals hidden. We assume that the SDN controller, switch software, and telemetry counters are trusted and uncompromised. The attacker cannot inject malicious controller rules, modify reward signals, and tamper with the RL optimization process. Communication channels between switches and the controller are assumed authenticated, preventing control-plane hijacking. We further assume that the governance layer is not directly compromised (e.g., no prompt injection through raw payload content), since the reflective loop consumes only deterministic aggregate counters and validated trace summaries rather than untrusted free-form text. This reduces the surface for adversarial prompt manipu-lation, though traffic can still indirectly shape the evidence statistics and thereby attempt to steer ∆Π via distribution shift. Out-of-scope threats (explicitly excluded) include: 1) controller compromise and rule poisoning, 2) switch firmware compromise, 3) telemetry tampering and counter spoofing, 4) training-time poisoning of labeled data and evaluation labels (FP/FN), and 5) cryptographic attacks on authenticated control channels. These are important in practice but are orthogonal to the traffic-induced closed-loop instability targeted in this work. The defender aims to minimize long-horizon risk rather than only average performance. Formally, the learning objective is to minimize the tail-sensitive return: 

min  

> π, Π

CVaR α 

> T−1

X

> t=0

γtRt

!

, (46) which ensures that worst-case outcomes such as persistent congestion, controller overload, and widespread misclassifi-cation remain bounded even under adaptive adversarial traffic patterns. This objective aligns with the safety requirements of large-scale SDN-IoT networks, where rare catastrophic failures are more critical than average-case performance. In our setting, “worst-case” is operationally interpreted as tail events in the controller backlog, queue explosions, and high-percentile RTT under adaptive campaigns, and the reflective governance layer is explicitly permitted to reweight controller-stress penalties and tighten heavy-action feasibility when such tail evidence arises, thereby prioritizing resilience to rare but catastrophic episodes. V. E XPERIMENTAL EVALUATION PROTOCOL 

Experimental validation was conducted using the integrated SDN-IoT architecture shown in Figure 2, where IoT traffic is processed by decentralized RL agents on SDN switches under a centralized controller, and policy evolution is performed by a multi-agent LLM governance layer that edits the global policy entity Π. Evaluation is framed as a closed-loop systems study rather than a standalone IDS benchmark, explicitly modeling controller saturation, delayed rule actuation, and flow-table churn as first-class experimental factors. Traffic is generated within the SDN-IoT emulation environment using controlled benign device models and parameterized adversarial injection processes. This design enables systematic manipulation of syn-chronization probability, injection multipliers, and burst timing to stress backlog dynamics and rule-install delays. Static public intrusion datasets are not used because they cannot capture the mitigation–controller feedback loops central to closed-loop stability analysis [39] [40] [41] [42]. Each episode spans a fixed horizon of T fast-time steps with consistent telemetry aggregation. The controller is modeled with a bounded ser-vice rate μ, stochastic service times, and delayed FlowMod execution. All compared methods operate on identical topolo-gies, traffic seeds, controller settings, and observation spaces to ensure fair paired comparisons. Performance is evaluated jointly in terms of security, QoS, and operational cost. Se-curity effectiveness is measured using macro-F1 and worst-case agent performance. QoS integrity is assessed through RTT percentiles and tail amplification analysis. Operational overhead is quantified using FlowMod intensity, disruption 11  

> Fig. 2. Experimental architecture of the self-reflective SDN-IoT network. Local RL agents run on SDN switches; the controller aggregates telemetry and enforces flow rules; and the multi-agent LLM engine performs structured policy evolution based on controller logs.

metrics, and the evolution of controller backlog. Multiple independent seeds are used, and comparisons employ paired statistical tests with reported effect sizes. Baselines include: 1) static threshold-based mitigation, 2) unconstrained PPO, and 3) constrained PPO without governance. All baselines share identical observations and action sets, ensuring that improvements are attributable to governance-driven evolution of Π. Governance evaluation proceeds by generating candidate 

∆Π edits from recent trace windows and validating them against fixed red-team campaigns executed with matched seeds for both the current and candidate policies. Updates are accepted only if hard-safety and non-regression criteria are satisfied. All reflection rounds log evidence bundles, parsed deltas, validation metrics, and approval decisions to guarantee auditability and replayability. Additionally, governance latency and reflection overhead are measured to confirm that slow-timescale policy evolution does not interfere with fast RL control. This protocol enables rigorous assessment of stability, robustness, and conservative policy convergence in controller-coupled SDN-IoT networks. 

A. Adversarial Scenario Design 

Adversarial scenarios are designed to stress both the data plane and the control plane, explicitly exercising controller backlog, delayed rule installation, and flow-table churn. Attack patterns are injected into heterogeneous IoT traffic and vary across episodes in intensity, synchronization probability, and temporal structure. We consider three primary attack classes: 1) high-volume burst attacks that trigger PacketIn surges and rule-install delays; 2) distributed low-rate scanning that in-creases flow-table churn without exceeding volume thresholds; and 3) synchronized mimicry patterns that resemble benign IoT update bursts while gradually elevating controller backlog. Attack intensity is parameterized using injection multipliers and device synchronization probability, spanning mild per-turbation (controller utilization < 60% ) to near-saturation conditions where P 

> i

ρti → μ. Attack timing is randomized to prevent temporal overfitting. To strengthen coverage, valida-tion campaigns include additional stress configurations; burst-and-idle attacks exploiting backlog hysteresis, multi-switch correlated attacks amplifying shared-controller coupling, and low-and-slow persistence patterns that accumulate tail risk without immediate alarms. Several scenarios are intentionally configured near stability boundaries to test whether policies maintain bounded backlog and QoS under escalation. Infras-tructure parameters (controller service rate, queue sizes, flow-table capacities) remain fixed across stress levels, isolating degradation effects to traffic characteristics rather than to variations in topology. Benign synchronized bursts are in-cluded in both training and evaluation to expose and penalize unnecessary disruption behavior. 

B. Security Stabilization Dynamics 

Figures 3, 5 illustrate the training dynamics of decentralized PPO mitigation agents under different trust-region sensitivities governed by the PPO clipping parameter ϵ. In PPO, the clipped surrogate objective constrains the likelihood ratio ρt to the interval [1 − ϵ, 1 + ϵ], thereby limiting the magnitude of policy updates and approximating a first-order trust-region method. The parameter ϵ therefore regulates the bias–variance trade-off in policy optimization; larger ϵ permits more aggressive policy updates with faster adaptation but higher variance, whereas smaller ϵ enforces conservative updates that improve stability at the cost of slower convergence. To examine the impact of trust-region width on closed-loop SDN-IoT sta-bility, we evaluate three configurations simultaneously: 1) a Fast configuration with ϵ = 0.30 , 2) a Best Performance configuration with ϵ = 0 .20 , and 3) a Stable configuration with ϵ = 0 .10 . Because mitigation actions influence controller backlog, flow-table churn, and delayed rule activation, abrupt policy shifts can amplify control-plane dynamics. Thus, ϵ

directly affects not only learning speed but also the stability of the controller-coupled system. Episode length serves as a long-horizon stability proxy, measuring sustained safe operation under adversarial load without controller overload, persistent backlog growth, and oscillatory behavior. Increasing steps per episode, therefore, reflect improved containment and bounded queue evolution. Across all ϵ settings, learning exhibits smooth and monotonic improvement followed by stable plateaus after approximately 700 episodes, indicating bounded closed-loop behavior and absence of divergence. The Fast configuration (ϵ = 0 .30 ) stabilizes earliest because of its larger permissible policy deviations, enabling rapid suppression of high-impact behaviors such as PacketIn escalation. However, its larger trust-region width introduces a slightly higher cross-seed vari-ance. The Best Performance configuration ( ϵ = 0 .20 ) achieves the highest asymptotic episode length, reflecting an effective balance between adaptation speed and regularization. The Stable configuration ( ϵ = 0 .10 ) converges more gradually but exhibits lower variance and a smoother plateau, consistent with stronger trust-region regularization that mitigates destabilizing updates under backlog stress. All curves are averaged across multiple independent seeds with paired scenario schedules to isolate policy learning from environmental stochasticity. 12       

> Fig. 3. Security stabilization dynamics under the Fast configuration (PPO clip ϵ= 0 .30 ). Steps per episode indicate sustained adversarial containment without control-plane overload.
> Fig. 4. Security stabilization dynamics under the Best Performance config-uration (PPO clip ϵ= 0 .20 ). The highest asymptotic episode length reflects extended safe containment under adversarial traffic while preserving controller stability.

The bounded convergence observed across all ϵ values con-firms that clipped PPO, when combined with Π-based safety filtering and controller-aware reward shaping, yields stable decentralized mitigation policies in controller-coupled SDN-IoT networks. Establishing stability across the trust region is essential because the governance layer subsequently edits the global policy constitution Π without retraining the neural parameters. The demonstrated robustness, therefore, provides a principled RL foundation for safe two-timescale policy evolution. VI. S YSTEM -L EVEL PERFORMANCE EVALUATION 

This section evaluates the proposed two-timescale solution as an integrated closed-loop control system, jointly analyzing detection robustness, tail-risk behavior, QoS stability, opera-tional overhead, and governance convergence under controller-coupled adversarial conditions. 

A. Detection Performance and Multi-Agent Consistency 

Detection performance is evaluated at the system level to assess the reliability of SDN-IoT control under heteroge-neous operating conditions. The cumulative distribution in Figure 6 shows that most evaluation episodes cluster within     

> Fig. 5. Security stabilization dynamics under the Stable configuration (PPO clip ϵ=0.10 ). Reduced cross-run variance indicates robust mitigation behavior under stochastic traffic with bounded control-plane impact.

a high-performance region, while a distinct lower tail cap-tures degraded episodes. These lower-tail events correspond to stress conditions described in Section V-A, particularly near-boundary regimes where P 

> i

ρti → μ, indicating struc-turally coupled degradation rather than random classification noise. Table I confirms this behavior numerically, with the minimum performance reaching 0.615 for SW3. Switch-level performance distributions are illustrated in Figure 7. The re-sults show tightly clustered Macro-F1 values across switches, indicating consistent decentralized learning behavior across the topology. Summary statistics in Table I show closely aligned mean F1 values (0.776–0.804) with no statistically significant difference across switches ( p = 0 .27 , d = 0 .32 ). This absence of location bias demonstrates that decentralized PPO agents learn stable mitigation policies without structural asymmetry across network positions. Episode-level results for each decentralized agent are shown in Figure 8. Perfor-mance remains tightly grouped across agents, with similar central tendencies and occasional low outliers under stress. A statistical comparison indicates no significant difference among agents ( p = 0 .19 , d = 0 .28 ), confirming consistent learning behavior across decentralized controllers. Although mean performance remains stable, tail behavior is opera-tionally significant. Table II highlights a practically meaningful mean–worst performance gap of 0.060 (0.785 vs. 0.725), indicating degradation during extreme episodes. The large effect size reported in Table III ( d = 0 .91 ) confirms that worst-case conditions constitute a distinct performance regime. Variance decomposition further shows that within-switch vari-ability dominates between-switch variability, indicating that episodic stress, rather than structural-topology differences, drives uncertainty. 

B. Security-Cost Trade-off 

The security-cost relationship is analyzed as a coupled con-trol phenomenon, since in SDN-IoT networks, improvements in detection inherently entail actuation overhead and QoS impacts. The global Pareto behavior [43] in Figure 9 shows a clear pattern; higher Macro-F1 correlates with increased disruption cost from FlowMod intensity and control-plane 13   

> Fig. 6. Empirical cumulative distribution function (CDF) of detection perfor-mance (Macro-F1) across evaluation episodes.
> Fig. 7. Distribution of Macro-F1 per switch with episode-level points. Cen-tral tendencies remain closely aligned across switches, indicating consistent decentralized performance across the topology.

actuation. The Pareto frontier indicates that performance im-provements beyond 0.80 F1 require disproportionately higher mitigation effort, revealing diminishing returns at higher de-tection levels. This escalation reflects a structural property of SDN control loops: aggressive containment actions, such as frequent rule installation and rate limiting, increase controller load and flow-table churn, which in turn feed back into latency and backlog dynamics. Thus, security improvements are not independent but tied to the resource-constrained control system with finite μ and delayed actuation. Stratified results in Figure 10 clarify this escalation. Episodes grouped into Low, Medium, and High detection levels show consistent growth in disruption. Table IV quantifies this increase; disruption rises from 26.9 (Low) to 28.9 (Medium) to 30.0 (High). One-way ANOVA [44] confirms significant differences in disruption across detection levels ( p < 0.01 ). Post hoc comparisons report a moderate effect size between Low and Medium (d = 0 .64 ) and a large effect size between Low and High ( d =                            

> Fig. 8. Episode-level Macro-F1 for each decentralized agent. Performance remains tightly clustered across agents. TABLE I PER -SWITCH MACRO -F1 SUMMARY STATISTICS (MEAN ,STANDARD DEVIATION ,MINIMUM ,AND MAXIMUM )ACROSS EVALUATION EPISODES .Switch Mean Std Min Max SW0 0.804 0.060 0.689 0.895 SW1 0.784 0.050 0.666 0.854 SW2 0.777 0.057 0.674 0.876 SW3 0.776 0.062 0.615 0.873

1.12 ), indicating that escalation is structurally meaningful. The large η2 = 0 .41 reported in Table V suggests that a significant portion of disruption variance is explained by detection esca-lation, reinforcing that the trade-off is systemic. QoS impact varies under mitigation pressure, with Figure 11 showing pos-itive coupling between FlowMods and RTT p95 1. The Pearson coefficient [45] equals r = 0 .69 with p = 0 .00025 , confirming robustness. The coefficient of determination ( R2 = 0.48 )indicates that nearly half of the variability in RTT is explained by mitigation intensity. Although RTT stays within acceptable limits, the upward slope indicates direct latency sensitivity to aggressive mitigation. Further, Figures 9 10 11, and Table IV reveal a coherent structural relationship: detection gains are statistically significant yet coupled with increased disruption and QoS impact. The transition from Medium to high detec-tion yields smaller incremental F1 gains than the transition from low to medium, confirming diminishing marginal utility. RTT percentiles remain bounded despite FlowMod escalation, suggesting that safety-constrained execution and controller-aware penalties prevent runaway latency. This behavior shows that the system operates within a stable region of the backlog dynamics, not crossing into persistent overload. Table V sum-marizes significance and effect sizes for detection escalation and mitigation-QoS coupling. Results confirm a statistically robust and practically meaningful trade-off, supporting the idea                   

> 1RTT p95 denotes the 95th percentile of end-to-end round-trip time measurements, i.e., the latency value below which 95% of observed packets fall. It captures upper-tail delay behavior and near-saturation effects that are not reflected by mean latency 14
> TABLE II SUMMARY OF MULTI -AGENT ROBUSTNESS METRICS ,INCLUDING OVERALL MEAN MACRO -F1, AVERAGE WORST -AGENT PERFORMANCE ,
> AND THE MEAN –WORST PERFORMANCE GAP .Metric Value Mean F1 (all agents) 0.785 Average worst-agent F1 0.725 Mean-Worst gap 0.060 Std across agents 0.058
> Fig. 9. Detection–disruption trade-off across evaluation episodes. Higher macro-F1 levels are systematically associated with greater operational dis-ruption.

that robust SDN-IoT defense requires coordinated governance of detection and actuation rather than isolated optimization of accuracy. Moreover, diminishing returns justify using a tail-aware objective rather than maximizing mean detection. Pushing F1 beyond 0.80 induces disproportionate actuation with limited containment benefit, increasing controller stress. The reflective governance layer acts as a structural regulator, adjusting Π to rebalance reward weights and thresholds as FlowMod-RTT coupling intensifies, thereby preventing the system from becoming unstable. Thus, the trade-off curves not only show cost escalation but also empirically validate the need for the two-timescale architecture. Fast RL maximizes local containment, while slow governance reshapes the feasi-ble action envelope to maintain system-level efficiency. The Pareto frontier represents both the security-cost relationship and the operational boundary of stable SDN-IoT control under adaptive adversarial pressure. 

C. QoS Integrity 

QoS integrity is assessed through the empirical RTT dis-tribution shown in Figure 12, as latency stability is critical in SDN-IoT networks. The cumulative distribution shows three latency zones: a low-latency region under 76 ms, a central concentration between 76–78 ms, and a limited upper tail beyond 78 ms. The curve is smooth and monotonic, indicating no instability and oscillations during mitigation. This smooth profile is vital in SDN systems, where aggres-sive mitigation can cause feedback oscillations. The absence of irregularities suggests that the controller-agent interaction remains stable. The median RTT is 77.12 ms, with the 10th and 90th percentiles at 75.99 ms and 78.62 ms, respectively. The inter-decile range is 2.63 ms, showing that mitigation  

> Fig. 10. Operational disruption stratified by detection level (Low, Medium, High). It shows systematic escalation in disruption as Macro-F1 increases.
> Fig. 11. Mitigation–QoS coupling between FlowMod intensity and RTT p95. The positive association indicates that greater actuation effort is associated with higher latency variability.

actions do not cause large-scale jitter and systemic latency drift. Since mitigation intensity affects FlowMod frequency and the control-plane load, the bounded inter-decile dispersion suggests that queue evolution remains within a stable region defined by the controller’s service condition. This ensures that latency remains controlled, as mitigation-induced traffic does not exceed service capacity. The distribution shows mild positive skewness, with the upper tail slightly extended. A one-sample comparison against the baseline RTT (77 ms) reveals no significant deviation in central tendency ( p = 0 .41 ), with a small effect size ( d = 0 .11 ). However, values exceeding the 90th percentile show a statistically significant but small increase in latency ( p < 0.05 , d = 0 .38 ), suggesting that inten-sified mitigation marginally increases the frequency of extreme latency episodes without affecting the median. Importantly, the CDF shows no multimodality and sharp inflection points, indicating that governance-driven policy adjustments don’t induce oscillations. The absence of heavy-tail expansion sug-gests that FlowMod-based mitigation does not lead to runaway latency, aligning with the mitigation-QoS coupling analysis in Figure 11. From a risk-sensitive perspective, the absence of heavy-tail growth supports the CVaR-based optimization objective. The bounded distribution indicates effective tail-risk minimization under reflective policy evolution, preventing catastrophic controller overload. Table VI quantifies the RTT variability, which, although statistically significant, remains small in magnitude, ensuring service continuity. Furthermore, these results show that mitigation-aware control preserves QoS 15                                      

> TABLE III STATISTICAL COMPARISON OF DETECTION PERFORMANCE ACROSS SWITCHES AND AGENTS ,REPORTING EFFECT SIZES AND SIGNIFICANCE LEVELS .Comparison Effect Size (Cohen’s d)p-value Interpretation Best vs Worst Switch Mean 0.32 0.27 Small, not significant Best vs Worst Agent Mean 0.28 0.19 Small, not significant Mean vs Worst-Agent Gap 0.91 <0.01 Large, practically important Within-switch variability vs mean diff 1.12 <0.01 Variability dominates structure TABLE IV QUANTITATIVE SUMMARY OF DETECTION –COST TRADE -OFF ACROSS EPISODE -LEVEL DETECTION BINS (L OW , M EDIUM , H IGH ). F1 Level Mean F1 Disruption RTT p95 (ms) FlowMods Low 0.739 26.9 77 153 Medium 0.780 28.9 77 160 High 0.818 30.0 77 175
> Fig. 12. Empirical CDF of end-to-end RTT across evaluation episodes.

integrity, with security escalation inducing measurable but bounded latency sensitivity. Governance mechanisms prevent structural drift towards unstable delay, maintaining a con-trolled efficiency envelope without sacrificing performance. 

D. System-Level Summary 

The system-level evaluation integrates detection robustness, QoS stability, operational disruption, and governance dynam-ics within a unified closed-loop solution. Figure 13 presents a normalized multi-metric profile across agents, demonstrating that improvements in detection performance do not induce disproportionate control-plane stress. The balanced radial mag-nitudes confirm coordinated gains across the security effec-tiveness and stability dimensions, indicating that performance improvements arise from structural alignment rather than from single-metric optimization. Quantitative results summarized in Table VII show that the macro-F1 mean reaches 0.785 (Std = 0.030). A one-sample comparison against the PPO-only baseline (Mean = 0.773) yields statistically significant improvement ( t(39) = 3 .92 , p < 0.01 ) with moderate-to-large effect size ( d = 0 .74 , 95% CI [0.28, 1.19]). Worst-case perfor-mance (Worst-F1) also improves significantly ( t(39) = 2 .41 ,

p < 0.05 , d = 0 .63 ), indicating compression of tail degrada-tion. RTT p95 remains statistically unchanged ( t(39) = 0 .88 ,

p = 0 .38 ), confirming that robustness gains are not achieved at the expense of QoS inflation. Operational disruption (Mean   

> Fig. 13. Normalized multi-metric security–stability profile across agents.
> Fig. 14. Policy evolution under LLM-based governance, showing accepted versus rejected structured updates across reflection rounds.

= 28.94, Std = 1.92) exhibits moderate variance explained by detection escalation ( η2 = 0 .36 ), but remains within bounded limits. FlowMod intensity (Mean = 163.75, Std = 10.95) exhibits no heavy-tail amplification. Governance convergence behavior is illustrated in Figure 14. Early reflection rounds display alternating acceptance and rejection of policy deltas, whereas later rounds stabilize. Linear regression over rejection rate per round yields a significant negative slope ( β = −0.041 ,

R2 = 0 .61 , p < 0.01 ), confirming convergence of the policy constitution Π rather than oscillatory drift. To evaluate the conservative non-regression mechanism, Figure 15 demon-strates candidate ∆Π edits under identical red-team stress campaigns. The feasible region is defined by ∆F 1 ≥ − ϵF 116                                                                                      

> TABLE V STATISTICAL TESTS OF THE SECURITY –COST TRADE -OFF ,REPORTING EFFECT SIZES AND SIGNIFICANCE LEVELS .Test Effect Size p-value Interpretation Detection Level →Disruption (ANOVA) η2= 0 .41 <0.01 Large effect Low vs High Disruption d= 1 .12 <0.01 Large increase FlowMods →RTT p95 (Pearson) r= 0 .69 0.00025 Strong coupling FlowMods →RTT ( R2)0.48 <0.001 48% variance explained TABLE VI STATISTICAL SUMMARY OF RTT DISTRIBUTION METRICS ,INCLUDING CENTRAL TENDENCY ,DISPERSION ,AND UPPER -TAIL ELEVATION .Metric Value p-value Effect Size Median RTT 77.12 ms 0.41 d= 0 .11
> P10 75.99 ms ––P90 78.62 ms ––Inter-decile Range 2.63 ms ––Upper-tail Elevation +1.5% <0.05 d= 0 .38
> Skewness (qualitative) Mild positive –Small TABLE VII OVERALL SYSTEM STATISTICAL SUMMARY
> Metric Mean Std Test Statistic Effect Size Macro-F1 0.785 0.030 t(39) = 3 .92 d= 0 .74
> Worst-F1 0.725 0.041 t(39) = 2 .41 d= 0 .63
> RTT p95 (ms) 77.1 0.62 t(39) = 0 .88 Small Disruption 28.94 1.92 F(2 ,57) = 6 .31 η2= 0 .36

and ∆RT T p95 ≤ ϵrtt . Of 25 evaluated candidates, 11 were ac-cepted (44.0%) and 14 rejected. No accepted proposal violated hard-safety constraints. Descriptive statistics of accepted and rejected deltas are reported in Table VIII. Accepted edits show positive mean detection shift (+0.006) and insignificant RTT inflation (+0.12 ms), whereas rejected edits exhibit negative detection drift (-0.028) and substantial latency inflation (+2.14 ms). Inferential comparison using Welch’s t-tests [46] confirms statistical separation between accepted and rejected deltas, as shown in Table IX. Detection deltas differ significantly (t(20 .8) = 4 .87 , p < 0.001 , d = 1 .42 , 95% CI [0.83, 2.01]) and latency deltas also differ significantly ( t(18 .6) = 5 .31 ,

p < 0.001 , d = 1 .56 , 95% CI [0.95, 2.17]). Both effect sizes exceed 1.4, indicating a large practical separation between beneficial and destabilizing policy edits. The aggregate impact of accepted governance updates is summarized in Figure 16. Governance-driven policy evolution increases Mean F1 by 1.3% and Worst-F1 by 4.3%, while reducing controller load by 27.3% and operational disruption by 5.9%. The absence of a significant change in RTT p95 confirms that robustness improvements are achieved without latency inflation. Col-lectively, the statistical evidence demonstrates that the two-timescale architecture enforces conservative non-regression, compresses worst-case degradation, reduces controller stress, and preserves QoS stability. 

E. Runtime Trace Analysis 

Figure 17 provides a step-level view of Π-constrained de-centralized PPO control during synchronized burst conditions.                                     

> Fig. 15. Conservative non-regression gating under red-team evaluation. Candidate policy edits are accepted only if they lie within the feasible region defined by ∆F1≥ − ϵF1and ∆RT T p95 ≤ϵrtt .TABLE VIII DESCRIPTIVE STATISTICS OF GOVERNANCE GATING OUTCOMES
> Metric Accepted (n=11) Rejected (n=14) Mean ∆F1 +0.006 -0.028 Std ∆F1 0.009 0.021 Mean ∆RTT p95 (ms) +0.12 +2.14 Std ∆RTT p95 0.31 1.08 Hard-Safety Violations 07

At Episode 214 (Step 37), controller backlog reaches d = 41 

with elevated flow pressure and pending FlowMods, indicating transient control-plane stress. Switch agents (e.g., SW1, SW2) initially sample heavy actions, such as DROP_FLOW , but these are masked by overload-aware constraints encoded in Π

once the backlog and pressure thresholds are exceeded. The enforced fallback to rate limiting reduces PacketIn intensity (e.g., from 318 → 247 packets/s at SW2) and decreases the backlog ( 41 → 39 ), while preserving RTT p95 . This validates the controller dynamics dt+1 = max(0 , d t + P 

> i

ρti − μ):once effective arrival falls below the service capacity μ,the backlog contracts and stability is restored without la-tency amplification. The governance layer subsequently an-alyzes masking frequency and delayed-actuation signatures to identify structural vulnerabilities under overload. Through the Critic–Compiler–Judge pipeline, a structured ∆Π update tightens heavy-action constraints and increases controller-penalty weighting during saturation. The candidate passes non-regression and hard-safety validation before activation. This trace demonstrates coordinated interaction across timescales: RL sampling proposes mitigation, Π enforces instantaneous safety through constrained execution, and governance refines structural constraints based on evidence of persistent stress. Stability is maintained without retraining, confirming closed-17                       

> TABLE IX INFERENTIAL COMPARISON OF ACCEPTED VS REJECTED POLICY DELTAS
> (W ELCH ’S T -TEST )Metric tdf p-value Cohen’s d(95% CI)
> ∆F1 4.87 20.8 <0.001 1.42 [0.83, 2.01]
> ∆RTT p95 5.31 18.6 <0.001 1.56 [0.95, 2.17]
> Fig. 16. Normalized governance benefit summary. Accepted policy edits improve mean and worst-case detection while reducing controller load and operational disruption.

loop stabilization through policy-constitution editing rather than parameter adjustment. VII. C OMPARISON WITH PREVIOUS WORK 

Recent SDN-IoT security research has largely focused on improving attack-detection accuracy by deploying ML-based models on centralized SDN controllers. DL–based IDS using CNN, LSTM, and hybrid architectures achieve high classification accuracy but remain detection-centric and rely on static mitigation pipelines triggered by predefined rules rather than adaptive control mechanisms [47] [48] [28] [31]. More recent studies investigate adaptive defense strategies using reinforcement learning and hybrid AI architectures. RL approaches improve mitigation decisions by enabling agents to react dynamically to changing network conditions [29]. Hybrid architectures combining reinforcement learning with large language models further support automated reasoning and operational decision support in cyber defense systems [26]. However, these solutions typically update policies through opaque parameter adjustments without explicitly modeling control-plane dynamics and governance mechanisms. Other work explores distributed cyber defense using multi-agent systems, where cooperative agents perform detection and response across network nodes [32]. Control-theoretic meth-ods have also been proposed to maintain system stability under adversarial conditions such as false-data injection at-tacks [37]. Additional research addresses SDN-IoT security through QoS-aware routing and attack detection frameworks that integrate deep learning with network optimization [27]. Although these approaches improve detection accuracy and coordination, they generally do not incorporate adaptive policy evolution and controller-aware mitigation. In contrast, this work models SDN-IoT defense as a closed-loop control system in which mitigation actions reshape future network obser-vations through delayed rule activation, flow-table pressure, and controller backlog dynamics. The proposed solution in-troduces a two-timescale architecture that separates real-time mitigation from long-horizon governance. Fast-decentralized PPO agents operate under safety-constrained policies, while a slower governance layer updates a global policy entity via au-ditable, machine-readable deltas ∆Π . A Critic–Compiler–Red-Team–Judge validation pipeline enforces strict non-regression constraints across security, QoS, and operational stress condi-tions. Table X summarizes the capability differences between representative prior work and the proposed approach. VIII. D ISCUSSION 

The experimental results demonstrate that the two-timescale architecture achieves robustness by constraining tail-risk be-havior in SDN-IoT networks rather than maximizing aver-age detection accuracy. While the mean Macro-F1 remains consistent, system reliability is driven by episodic degra-dation events, with the observed mean–worst performance gap highlighting the impact of transient backlog surges and synchronized traffic bursts. This reinforces the separation between fast RL control and slow reflective policy evolu-tion. Decentralized PPO agents respond immediately to lo-cal telemetry, while the governance layer adjusts the shared policy constitution Π to mitigate recurrent instability without destabilizing the learned parameters. This architecture rede-fines robustness as a structural property of the closed-loop system, focusing on compressing the lower tail of performance rather than shifting the distribution upward. The performance variability is predominantly within switches, indicating con-sistent baseline behavior across the topology. However, the persistence of lower-tail episodes suggests that RL alone does not fully capture the effects of control-plane feedback, such as queue occupancy and flow-table pressure. Reflective governance addresses this by adapting the admissible action set SΠ(o), reshaping safety thresholds, and adjusting reward priorities. The relationship between detection performance and operational cost further emphasizes the need for structural constraints. Detection improvements coincide with increases in FlowMod intensity and disruption, and the diminishing returns beyond a certain accuracy threshold indicate that higher detection levels require disproportionately greater mitigation effort. Without governance constraints, detection-accuracy-driven optimization would push the system toward saturation, destabilizing queue dynamics. The governance layer ensures stability by selectively admitting policy edits when robustness gains align with QoS stability. The latency distribution shows that actuation does not induce oscillatory behavior, with mild latency increases observed during aggressive mitigation. These findings confirm that backlog-aware reward shaping and Π-based action masking prevent runaway latency amplification, maintaining system stability even under adversarial load fluc-tuations. The absence of heavy-tail expansion validates the effectiveness of the CVaR-oriented objective and constraint gating in preventing instability. Furthermore, governance de-cisions evolve across reflection rounds. Early rounds show exploratory adjustments to Π, with rejection rates decreas-ing over time as the policy constitution stabilizes. Accepted 18 

[Runtime Trace] Ep=214 | Step=37 | Mode=Eval | TraceID=rt-214-37 RTT_p95=78.1 ms | Backlog d=41 | Flow pressure=0.66 | Pending FlowMods=18 

GLOBAL CONTROLLER STATE 

Service rate μ=7 jobs/s | Ctrl utilization=0.71 | Throttle=ON Heavy-action masks (last 20 steps)=6 | Mean rule delay=168 ms Regime: synchronized IoT burst + credential guessing Reflective governance active: DeltaPi gating enabled 

SW1 (smart-home) rate=0.92M/s | queue=0.57 | flowtbl=0.74 PPO sample: DROP_FLOW Safety constraint (Π): overload-aware masking applied Executed: RATE_LIMIT | queue 0.57 →0.48 Reward: +0.20 | ctrl-penalty enforced 

SW2 (industrial) rate=1.14M/s | queue=0.63 | flowtbl=0.79 PPO sample: DROP_FLOW 

Π restriction: backlog + flow pressure → heavy actions disabled Executed: RATE_LIMIT Effect: PacketIn 318 →247/s | backlog 41 →39 | RTT 78.1 →77.6 ms Reward: +0.30 | stability margin positive LLM Governance Layer (Critic → Compiler → Judge) Detected structural issue: overload-induced delayed actuation Risk characterization: QoS tail inflation under frequent masking Proposed ∆Π : tighten heavy-action mask when (d>40) OR (flow_pressure>0.75) Policy update: increase ctrl-penalty weight (wctrl ↑) under saturation Validation: non-regression=PASS | hard-safety=PASS | rollback-ready=YES Decision: APPROVE | Activation: next policy checkpoint 

GLOBAL OUTCOME 

Mean reward=+0.317 | Backlog decreasing (41 →39) QoS preserved | Control-plane stabilized under burst regime Interpretation: constrained RL + reflective ∆Π governance ensured safe adaptation 

Fig. 17. Runtime trace illustrating Π-constrained decentralized PPO control and reflective LLM-based governance that generates auditable ∆Π updates under control-plane stress. TABLE X CAPABILITY COMPARISON WITH REPRESENTATIVE SDN-I OT SECURITY APPROACHES . RL ROLE : D = D ETECTION , M = M ITIGATION , C = CONTROLLER -AWARE CONTROL , G = G OVERNANCE -SHAPED CONTROL . S YMBOLS : ✓ = EXPLICIT CAPABILITY , △ = PARTIAL SUPPORT , × = NOT ADDRESSED .

Work RL role Closed-loop Ctrl.-aware Delayed Risk LLM gov. ∆T Fail-safe 

Chouhan et al. [28] D × × × × × × ×

Yasarathna et al. [47] D × × × × × × ×

Gali et al. [48] D × × × × × × ×

Kokila and Reddy [27] D △ × × × × × ×

Shi et al. [29] M △ △ × △ × × ×

Loevenich et al. [32] M △ △ × △ △ × △

Wang et al. [31] M △ △ × × × × △

Xu et al. [37] M △ △ × △ × × △

This work C+G ✓ ✓ ✓ ✓ ✓ ✓ ✓

edits improve worst-case detection without increasing the 95th percentile RTT, confirming the effectiveness of the non-regression gating mechanism. These results highlight the two-timescale architecture’s balance between agile local policies and conservative global policy evolution, distinguishing it from detection-driven and parameter-centric approaches. IX. L IMITATIONS AND FUTURE WORK 

Although the proposed two-timescale SDN-IoT solution demonstrates robust and bounded empirical convergence, sev-eral limitations remain. First, the CVaR-based objective im-proves sensitivity to tail risk but does not provide formal worst-case stability guarantees under arbitrary adversarial traffic. Future work should investigate Lyapunov-based analysis and constrained MDP formulations for theoretical boundedness guarantees. Second, the governance layer relies on summarized telemetry, which may limit responsiveness to rare failure modes; richer temporal representations could improve sen-sitivity. Third, policy-constitution edits are constrained by non-regression checks but lack formal structural verification, motivating the integration of symbolic methods. Fourth, the design assumes trusted controller infrastructure, requiring se-19 

cure telemetry attestation and integrity monitoring in adver-sarial deployments. Additionally, decentralized agents operate without explicit coordination, and static reflection scheduling and risk parameters may limit adaptability. Future research should explore coordinated multi-agent training, adaptive risk tuning, and validation in real-world deployments. X. C ONCLUSION 

This paper presented a self-reflective two-timescale control architecture for safe RL in SDN-IoT networks. The architec-ture separates fast, decentralized per-switch RL from slow, structured policy governance using a multi-agent LLM ensem-ble. The reflective layer edits an auditable global policy entity 

Π rather than directly modifying RL parameters. Experimental results show that reliability in SDN-IoT systems is more de-pendent on tail risk than on mean detection accuracy. The gov-ernance mechanism mitigates tail degradation without instabil-ity. Although mitigation intensity correlates with operational cost, structured policy editing constrains escalation within safe limits. This solution integrates decentralized RL with conser-vative policy evolution to balance security, QoS integrity, and governance convergence. The contribution lies in reframing adaptive SDN defense as a policy-management problem, in which stability arises from the interplay between local control and slow policy evolution. Interpretable policy-structure edit-ing provides a scalable approach to align learning-based mit-igation with operational change-control principles in safety-critical network environments. REFERENCES [1] E. Dritsas and M. Trigka, “A survey on the applications of cloud computing in the industrial internet of things,” Big data and cognitive computing , vol. 9, no. 2, p. 44, 2025. [2] I. Cheikh, S. Roy, E. Sabir, and R. Aouami, “Energy, scalability, data and security in massive iot: Current landscape and future directions,” 

IEEE Internet of Things Journal , 2026. [3] S. Hudda and K. Haribabu, “A review on wsn based resource constrained smart iot systems,” Discover Internet of things , vol. 5, no. 1, p. 56, 2025. [4] M. Ayyadurai, K. Vijay, K. Lokesh, P. Nandha Kumar, and C. Hari Sainath, “Cloud-to-thing continuum: Connecting iot devices and cloud infrastructures,” in Learning-Driven Data Fabrics for Sus-tainability: Cloud-to-Thing Continuum Solutions for Global Challenges .Springer, 2026, pp. 11–29. [5] S. R. Mishra et al. , “Sdn-enabled iot security frameworks: A systematic review,” Technologies , vol. 13, no. 3, p. 121, 2025. [6] “Security and privacy challenges in sdn-enabled iot,” Computers, Mate-rials & Continua , 2024. [7] R. Chaudhary, G. S. Aujla, N. Kumar, and P. K. Chouhan, “A compre-hensive survey on software-defined networking for smart communities,” 

International Journal of Communication Systems , vol. 38, no. 1, p. e5296, 2025. [8] H. Mulugeta, “Review of botnet attack detection in sdn-enabled iot using machine learning,” Sensors .[9] J. McMahan, Y. Wu, X. Zhu, and Q. Xie, “Optimal attack and defense for reinforcement learning,” in Proceedings of the AAAI conference on artificial intelligence , vol. 38, no. 13, 2024, pp. 14 332–14 340. [10] H. Fang and P. Yi, “Adaptive decision-making in attack-defense games with bayesian inference of rationality level,” IEEE Transactions on Industrial Electronics , vol. 71, no. 12, pp. 16 558–16 567, 2024. [11] Z. Mustafa, R. Amin, H. Aldabbas, and N. Ahmed, “Intrusion detec-tion systems for software-defined networks: a comprehensive study on machine learning-based techniques,” Cluster Computing , vol. 27, no. 7, pp. 9635–9661, 2024. [12] A. H. Mozumder, M. J. Basha et al. , “Smartsecchain-sdn: A blockchain-integrated intelligent framework for secure and efficient software-defined networks,” arXiv preprint arXiv:2511.05156 , 2025. [13] S. Troia, L. Borgianni, G. Sguotti, S. Giordano, and G. Maier, “A comprehensive survey on software-defined wide area network,” IEEE Communications Surveys & Tutorials , 2025. [14] Z. A. Bhuiyan, S. Islam, M. M. Islam, A. A. Ullah, F. Naz, and M. S. Rahman, “On the (in) security of the control plane of sdn architecture: A survey,” IEEE Access , vol. 11, pp. 91 550–91 582, 2023. [15] F. Razvan and C. Mitica, “Enhancing network security through inte-gration of game theory in software-defined networking framework: F. razvan et al.” International Journal of Information Security , vol. 24, no. 3, p. 100, 2025. [16] S. Khozam, G. Blanc, S. Tixeuil, and E. Totel, “Qosentry: A rein-forcement learning framework for qos-preserving ddos mitigation in software-defined networks,” Journal of Network and Systems Manage-ment , vol. 33, no. 4, p. 97, 2025. [17] L. P. Aguirre-Sanchez, Y. Shen, and M. Guo, “Qos-congestion aware deep reinforcement learning approach for multi-path routing in software-defined networks,” Journal of Network and Computer Applications , 2025. [Online]. Available: https://www.sciencedirect.com/ science/article/pii/S221421262500331X [18] D. Kalambe, D. Sharma, P. Kadam, and S. Surati, “A comprehensive plane-wise review of ddos attacks in sdn: Leveraging detection and mitigation through machine learning and deep learning,” Journal of Network and Computer Applications , vol. 235, p. 104081, 2025. [19] A. Hozouri, A. Mirzaei, and M. Effatparvar, “A comprehensive survey on intrusion detection systems with advances in machine learning, deep learning and emerging cybersecurity challenges,” Discover Artificial Intelligence , vol. 5, no. 1, p. 314, 2025. [20] B. Khanal, C. Kumar, and M. S. A. Ansari, “Real-time anomaly detection framework to mitigate emerging threats in software defined networks,” Journal of Network and Systems Management , vol. 33, no. 2, p. 26, 2025. [21] V. P. Bijlwan and N. Kumar, “Deep reinforcement learning in software defined networking: A survey, research challenges and future perspec-tives,” IEEE Communications Surveys & Tutorials , 2026. [22] V. Balasubramanian, M. Aloqaily, M. Guizani, and B. Ouni, “An iot-driven reinforcement learning framework for optimized flow manage-ment in autonomous systems,” IEEE Internet of Things Journal , 2025. [23] P. Michailidis, I. Michailidis, C. R. Lazaridis, and E. Kosmatopoulos, “Traffic signal control via reinforcement learning: A review on applica-tions and innovations,” Infrastructures , vol. 10, no. 5, p. 114, 2025. [24] V. N. Skoropad, S. Dej .anski, V. Pantovi´ c, Z. Injac, S. Vujiˇ ci´ c, M. Jo-vanovi´ c-Milenkovi´ c, B. Jevti´ c, V. Luki´ c-Vujadinovi´ c, D. Vidojevi´ c, and I. Bodolo, “Dynamic traffic flow optimization using reinforcement learning and predictive analytics: A sustainable approach to improving urban mobility in the city of belgrade,” Sustainability , vol. 17, no. 8, p. 3383, 2025. [25] A. L. Buczak and E. Guven, “A survey of data mining and machine learning methods for cyber security intrusion detection,” IEEE Commu-nications Surveys and Tutorials , vol. 18, no. 2, pp. 1153–1176, 2016. [26] S. Khalis, K. Hassini, M. Chemmakha, O. Habibi, and M. Lazaar, “Robust cnn-based threat detection in sdn-enabled iot networks,” Journal of Network and Systems Management , vol. 34, no. 2, p. 40, 2026. [27] K. M. Kokila M and S. R. K. Srinivasa Reddy Konda, “Deepsdn: Deep learning based software defined network model for cyberthreat detection in iot network,” ACM transactions on internet technology , 2025. [28] A. Chouhan, N. Shahriar, and J. Yao, “Hcl: A hybrid cnn-lstm framework for intrusion detection in sdn-iot networks,” in 2025 International Conference on Computing, Networking and Communications (ICNC) .IEEE, 2025, pp. 254–258. [29] Y. Shi, H. Zhang, L. Shi, and S. Xu, “Autonomous cyber defense for aiot using graph attention network-enhanced reinforcement learning,” 

Computer Communications , vol. 241, p. 108265, 2025. [30] W. Gan and L. Qiao, “An adaptive deep reinforcement learning frame-work for auv attack-defense games,” IEEE Transactions on Intelligent Transportation Systems , 2025. [31] Z. Wang, Y. Wang, X. Xiong, Q. Ren, and J. Huang, “A novel framework for enhancing decision-making in autonomous cyber defense through graph embedding,” Entropy , vol. 27, no. 6, p. 622, 2025. [32] J. Loevenich, E. Adler, T. Huerten, and R. R. F. Lopes, “Design and evaluation of an autonomous cyber defence agent using drl and an augmented llm,” Computer Networks , vol. 262, p. 111162, 2025. [33] X. Cadet, S. Boboila, E. Koh, P. Chin, and A. Oprea, “Quantitative resilience modeling for autonomous cyber defense,” arXiv preprint arXiv:2503.02780 , 2025. 20 

[34] A. T. Luc´ ıa, R. Miguel, P. Javier, and A. L. Mario, “cyberspade: A hierarchical multi-agent architecture for coordinated cyberdefense,” 

Journal of Cybersecurity and Privacy , vol. 6, no. 1, p. 28, 2026. [35] V. Selvaraj, “Cognitive ai for proactive cyber defense: A rule-based multi-agent approach,” in 2025 IEEE International Conference on Re-cent Advances in Computing and Systems (REACS) . IEEE, 2025, pp. 1–9. [36] H. Aydin and M. A. Aydin, “A multi-agent-based ddos detection and defense system design with federated learning and blockchain in public cloud network environment,” Cluster Computing , vol. 28, no. 16, p. 1018, 2025. [37] Y. Xu, W. Zhang, Y. Zhang, and Z.-G. Wu, “Distributed resilient control of cyber-physical multi-agent systems under attacks,” IEEE Transactions on Industrial Cyber-Physical Systems , 2025. [38] A. Arletti, “A directional rockafellar-uryasev regression,” arXiv preprint arXiv:2411.02557 , 2024. [39] M. Ficco and F. Palmieri, Security and resilience in intelligent data-centric systems and communication networks . Academic Press, 2017. [40] H. Zhu, L. Xu, Z. Bao, Y. Liu, L. Yin, W. Yao, C. Wu, and L. Wu, “Secure control against multiplicative and additive false data injection at-tacks,” IEEE Transactions on Industrial Cyber-Physical Systems , vol. 1, pp. 92–100, 2023. [41] W. Xing and J. Shen, “Security control of cyber–physical systems under cyber attacks: A survey,” Sensors , vol. 24, no. 12, p. 3815, 2024. [42] S. Huang, C. M. Poskitt, and L. K. Shar, “Security modelling for cyber-physical systems: A systematic literature review,” ACM Transactions on Cyber-Physical Systems , 2024. [43] S. Latif, S. Akraam, T. Karamat, M. A. Khan, C. Altrjman, S. Mey, and Y. Nam, “An efficient pareto optimal resource allocation scheme in cognitive radio-based internet of things networks,” Sensors , vol. 22, no. 2, p. 451, 2022. [44] A. Chatzi and O. Doody, “The one-way anova test explained,” Nurse researcher , vol. 33, no. 4, 2025. [45] Y. Liu, Y. Mu, K. Chen, Y. Li, and J. Guo, “Daily activity feature selection in smart homes based on pearson correlation coefficient,” 

Neural processing letters , vol. 51, no. 2, pp. 1771–1787, 2020. [46] F.-X. Standaert, “How (not) to use welch’s t-test in side-channel security evaluations,” in International conference on smart card research and advanced applications . Springer, 2018, pp. 65–79. [47] T. L. Yasarathna, M. Liyanage, and N.-A. Le-Khac, “Deep learning based autonomous anomaly detection for security in sdn-iot networks,” 

IEEE Open Journal of the Communications Society , 2025. [48] M. Gali and A. Mahamkali, “Deep learning based attack detection and qos aware secure routing protocol for sdn-iot network,” Concurrency and Computation: Practice and Experience , vol. 37, no. 6-8, p. e70045, 2025.