---
title: "eBandit: Kernel-Driven Reinforcement Learning for Adaptive Video Streaming"
title_zh: eBandit：用于自适应视频流的内核驱动强化学习
authors: Mahdi Alizadeh
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08791v2"
tags: ["query:netmes"]
score: 7.0
evidence: 使用 eBPF 将网络监控移至 Linux 内核
tldr: 针对用户态自适应比特率（ABR）算法无法实时获取传输层信号且响应滞后的问题，本文提出eBandit框架。该框架利用eBPF技术将网络监控和ABR策略选择下沉至Linux内核，通过轻量级的epsilon-greedy多臂老虎机（MAB）算法，根据实时TCP指标动态评估并切换最优启发式策略。实验表明，eBandit在合成和真实移动网络环境下均显著提升了视频观看体验（QoE）。
selection_source: fresh_fetch
motivation: 用户态ABR算法因缺乏传输层实时信号（如RTT和交付率）且响应滞后，难以应对快速变化的网络环境。
method: 采用eBPF技术在内核sockops程序中实现轻量级多臂老虎机算法，基于实时TCP指标动态选择最优ABR启发式策略。
result: "在合成对抗轨迹上QoE提升了7.2%，并在真实世界移动会话中实现了最高的平均分块QoE。"
conclusion: 将ABR决策逻辑移至内核并结合在线学习，能更精准、快速地响应网络波动，优化视频传输质量。
---

## 摘要
用户空间自适应比特率（ABR）算法无法观测到最重要的传输层信号（如最小 RTT 和瞬时传输速率），且仅在网络变化的影响已传播至播放缓冲区后才做出响应。我们提出了 eBandit，这是一个利用 eBPF 将网络监控和 ABR 算法选择均迁移至 Linux 内核的框架。一个轻量级的 epsilon-greedy 多臂老虎机（MAB）在 sockops 程序中运行，根据源自实时 TCP 指标的奖励来评估三种 ABR 启发式算法。在对抗性合成轨迹上，eBandit 实现了 $416.3 \pm 4.9$ 的累积体验质量（QoE），比表现最好的静态启发式算法高出 $7.2\%$。在 42 个真实会话中，eBandit 实现了 $1.241$ 的每分片平均 QoE，在所有策略中排名最高，这证明了驻留内核的老虎机学习能够迁移到异构的移动网络环境。

## Abstract
User-space Adaptive Bitrate (ABR) algorithms cannot see the transport layer signals that matter most, such as minimum RTT and instantaneous delivery rate, and they respond to network changes only after damage has already propagated to the playout buffer. We present eBandit, a framework that relocates both network monitoring and ABR algorithm selection into the Linux kernel using eBPF. A lightweight epsilon-greedy Multi-Armed Bandit (MAB) runs inside a sockops program, evaluating three ABR heuristics against a reward derived from live TCP metrics. On an adversarial synthetic trace eBandit achieves $416.3 \pm 4.9$ cumulative QoE, outperforming the best static heuristic by $7.2\%$. On 42 real-world sessions eBandit achieves a mean QoE per chunk of $1.241$, the highest across all policies, demonstrating that kernel-resident bandit learning transfers to heterogeneous mobile conditions.