---
title: PCA-Driven Adaptive Sensor Triage for Edge AI Inference
title_zh: 基于 PCA 驱动的边缘 AI 推理自适应传感器分选
authors: "Ankit Hemant Lade, Sai Krishna Jasti, Nikhil Sinha, Indar Kumar, Akanksha Tiwari"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05045v1"
tags: ["query:netmes"]
score: 6.0
evidence: 带宽预算下的自适应传感器采样率
tldr: 针对工业物联网中多通道传感器网络带宽受限的问题，本文提出 PCA-Triage 算法。该算法利用增量主成分分析（PCA）的载荷量，在带宽预算内动态调整各通道的采样率。PCA-Triage 无需训练参数，计算效率极高（单次决策 0.67ms），在多个基准数据集上表现优异，能在大幅降低带宽占用的同时保持接近全量数据的推理精度，且对噪声和丢包具有鲁棒性。
selection_source: fresh_fetch
motivation: 工业物联网中多通道传感器产生的数据量常超过网络带宽限制，亟需高效的在线数据过滤方案。
method: 提出一种基于增量 PCA 载荷的流式算法，根据各通道对全局方差的贡献度动态分配采样频率。
result: "在多个基准测试中，该方法在 50% 带宽下表现优于多数基准，且在 TEP 数据集上仅用 30% 带宽即可维持 0.90 以上的 F1 分数。"
conclusion: PCA-Triage 是一种高效、无需训练且鲁棒性强的边缘端传感器数据分流方案，适用于带宽受限的实时 AI 推理场景。
---

## 摘要
工业物联网中的多通道传感器网络通常会超出可用带宽。我们提出了 PCA-Triage，这是一种流式算法，可在带宽预算限制下将增量 PCA 载荷（loadings）转换为成比例的各通道采样率。PCA-Triage 的运行时间复杂度为 O(wdk)，且无需可训练参数（每次决策耗时 0.67 毫秒）。我们在 7 个基准数据集（包含 8 至 82 个通道）上针对 9 个基准模型进行了评估。在 50% 带宽下，PCA-Triage 在 6 个数据集中的 3 个上是表现最优的无监督方法，并在 6 个数据集中的 5 个上以显著的效应量（r = 0.71--0.91）优于所有基准模型。在 TEP 数据集上，它实现了 F1 = 0.961 +/- 0.001 的性能，与全量数据性能的差距在 0.1% 以内，同时在 30% 的预算下仍能保持 F1 > 0.90。针对性的扩展可将 F1 提升至 0.970。该算法对丢包和传感器噪声具有鲁棒性（在最坏的综合情况下性能下降仅为 3.7--4.8%）。

## Abstract
Multi-channel sensor networks in industrial IoT often exceed available bandwidth. We propose PCA-Triage, a streaming algorithm that converts incremental PCA loadings into proportional per-channel sampling rates under a bandwidth budget. PCA-Triage runs in O(wdk) time with zero trainable parameters (0.67 ms per decision).   We evaluate on 7 benchmarks (8--82 channels) against 9 baselines. PCA-Triage is the best unsupervised method on 3 of 6 datasets at 50% bandwidth, winning 5 of 6 against every baseline with large effect sizes (r = 0.71--0.91). On TEP, it achieves F1 = 0.961 +/- 0.001 -- within 0.1% of full-data performance -- while maintaining F1 > 0.90 at 30% budget. Targeted extensions push F1 to 0.970. The algorithm is robust to packet loss and sensor noise (3.7--4.8% degradation under combined worst-case).