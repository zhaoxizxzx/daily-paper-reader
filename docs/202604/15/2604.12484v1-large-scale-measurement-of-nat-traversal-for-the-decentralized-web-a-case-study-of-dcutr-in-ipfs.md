---
title: "Large-Scale Measurement of NAT Traversal for the Decentralized Web: A Case Study of DCUtR in IPFS"
title_zh: 去中心化网络 NAT 穿透的大规模测量：IPFS 中 DCUtR 的案例研究
authors: "Dennis Trautwein, Cornelius Ihle, Moritz Schubotz, Corinna Breitinger, Bela Gipp"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12484v1"
tags: ["query:netmes"]
score: 9.0
evidence: P2P网络中NAT穿越的大规模测量研究
tldr: "本研究对IPFS网络中的去中心化NAT穿透协议DCUtR进行了首次大规模测量，分析了来自167个国家、8.5万个网络的440万次尝试。研究确立了打洞阶段约70%的成功率基准，并挑战了UDP在NAT穿透中优于TCP的传统观点，证明两者在DCUtR的高精度同步下表现相当。该研究验证了协议在无许可环境下的高效性，为实现去中心化Web的通用连接提供了重要参考和改进路线。"
selection_source: fresh_fetch
motivation: 现有的NAT穿透方案往往重新引入了去中心化系统试图避免的中心化问题，亟需对完全去中心化的穿透协议进行实证评估。
method: 在生产环境的libp2p IPFS网络中，通过大规模测量DCUtR协议的打洞尝试，分析不同网络环境下的连接成功率。
result: "打洞阶段成功率达70%，且TCP与QUIC的成功率在统计上无显著差异，97.6%的成功连接在首次尝试时即建立。"
conclusion: DCUtR协议设计高效且不依赖中继特性，为去中心化网络提供了可靠的连接基础，并指明了通往通用连接的优化方向。
---

## 摘要
去中心化对等网络（P2P）系统的愿景从根本上受到网络地址转换（NAT）穿透挑战的限制，而现有的解决方案往往会重新引入它们试图避免的中心化问题。本文在基于 libp2p 的生产环境星际文件系统（IPFS）网络中，对一种完全去中心化的 NAT 穿透协议——通过中继进行直接连接升级（DCUtR），进行了首次大规模测量研究。基于来自 167 个国家、85,000 多个不同网络的 440 多万次穿透尝试，我们对现代 P2P 连通性进行了实证分析。在前提条件（中继预留和公共地址发现）成功的情况下，我们确定了打洞阶段的条件成功率为 $70\% \pm 7.1\%$，为该领域提供了一个关键的新基准。至关重要的是，我们通过实证挑战了长期以来认为 UDP 在 NAT 穿透方面具有优越性的观点，证明了 DCUtR 基于 RTT 的高精度同步使得 TCP 和 QUIC 的成功率在统计上没有显著差异（均约为 70%）。我们的分析通过展示成功率与中继特性无关，以及该机制的高效性（97.6% 的成功连接在首次尝试时即建立），进一步验证了该协议针对无许可环境的设计。基于此分析，我们提出了旨在实现通用连通性的协议增强具体路线图，并贡献了完整的数据集以促进该领域的进一步研究。

## Abstract
The promise of decentralized peer-to-peer (P2P) systems is fundamentally gated by the challenge of Network Address Translation (NAT) traversal, with existing solutions often reintroducing the very centralization they seek to avoid. This paper presents the first large-scale measurement study of a fully decentralized NAT traversal protocol, Direct Connection Upgrade through Relay (DCUtR), within the production libp2p-based InterPlanetary File System (IPFS) network. Drawing on over 4.4 million traversal attempts from 85,000+ distinct networks across 167 countries, we provide an empirical analysis of modern P2P connectivity. We establish a conditional success rate of $70\% \pm 7.1\%$ for the hole-punching stage, given that prerequisite relay reservation and public address discovery succeed, providing a crucial new benchmark for the field. Critically, we empirically challenge the long-held belief of UDP's superiority for NAT traversal, demonstrating that DCUtR's high-precision, RTT-based synchronization yields statistically indistinguishable success rates for both TCP and QUIC ($\sim70\%$). Our analysis further validates the protocol's design for permissionless environments by showing that success is independent of relay characteristics and that the mechanism is highly efficient, with $97.6\%$ of successful connections established on the first attempt. Building on this analysis, we propose a concrete roadmap of protocol enhancements aimed at achieving universal connectivity and contribute our complete dataset to foster further research in this domain.

---

## 论文详细总结（自动生成）

这是一份关于论文《Large-Scale Measurement of NAT Traversal for the Decentralized Web: A Case Study of DCUtR in IPFS》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在去中心化对等网络（P2P）中，如何在不依赖中心化服务器（如传统的 STUN/TURN）的情况下，实现高效且鲁棒的 NAT（网络地址转换）穿透。
*   **研究动机**：
    *   NAT 技术虽然缓解了 IPv4 地址枯竭，但阻碍了 P2P 直接连接。
    *   现有的 WebRTC/ICE 等方案依赖中心化信令服务器，违背了去中心化 Web 的初衷。
    *   缺乏对生产环境下完全去中心化 NAT 穿透协议（如 libp2p 中的 DCUtR）的大规模实证研究。

### 2. 方法论
*   **核心思想**：利用已有的中继连接（Relay）作为信令通道，通过精确的 RTT（往返时延）测量和同步机制，促使两个位于 NAT 后的节点同时发起连接（打洞），从而升级为直接连接。
*   **关键技术细节（DCUtR 流程）**：
    1.  **地址发现**：利用 Identify 协议获取外部观测到的公共 IP 和端口。
    2.  **中继预留**：私网节点在公共中继节点（Circuit v2）上获取预留位。
    3.  **连接反转（可选）**：若一方为公网，则直接反向连接。
    4.  **同步打洞**：
        *   发起方通过中继发送 `CONNECT` 消息并测量 RTT。
        *   发起方发送 `SYNC` 消息，并在等待 1/2 RTT 后发起拨号。
        *   接收方收到 `SYNC` 后立即发起拨号。
    5.  **传输层支持**：同时支持 TCP（利用 Simultaneous Open）和 QUIC（利用 UDP 初始包）。

### 3. 实验设计
*   **数据集/场景**：
    *   在生产环境的 IPFS 网络中进行，时间跨度为 2022 年 12 月至 2023 年 1 月。
    *   涵盖了来自 167 个国家的 85,000 多个网络，收集了超过 440 万次打洞尝试。
*   **测量架构**：
    *   **Honeypot（诱饵节点）**：持续爬行 DHT 以发现位于 NAT 后且支持 DCUtR 的节点。
    *   **Server（服务端）**：管理任务分配和结果收集。
    *   **Clients（客户端）**：由志愿者运行，负责实际发起打洞测试。
*   **Benchmark/对比**：
    *   以打洞阶段的“条件成功率”为核心指标。
    *   对比了 TCP 与 QUIC 的表现，以及不同中继特性对成功率的影响。

### 4. 资源与算力
*   **算力说明**：本论文属于网络测量研究，而非深度学习模型训练，因此**未涉及 GPU 算力消耗**。
*   **资源投入**：使用了分布式的志愿者客户端（212 个 API Key）、一个基于 Go 编写的测量服务端以及 Postgres 数据库进行数据存储与分析。

### 5. 实验数量与充分性
*   **实验规模**：数据量极大（440 万+ 结果），具有极高的统计显著性。
*   **充分性**：
    *   验证了 5 个核心假设（H1-H4），涵盖了可行性、中继独立性、同步效率、传输层无关性和优化有效性。
    *   **客观性**：通过随机分配协议过滤器（强制使用 TCP 或 QUIC）来确保对比的公平性。
    *   **覆盖面**：全球范围内的网络环境覆盖，使其结论具有普适性。

### 6. 主要结论与发现
*   **成功率基准**：在前提条件（中继和地址发现）成功的情况下，打洞阶段的成功率为 **70% ± 7.1%**。
*   **挑战传统认知**：实证表明 **TCP 和 QUIC 的打洞成功率几乎相同**（均在 70% 左右），打破了“UDP 穿透远优于 TCP”的长期观点。
*   **高效同步**：97.6% 的成功连接在**第一次尝试**时即建立，证明了基于 RTT 的同步机制非常精准。
*   **中继独立性**：打洞成功率与中继节点的地理位置、RTT 或网络特性基本无关，验证了去中心化中继架构的可行性。

### 7. 优点
*   **首次大规模实证**：填补了去中心化 NAT 穿透在大规模生产网络中缺乏数据的空白。
*   **协议设计验证**：有力地证明了 DCUtR 协议在无许可环境下的健壮性。
*   **开源贡献**：提供了完整的、经过匿名化处理的大规模数据集，供社区进一步研究。
*   **优化建议**：提出了基于“生日悖论”的概率穿透和 RTT 计算精细化等具体的协议改进路线图。

### 8. 不足与局限
*   **样本偏差**：志愿者客户端可能比普通用户拥有更好的网络环境（选择偏差）。
*   **发现偏差**：诱饵节点只能发现那些已经能通过中继连接的节点，排除了极度受限的网络。
*   **缺乏 NAT 分类**：由于测量架构限制，无法根据 RFC 4787 对远程节点的 NAT 类型（如对称型 NAT）进行精确分类。
*   **端到端成功率**：70% 是打洞阶段的成功率，若考虑前置的中继预留失败，整体连通率会更低。

（完）
